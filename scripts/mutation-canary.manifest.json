[
  {
    "id": "csv-writer-skips-rather-than-stops",
    "why": "A participant whose answers vanished mid-export is an absence, not the end of the file. `break` turns a skip into silent truncation - the researcher gets a CSV that parses, holding part of their data, with nothing saying so. This mutation survived 825 jest, 590 vitest and 16 database tests.",
    "file": "backend/src/firsthand/survey-csv-response.ts",
    "anchor": "        continue;",
    "mutation": "        break;",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "writes no row for a participant whose answers vanished mid-export"
  },
  {
    "id": "csv-writer-honours-backpressure",
    "why": "Ignoring `res.write`'s return value turns a slow client into an unbounded heap at the socket - reintroducing, one layer up, exactly the problem streaming removes at the database. It also disarms the listener-leak test as a side effect, because a writer that never waits never attaches a listener. Moved here by cto/AdaptaLabs#80: the write loop is one copy with two callers now, so this line guards both exports at once.",
    "file": "backend/src/utils/bounded-socket-write.ts",
    "anchor": "      if (res.write(chunk)) {",
    "mutation": "      if (res.write(chunk) || true) {",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "writes nothing more until the socket drains"
  },
  {
    "id": "csv-writer-destroys-rather-than-ends",
    "why": "Once the header is written the status is 200 and cannot be taken back, so a failure part-way through must destroy the connection. `end()` hands over a short CSV that parses - a researcher computing a mean over part of their data.",
    "file": "backend/src/firsthand/survey-csv-response.ts",
    "anchor": "    res.destroy();",
    "mutation": "    res.end();",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "destroys the socket rather than ending it, when a row throws"
  },
  {
    "id": "csv-writer-removes-its-listeners",
    "why": "A large export under backpressure accumulates one dead error listener per slow write - thousands of them, with the MaxListenersExceededWarning arriving long after the cause. The bug this fixed had no test, so it could come straight back. Moved here by cto/AdaptaLabs#80: the write loop is one copy with two callers now, so this line guards both exports at once.",
    "file": "backend/src/utils/bounded-socket-write.ts",
    "anchor": "        res.off('drain', onDrain);\n        res.off('error', onError);\n        res.off('close', onClose);\n",
    "mutation": "",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "leaves no listener behind, however many writes wait for a drain"
  },
  {
    "id": "csv-writer-treats-a-hangup-as-a-hangup",
    "why": "`close` settling a pending write must RESOLVE. Rejecting routes every ordinary cancellation - a closed tab, a navigation away - into the failure path and logs it as an export failure. The bytes are identical either way, so only the log can see it. Moved here by cto/AdaptaLabs#80: the write loop is one copy with two callers now, so this line guards both exports at once.",
    "file": "backend/src/utils/bounded-socket-write.ts",
    "anchor": "      const onClose = done(() => resolve());",
    "mutation": "      const onClose = done(() => reject(new Error('closed')));",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "treats an ordinary hangup as a hangup, not as a failure"
  },
  {
    "id": "csv-participant-bound-is-detectable",
    "why": "Without the +1 the `>` comparison is unsatisfiable, so the 413 becomes unreachable and a 200,001-participant study quietly exports the first 200,000 as a CSV that parses. Refusal turned into truncation. Survived 824 jest and 602 vitest tests.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        LIMIT ${MAX_CSV_PARTICIPANTS + 1}",
    "mutation": "        LIMIT ${MAX_CSV_PARTICIPANTS}",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export.test.ts",
    "test": "asks the database for one participant more than it will return"
  },
  {
    "id": "csv-participant-bound-admits-its-own-boundary",
    "why": "Only the refusing side was pinned, so `>` becoming `>=` survived every test in the repository - and in production that turns the largest legitimate export into a 413 naming a limit it has not reached. A boundary needs both of its sides.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "      if (result.rows.length > MAX_CSV_PARTICIPANTS) {",
    "mutation": "      if (result.rows.length >= MAX_CSV_PARTICIPANTS) {",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export.test.ts",
    "test": "admits a set that exactly fills the bound"
  },
  {
    "id": "response-row-bound-is-detectable",
    "why": "The same +1 detector on the unbatched reader, which decides the 413 for the aggregate routes. Without it the largest studies are silently truncated into a mean, an NPS and a chart.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        LIMIT ${MAX_RESPONSE_ROWS + 1}",
    "mutation": "        LIMIT ${MAX_RESPONSE_ROWS}",
    "runner": "vitest",
    "spec": "src/firsthand/survey-results-repository.test.ts",
    "test": "asks the database for one row more than it will return"
  },
  {
    "id": "response-row-bound-admits-its-own-boundary",
    "why": "The accepting side of the row bound, for the same reason as the participant one: a limit that refuses the largest legitimate read is as wrong as one that truncates it.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "      if (result.rows.length > MAX_RESPONSE_ROWS) {",
    "mutation": "      if (result.rows.length >= MAX_RESPONSE_ROWS) {",
    "runner": "vitest",
    "spec": "src/firsthand/survey-results-repository.test.ts",
    "test": "returns a result set that exactly fills the bound"
  },
  {
    "id": "csv-batch-asks-for-its-own-ids",
    "why": "The output is identical whether a batch fetched its hundred participants or all of them, so deleting the predicate AND its bind parameter together changes nothing anybody can see - while loading the study's whole answer set once per batch, which is the exact heap streaming exists to remove. It survived every test in four files, including the real-Postgres one, because they all compare output. ANCHOR UPDATED when !214 moved this query into `readBatch` and reindented its parameter list - the canary reported ANCHOR_MISSING rather than quietly dropping the entry, which is the whole point of requiring an exact single match. THE PARAMETER HALF, and only that half: it asserts what the statement BOUND. What it RETURNED is now pinned separately by csv-batch-read-takes-only-what-it-asked-for, which is what closes cto/AdaptaLabs#20 - this entry used to say that gap was open, and it no longer is.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        WHERE ${filter} AND r.session_id = ANY($${params.length + 1}::text[])\n        ORDER BY r.saved_at ASC, r.id ASC\n      `,\n            [...params, batch]",
    "mutation": "        WHERE ${filter}\n        ORDER BY r.saved_at ASC, r.id ASC\n      `,\n            [...params]",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export.test.ts",
    "test": "asks each batch for ITS OWN hundred ids, and never for everything"
  },
  {
    "id": "csv-batch-read-takes-only-what-it-asked-for",
    "why": "The other half of the batch predicate, and the half nothing could see. Widening the statement while leaving the bind parameter in place - `AND (r.session_id = ANY(...) OR TRUE)` - keeps every parameter assertion satisfied and the substring present while every batch reads the study's ENTIRE answer set: the heap defect streaming exists to remove, restored in full. Measured on this branch's base: 62 tests across the four files covering this path, the real-Postgres one included, exit 0. Nothing downstream can see it and nothing downstream is at fault - `streamParticipants` yields from the id list, so the surplus rows are grouped, ignored and dropped, and the CSV bytes are identical. So the new test asserts on the ROWS: what each batch bound, against what Postgres handed back. Not a tighter string match on the WHERE clause, which would be prefix-fragile in the way `clock_timestamp()` proved to be. cto/AdaptaLabs#20.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        WHERE ${filter} AND r.session_id = ANY($${params.length + 1}::text[])\n        ORDER BY r.saved_at ASC, r.id ASC",
    "mutation": "        WHERE ${filter} AND (r.session_id = ANY($${params.length + 1}::text[]) OR TRUE)\n        ORDER BY r.saved_at ASC, r.id ASC",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-batch-scope-postgres.test.ts",
    "test": "reads only the participants each batch asked for",
    "needsDatabase": true
  },
  {
    "id": "opportunity-scope-is-not-study-wide",
    "why": "Collapsing the per-opportunity scope to a study-wide one hands one researcher every participant another recruited, under different consent wording. It passed 588 tests in CI's configuration, because the only place the predicate is evaluated is a test that needs a real database.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        filter: \"s.study_id = $1 AND s.opportunity_id = $2\",\n        params: [scope.studyId, scope.opportunityId]",
    "mutation": "        filter: \"s.study_id = $1\",\n        params: [scope.studyId]",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export-postgres.test.ts",
    "test": "scopes a per-opportunity export to that opportunity alone",
    "needsDatabase": true
  },
  {
    "id": "results-read-ceiling-is-pinned-as-a-literal",
    "why": "Every other test in that file derives its expectations from the constant, so they all keep passing at any ceiling - including one high enough to be no ceiling at all. This number is a memory budget on a single-replica 2Gi pod, and an OOM kill here drops every live participant session.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "export const MAX_CONCURRENT_RESULTS_READS = 1;",
    "mutation": "export const MAX_CONCURRENT_RESULTS_READS = 2;",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "holds the ceiling at the number that was decided"
  },
  {
    "id": "results-read-permit-comes-back-on-close",
    "why": "The permit is released only on the response's close. Losing that release leaks a permit for the life of the process, and two of them shut all four results routes - 503 forever, for every admin including the superadmin, reachable by a tab close. ANCHOR RE-POINTED by the #9 branch, and not because it had gone stale on main: the `release` -> `releaseGate`/`releaseHeld` rename in that branch broke it, the harness reported ANCHOR_MISSING, and it was repaired in the same branch. Main's own copy always located to exactly 1, so there was never an unguarded window on main to go hunting for.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "    abandoned.abort();\n    releaseHeld();",
    "mutation": "    abandoned.abort();",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "gives the permit back when the response closes, however it ended"
  },
  {
    "id": "results-gate-runs-before-the-study-is-read",
    "why": "Loading first means a non-superadmin gets 404 for a study id that does not exist and 403 for one that does - an existence oracle over study ids, to a caller entitled to neither answer. It also makes an unentitled caller pay for a pool checkout.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "  requireSuperadminForStudyResults(req);\n\n  const stored = await getStudyById(req.params.studyId);\n  if (!stored) {\n    throw new NotFoundError('Survey');\n  }\n\n  const responses = await listResponsesForStudy(req.params.studyId);",
    "mutation": "  const stored = await getStudyById(req.params.studyId);\n  if (!stored) {\n    throw new NotFoundError('Survey');\n  }\n\n  requireSuperadminForStudyResults(req);\n\n  const responses = await listResponsesForStudy(req.params.studyId);",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "does not read the study at all before refusing"
  },
  {
    "id": "opportunity-ownership-fails-closed",
    "why": "`canWriteStudy` fails OPEN for an unowned study, because a write can adopt the row and a read can adopt nothing. The same shape here hands every admin the answers of any opportunity whose owner has been removed, and it passed all 293 tests in its own file. The anchor moved when the inline guard became the shared `isOpportunityOwner`; the mutation still spells out the fail-open form the helper replaced, so this entry pins the ROUTE's disposition independently of the helper's - see opportunity-ownership-helper-fails-closed for the helper's own.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "  if (!isSuperadmin && !isOpportunityOwner(row, req.user)) {",
    "mutation": "  if (!isSuperadmin && row && row.owner_user_id != null && row.owner_user_id !== req.user?.id) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.test.ts",
    "test": "refuses an opportunity nobody owns, rather than letting it fail open"
  },
  {
    "id": "export-drain-bound-is-pinned-as-a-literal",
    "why": "The per-write drain bound decides how long one admin who reads nothing can refuse every other admin. Derived from the constant rather than written down, a test cannot see the constant change.",
    "file": "backend/src/firsthand/survey-csv-response.ts",
    "anchor": "export const SURVEY_CSV_DRAIN_TIMEOUT_MS = 30_000;",
    "mutation": "export const SURVEY_CSV_DRAIN_TIMEOUT_MS = 60_000;",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "pins both bounds as literals"
  },
  {
    "id": "export-deadline-is-pinned-as-a-literal",
    "why": "The whole-export wall clock is the ceiling on how long a single request can hold the only results-read permit. Before it existed there was no bound in time at all.",
    "file": "backend/src/firsthand/survey-csv-response.ts",
    "anchor": "export const SURVEY_CSV_EXPORT_DEADLINE_MS = 300_000;",
    "mutation": "export const SURVEY_CSV_EXPORT_DEADLINE_MS = 600_000;",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "pins both bounds as literals"
  },
  {
    "id": "csv-batch-retry-budget-is-bounded",
    "why": "Retries spend the export's deadline. Widened or unbounded, a busy pool turns a bounded export into one that never gives the permit back - the defect the deadline exists to close, reintroduced through the fix for a different one. FOUR, NOT SIX, AND THAT IS NOT ARBITRARY. At six the backoff totals about 15.75s against vitest's 5s budget, so the named test died on a runner timeout BEFORE reaching the assertion - the entry reported KILLED while its pinned literal was never executed, and any change merely making this path slower would have killed it equally. A second false kill of the same family as the attachment one, found by the same gate. At four the test fails on the literal: `expected 5 to be 4`.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "const CSV_BATCH_RETRY_ATTEMPTS = 3;",
    "mutation": "const CSV_BATCH_RETRY_ATTEMPTS = 4;",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export.test.ts",
    "test": "gives up after exactly three retries rather than forever"
  },
  {
    "id": "retry-backoff-abandons-an-already-aborted-signal",
    "why": "`addEventListener('abort')` on a signal that has ALREADY aborted never fires, and the deadline firing during the refused query is exactly how this is reached - so without the check the backoff slept through the deadline in the common case. Same shape as `close` firing once on a response destroyed before the first write.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "    if (signal.aborted) {\n      reject(signal.reason);\n      return;\n    }\n\n",
    "mutation": "    ",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-export.test.ts",
    "test": "abandons its backoff the moment the export deadline fires"
  },
  {
    "id": "server-socket-timeout-is-actually-set",
    "why": "Node's `server.timeout` defaults to 0 - no bound at all on a socket that goes quiet mid-request. It is the backstop for every response that has no deadline of its own, and the assertion that catches its absence was itself reading a Node default until sentinels were added.",
    "file": "backend/src/server-timeouts.ts",
    "anchor": "  server.timeout = SERVER_SOCKET_TIMEOUT_MS;",
    "mutation": "  void SERVER_SOCKET_TIMEOUT_MS;",
    "runner": "jest",
    "spec": "src/__tests__/server-timeouts.test.ts",
    "test": "applies all three to the server it is given"
  },
  {
    "id": "export-signal-reaches-the-batch-reads",
    "why": "`writeSurveyCsv` aborts its controller on every exit, and that is what lets a route test hold the signal its stand-in was handed and assert it ends up aborted. Without it the route can be given a substitute signal with the whole suite green, and the database half of the deadline silently stops being bounded.",
    "file": "backend/src/firsthand/survey-csv-response.ts",
    "anchor": "    expired.abort(new SurveyCsvExportEnded());",
    "mutation": "    void expired;",
    "runner": "vitest",
    "spec": "src/firsthand/survey-csv-response.test.ts",
    "test": "aborts the signal it handed out once the export has ended"
  },
  {
    "id": "f1-precondition-compares-instants-not-strings",
    "why": "`ownerRow.updated_at` arrives from node-pg as a JS Date - measured, and the reason is that nothing in this repository calls `setTypeParser`, so `timestamptz` takes pg's built-in parser. `String(date)` is therefore a LOCALE string (`Fri Aug 21 2026 10:15:30 GMT+0100 (British Summer Time)`), which can never equal the ISO value the caller was served: a raw string comparison does not refuse the occasional save, it refuses EVERY save, for ever, on every study. An earlier version of this entry blamed microsecond precision - TIMESTAMPTZ is microsecond in Postgres, the caller is served a millisecond ISO string - which is plausible and is NOT what happens, because the extra digits are gone before JavaScript sees them. The named test builds a string row value by hand for exactly that reason; it is defensive against a type parser being registered later, not a shape production produces.",
    "file": "backend/src/firsthand/studies-repository.ts",
    "anchor": "        const storedUpdatedAt = new Date(ownerRow.updated_at).getTime();\n        const claimedUpdatedAt = new Date(expectedUpdatedAt).getTime();",
    "mutation": "        const storedUpdatedAt = String(ownerRow.updated_at);\n        const claimedUpdatedAt = String(expectedUpdatedAt);",
    "runner": "vitest",
    "spec": "src/firsthand/studies-repository.test.ts",
    "test": "matches a row whose stored value carries microseconds"
  },
  {
    "id": "f1-precondition-fails-closed-on-an-unreadable-claim",
    "why": "The fail-closed direction has NO code of its own: `new Date('nonsense').getTime()` is NaN, NaN equals nothing, so `!==` already refuses. That makes it exactly the property a well-meaning refactor deletes - adding the `Number.isNaN` guard that looks like it is handling the case turns an unreadable assertion into no assertion, and writing over a colleague on the strength of one is the worst available answer. The source comment warns against that guard; this is what stops somebody adding it anyway.",
    "file": "backend/src/firsthand/studies-repository.ts",
    "anchor": "        if (claimedUpdatedAt !== storedUpdatedAt) {",
    "mutation": "        if (!Number.isNaN(claimedUpdatedAt) && claimedUpdatedAt !== storedUpdatedAt) {",
    "runner": "vitest",
    "spec": "src/firsthand/studies-repository.test.ts",
    "test": "refuses a precondition it cannot parse rather than ignoring it"
  },
  {
    "id": "f1-updated-at-is-stamped-at-statement-time",
    "why": "`NOW()` is `transaction_timestamp()` - the time the BEGIN ran, not the time the UPDATE ran. Two writers who both begin, then queue on the same `FOR UPDATE`, stamp times in the order they STARTED rather than the order they committed, so the second save can move `updated_at` BACKWARDS. A precondition compared against a value that can go backwards is a precondition that lets a stale save win. `clock_timestamp()` is read when the statement runs, so it cannot.",
    "file": "backend/src/firsthand/studies-repository.ts",
    "anchor": "        updates.push(\"updated_at = clock_timestamp()\");",
    "mutation": "        updates.push(\"updated_at = NOW()\");",
    "runner": "vitest",
    "spec": "src/firsthand/studies-repository.test.ts",
    "test": "stamps the time when the statement runs, not when the transaction began"
  },
  {
    "id": "f1-precondition-is-read-inside-the-row-lock",
    "why": "The value the precondition is checked against rides along on the statement that TAKES the lock. Read in a round trip of its own, before or after, the row can be written between that read and the write - which is the exact race the precondition exists to close, so the whole mechanism becomes decorative while every test that only checks the 409 keeps passing.",
    "file": "backend/src/firsthand/studies-repository.ts",
    "anchor": "`SELECT owner_user_id, kind, updated_at FROM studies WHERE id = $1 FOR UPDATE`",
    "mutation": "`SELECT owner_user_id, kind FROM studies WHERE id = $1 FOR UPDATE`",
    "runner": "vitest",
    "spec": "src/firsthand/studies-repository.test.ts",
    "test": "reads the precondition off the statement that takes the row lock"
  },
  {
    "id": "results-read-close-listener-is-registered-before-the-wait",
    "why": "The shipped defect, and the worst one this middleware has had. Registered inside the `.then`, the listener only ever existed for requests already ADMITTED - a caller who closed the tab while QUEUED had had `close` emitted before it attached, so the abort never reached `Semaphore.acquire`, the dead waiter kept its place in the FIFO queue, and the permit was gone for the life of the process. MAX_CONCURRENT_RESULTS_READS of those shuts all four results routes: 503 forever on a single-replica pod, for every admin including the superadmin, reachable by a tab close. The test that was supposed to cover it aborted a request that had already been admitted, which is the case that always worked. ANCHOR RE-POINTED by the #9 branch, and not because it had gone stale on main: the `release` -> `releaseGate`/`releaseHeld` rename in that branch broke it, the harness reported ANCHOR_MISSING, and it was repaired in the same branch. Main's own copy always located to exactly 1, so there was never an unguarded window on main to go hunting for.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "  res.on('close', onClose);\n\n  gate.acquire(RESULTS_READ_QUEUE_TIMEOUT_MS, tooManyResultsReads, abandoned.signal).then(\n    () => {\n      releaseGate = releaseOnce(gate);",
    "mutation": "  gate.acquire(RESULTS_READ_QUEUE_TIMEOUT_MS, tooManyResultsReads, abandoned.signal).then(\n    () => {\n      res.on('close', onClose);\n      releaseGate = releaseOnce(gate);",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "leaves the queue when it hangs up while still waiting"
  },
  {
    "id": "results-read-consults-the-close-that-raced-the-grant",
    "why": "`res.destroyed` and `res.writableEnded` are not yet true for a response whose `close` has fired but whose teardown has not finished, so the `closed` flag is the only one of the three that sees a close landing between the grant and the handler. Without it the permit is handed to a handler nobody is reading and comes back only when that handler finishes - on these four routes, up to a 120-second query later.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "      if (closed || res.destroyed || res.writableEnded) {",
    "mutation": "      if (res.destroyed || res.writableEnded) {",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "gives the permit back when the close lands between the grant and the handler"
  },
  {
    "id": "results-read-queue-timeout-is-pinned-as-a-literal",
    "why": "Matched deliberately to the runtime pool's own admission budget so the two ceilings a results read passes through are the same size. Every other test in the file derives its waiting from the constant, so a widened queue timeout - one long enough that a refused caller waits past every ingress bound - passes all of them.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "export const RESULTS_READ_QUEUE_TIMEOUT_MS = 10_000;",
    "mutation": "export const RESULTS_READ_QUEUE_TIMEOUT_MS = 60_000;",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "holds the queue timeout at the number that was decided"
  },
  {
    "id": "participant-reservation-is-pinned-as-a-literal",
    "why": "The participant guarantee IS this number: how many of the pool's five connections admin work may never occupy. ADMIN_CONCURRENCY_LIMIT is derived from it, so lowering it silently WIDENS the admin cap and eats the reservation - and every other test in the file sizes its loops from the constant, so they all keep passing at a reservation of one, or of none.",
    "file": "backend/src/firsthand/runtime-pool-admission.ts",
    "anchor": "export const PARTICIPANT_RESERVED_CONNECTIONS = 3;",
    "mutation": "export const PARTICIPANT_RESERVED_CONNECTIONS = 1;",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-pool-admission.test.ts",
    "test": "holds the connection split to the numbers that were decided"
  },
  {
    "id": "runtime-session-settings-are-not-transaction-local",
    "why": "`SET LOCAL` applies to the surrounding transaction, and these queries mostly run with no explicit transaction at all. Outside one, Postgres ACCEPTS it, emits `WARNING: SET LOCAL can only be used in transaction blocks`, and applies nothing - so every checkout would carry whatever timeout the last borrower left behind, while looking exactly like a working bound. The guarding assertion is an ABSENCE (`no statement matches /SET\\s+LOCAL/i`), which passes just as well when the statement list is empty; this entry is its control.",
    "file": "backend/src/firsthand/runtime-database.ts",
    "anchor": "      `SET statement_timeout TO ${statementTimeoutOf(statementTimeoutMs)}`",
    "mutation": "      `SET LOCAL statement_timeout TO ${statementTimeoutOf(statementTimeoutMs)}`",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-pool-starvation.test.ts",
    "test": "never emits SET LOCAL, which outside a transaction sets nothing at all"
  },
  {
    "id": "statement-timeout-floor-refuses-to-disable-itself",
    "why": "`SET statement_timeout TO 0` DISABLES the timeout - the one value that turns the bound into no bound - and a fractional or negative one is a syntax error that fails every checkout. The floor of 1 is what stops a caller-supplied number reaching Postgres as either. Nothing else in the module re-checks it.",
    "file": "backend/src/firsthand/runtime-database.ts",
    "anchor": "  return Math.max(1, Math.round(statementTimeoutMs));",
    "mutation": "  return Math.round(statementTimeoutMs);",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-pool-starvation.test.ts",
    "test": "refuses to disable the timeout, whatever it is handed"
  },
  {
    "id": "results-statement-bound-is-pinned-as-a-literal",
    "why": "120 seconds is how long one results read may hold both the only results-read permit AND one of the two admin permits. Every other assertion in the file reads the constant back, so dropping it to 1s survived the entire suite once already - and in production would have cancelled every real export as a 500.",
    "file": "backend/src/firsthand/runtime-database.ts",
    "anchor": "export const RESULTS_STATEMENT_TIMEOUT_MS = 120_000;",
    "mutation": "export const RESULTS_STATEMENT_TIMEOUT_MS = 240_000;",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-pool-starvation.test.ts",
    "test": "holds both statement bounds at the values that were decided"
  },
  {
    "id": "detached-answer-takes-both-columns-from-one-row",
    "why": "THE SHIPPED DEFECT, and it needs a real database to see at all. Migration 0015 carries `CHECK ((study_id IS NULL) = (step_id IS NULL))`, so binding the session's own study_id straight through while letting step_id fall out of a subquery that resolves to NULL produces a half-detached row Postgres refuses - aborting the transaction around the WHOLE session write, so a participant answering a question the researcher removed a moment ago loses every answer they have given. No concurrency required. Every string assertion in the mocked sibling file passed throughout, because a mocked pg cannot raise a constraint.",
    "file": "backend/src/firsthand/runtime-repository-postgres.ts",
    "anchor": "          (SELECT study_id FROM attached_step),",
    "mutation": "          $3,",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-response-attachment-postgres.test.ts",
    "test": "keeps a participant's save working when they answer a question the researcher has just removed",
    "needsDatabase": true
  },
  {
    "id": "a-save-does-not-delete-detached-answers",
    "why": "A save DELETEs the session's responses and re-INSERTs them. Without `step_id IS NOT NULL` on that delete, answering any later question destroys every answer whose question has since been removed - silently and permanently, when ON DELETE SET NULL kept them precisely so they could still be reported. The paired filter on the load side is one decision with this one and the two have to agree.",
    "file": "backend/src/firsthand/runtime-repository-postgres.ts",
    "anchor": "    \"DELETE FROM participant_responses WHERE session_id = $1 AND step_id IS NOT NULL\",",
    "mutation": "    \"DELETE FROM participant_responses WHERE session_id = $1\",",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-response-attachment-postgres.test.ts",
    "test": "does not erase a detached answer on the next ordinary save",
    "needsDatabase": true
  },
  {
    "id": "the-attachment-lookup-is-scoped-to-this-study",
    "why": "`study_steps` is keyed on the PAIR (study_id, id), so the same step id can legitimately exist in two studies and this CTE is what decides which one an answer is filed against. Scoped to `ss.id` alone it resolves the OTHER study's row, and the composite foreign key raises nothing because the pair it was handed is real - one participant's answer simply appears in a different researcher's results, correctly attached, under their question. Nothing evaluated this predicate until the test named here was written for it: every existing case used a step id that exists in exactly one study, where scoped and unscoped agree. THE `$3::text IS NOT NULL` IS NOT DECORATION AND MUST NOT BE TIDIED AWAY. `$3` is referenced ONLY in this predicate, so dropping it outright orphans the bind parameter and Postgres answers `42P18 could not determine data type of parameter $3` at RUNTIME - which fails the named test without ever evaluating the scope, and the harness reports that as KILLED because the suite loaded fine. That is a false kill, it was the shipped state of this entry, and a review gate caught it. Keeping `$3` referenced makes the mutation type-valid so the query actually runs and the test fails on the row it gets back: `study_id: study_other, step_id: study_survey_beta` where both should be null.",
    "file": "backend/src/firsthand/runtime-repository-postgres.ts",
    "anchor": "           WHERE ss.study_id = $3 AND ss.id = $4",
    "mutation": "           WHERE ss.id = $4 AND $3::text IS NOT NULL",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-response-attachment-postgres.test.ts",
    "test": "files an answer as detached rather than adopting a same-named step from another study",
    "needsDatabase": true
  },
  {
    "id": "results-route-is-on-the-tight-bucket-not-the-read-one",
    "why": "A route can carry a limiter and still carry the WRONG one. Mounting the study-wide results read on the 60-a-minute read bucket is structurally correct and only semantically wrong, so before the table existed a 200,001-row route on the cheap bucket passed all 82 tests. Sixty of these in flight together is an OOM of a single-replica 2Gi pod, which drops every live participant session and not just the caller's.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "router.get('/studies/:studyId/results', requireAdmin, studyResultsLimiter, boundResultsRead,",
    "mutation": "router.get('/studies/:studyId/results', requireAdmin, studyReadLimiter, boundResultsRead,",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "mounts the RIGHT limiter on every route, after the auth gate"
  },
  {
    "id": "a-limiter-sits-after-the-auth-gate-not-before-it",
    "why": "Mounted ahead of `requireAdmin` a limiter spends a bucket on requests that never reach a handler, and every one of them keys to the same `'unauthenticated'` fallback - one shared bucket that any anonymous caller could exhaust to refuse every admin in the estate. The ordering is the control; a test that only asks whether a limiter is present cannot see it reversed.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "router.get('/studies', requireAdmin, studyReadLimiter,",
    "mutation": "router.get('/studies', studyReadLimiter, requireAdmin,",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "mounts the RIGHT limiter on every route, after the auth gate"
  },
  {
    "id": "the-study-read-ceiling-is-sixty-a-minute",
    "why": "Widened, the limiter stops limiting while every test that asserts only a 429 keeps passing - and the failure mode is not a red test, it is participants queueing on a five-connection pool behind a burst of admin reads. The guarding test asserts BOTH halves, the 61st refused AND the 60th not, so it can see the ceiling move in either direction.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "export const studyReadLimiter = perUserLimiter(\n  60,",
    "mutation": "export const studyReadLimiter = perUserLimiter(\n  600,",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "refuses a 61st read in a minute, and not the 60th"
  },
  {
    "id": "the-study-results-ceiling-is-ten-not-sixty",
    "why": "Ten, and deliberately NOT the 60 its per-opportunity twin uses: `listResponsesForStudy` reads every opportunity that ever used the study, a strict superset multiplied by the reuse the study picker actively encourages, and the same number would price two different orders of magnitude alike. Sixty was the first instinct and it is the exact mistake this pins against.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "export const studyResultsLimiter = perUserLimiter(\n  10,",
    "mutation": "export const studyResultsLimiter = perUserLimiter(\n  60,",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "limits the study-wide results read far tighter than its per-opportunity twin"
  },
  {
    "id": "the-429-arrives-in-the-envelope-the-frontend-reads",
    "why": "Every other 429 assertion in this repository checks `.status` and none reads `.body`. Hand express-rate-limit a bare string instead of `{ error: message }` and it replies `text/html`, `data.error` is undefined, and the frontend's `extractSaveError` falls through to \"Request failed with status code 429\" - so the three carefully worded sentences these limiters send would be the one thing with no test, and the author would see a stack-trace string instead.",
    "file": "backend/src/middleware/per-user-rate-limit.ts",
    "anchor": "    message: { error: message }",
    "mutation": "    message: message",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "refuses in the envelope the frontend reads, not just with a status"
  },
  {
    "id": "the-limiter-bucket-is-keyed-on-the-user-not-the-ingress",
    "why": "Behind two proxy hops `trust proxy: 1` resolves `req.ip` to the INGRESS, so an IP-keyed bucket is shared by every admin in the estate and one author reloading a form would 429 everybody else. That is precisely why the anonymous limiters here sit at 600, and why an authenticated route can afford a tight ceiling at all - the whole justification for these numbers rests on this one line.",
    "file": "backend/src/middleware/per-user-rate-limit.ts",
    "anchor": "    keyGenerator: (req: Request) => req.user?.id ?? 'unauthenticated',",
    "mutation": "    keyGenerator: (req: Request) => req.ip ?? 'unauthenticated',",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand.test.ts",
    "test": "keys the bucket on the user, not the ingress"
  },
  {
    "id": "f1-precondition-keeps-millisecond-precision",
    "why": "Putting both sides through the same truncation is NECESSARY AND NOT SUFFICIENT, and the entry above only pins the first half. Flooring both to the second is equally symmetric, satisfies every other assertion in the file, and makes two saves landing in the same wall-clock second both match their precondition - so the second silently overwrites the first, which is the lost update the whole mechanism exists to prevent. An autosave is well capable of two writes inside one second. A review gate measured this exact mutation passing 81 of 81 tests before the named test was written for it.",
    "file": "backend/src/firsthand/studies-repository.ts",
    "anchor": "        const storedUpdatedAt = new Date(ownerRow.updated_at).getTime();\n        const claimedUpdatedAt = new Date(expectedUpdatedAt).getTime();",
    "mutation": "        const storedUpdatedAt = Math.floor(new Date(ownerRow.updated_at).getTime() / 1000);\n        const claimedUpdatedAt = Math.floor(new Date(expectedUpdatedAt).getTime() / 1000);",
    "runner": "vitest",
    "spec": "src/firsthand/studies-repository.test.ts",
    "test": "refuses a claim that differs from the stored value by less than a second"
  },
  {
    "id": "opportunity-bookings-consults-ownership",
    "why": "GET /api/bookings/opportunities/:id/bookings returns every participant's NAME, EMAIL, BUSINESS UNIT and ROLE TITLE. Neutering its owner check to `true` passed 844 of 844 jest tests on e717537 - measured, one gate at a time. #79 widened the gate to owner-OR-superadmin (the Participants tab feeds from this roster), so the mutation now opens the roster to every researcher_admin rather than to everybody; the named test still fails by name because its caller is a non-owner researcher_admin. Re-proven KILLED after the widening.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    const isOwner = isOpportunityOwner(opportunityCheck.rows[0], req.user);",
    "mutation": "    const isOwner = true;",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.opportunity-bookings-ownership.test.ts",
    "test": "never runs the identity query for a non-owner"
  },
  {
    "id": "approve-consults-opportunity-ownership",
    "why": "Approving writes `admin_notes` - a researcher's written judgement of a named colleague - and awards that colleague points. Short-circuiting the owner check to false passed 844 of 844. The named test carries BOTH a status assertion and a no-UPDATE assertion; the status one is what fires first under this mutation, and the no-UPDATE arm is what makes the test worth naming here rather than the status alone - a 403 raised for some other reason would satisfy the status by itself.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    if (!isSuperadmin && !isOpportunityOwner(booking, req.user)) {\n      await client.query('ROLLBACK');\n      throw new ForbiddenError('You can only approve sessions for your own studies');",
    "mutation": "    if (false && !isSuperadmin && !isOpportunityOwner(booking, req.user)) {\n      await client.query('ROLLBACK');\n      throw new ForbiddenError('You can only approve sessions for your own studies');",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.approve-reject-ownership.test.ts",
    "test": "writes nothing when a non-owner tries to approve, and rolls back"
  },
  {
    "id": "reject-consults-opportunity-ownership",
    "why": "The SECOND of two near-identical handlers 80 lines apart, and that is the whole reason it gets its own entry: a fix applied to approve reads as a fix applied to both, and a canary entry naming only one would carry that same blind spot into the thing meant to detect it. Neutering it passed 844 of 844 independently of approve.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    if (!isSuperadmin && !isOpportunityOwner(booking, req.user)) {\n      await client.query('ROLLBACK');\n      throw new ForbiddenError('You can only reject sessions for your own studies');",
    "mutation": "    if (false && !isSuperadmin && !isOpportunityOwner(booking, req.user)) {\n      await client.query('ROLLBACK');\n      throw new ForbiddenError('You can only reject sessions for your own studies');",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.approve-reject-ownership.test.ts",
    "test": "writes nothing when a non-owner tries to reject, and rolls back"
  },
  {
    "id": "admin-dashboard-scopes-participant-identities",
    "why": "One `filterOwnerId` used to govern five queries in this handler and the fifth returns participant names and emails. Setting it to the superadmin value published every other researcher's participants past 844 of 844 tests. The constant is now split in two so the counts cannot carry the identity read with them; this entry pins the identity half.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  const participantIdentityOwnerId = user.role === 'superadmin' ? null : userId;",
    "mutation": "  const participantIdentityOwnerId = null;",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.participant-identity-scope.test.ts",
    "test": "binds the caller to the participant-identity read"
  },
  {
    "id": "admin-bookings-export-scopes-participant-identities",
    "why": "The SECOND owner filter in admin.ts, in a separate handler, and every row of the CSV it guards carries a participant's name and email. Fixing the dashboard's audits half the file, so both are pinned. Widening this one passed 844 of 844.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  const participantIdentityOwnerId = user.role === 'superadmin' ? null : user.id;",
    "mutation": "  const participantIdentityOwnerId = null;",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.participant-identity-scope.test.ts",
    "test": "binds the caller to the export query"
  },
  {
    "id": "opportunity-ownership-helper-fails-closed",
    "why": "THE SINGLE POINT WHERE ONE EDIT FAILS OPEN AT TWENTY-FIVE GATES AT ONCE. Every owner-vs-caller comparison in the routes now runs through this predicate, so making it treat an ABSENT owner as a match hands every admin the participant data of any opportunity whose owner has been removed - on all twenty-five sites, in one line, with no route file touched. The route-level entry beside this one cannot see it: that anchor pins how ONE handler composes the answer, not what the answer means.",
    "file": "backend/src/utils/opportunityOwnership.ts",
    "anchor": "  return Boolean(ownerId && ownerId === callerId);",
    "mutation": "  return ownerId == null || ownerId === callerId;",
    "runner": "jest",
    "spec": "src/utils/__tests__/opportunityOwnership.test.ts",
    "test": "refuses an ownerless row and an id-less caller, which a bare === pairs"
  },
  {
    "id": "leaderboard-limit-ceiling-is-a-hundred",
    "why": "Both leaderboard routes are UNAUTHENTICATED and fed `limit` straight into a SQL `LIMIT $1`, so `?limit=1000000` was one request from anyone on the network for every participant's name and participation volume. The ceiling is a policy number and the test asserts the literal rather than deriving it from the constant - three mutations survived a review in this repository by deriving exactly that way, including one that widened a concurrency cap.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "export const MAX_LEADERBOARD_LIMIT = 100;",
    "mutation": "export const MAX_LEADERBOARD_LIMIT = 500;",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.leaderboard-limit.test.ts",
    "test": "holds the ceiling at the number that was decided"
  },
  {
    "id": "leaderboard-monthly-route-is-capped-too",
    "why": "TWO ROUTES FOURTEEN LINES APART, near-identical, which is the shape where a fix applied to one reads as a fix applied to both - the same trap approve and reject set in bookings.ts. This entry reverts ONLY the monthly route to the old uncapped `parseInt(...) || 10`, so an entry naming the pair generally cannot stand in for it.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "    const limit = leaderboardLimit(req.query.limit);\n    const leaderboard = await getMonthlyLeaderboard(limit);",
    "mutation": "    const limit = parseInt(req.query.limit as string) || 10;\n    const leaderboard = await getMonthlyLeaderboard(limit);",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.leaderboard-limit.test.ts",
    "test": "caps the monthly limit at the number that was decided"
  },
  {
    "id": "points-history-limit-ceiling-is-a-hundred",
    "why": "The ceiling on a page of the caller's own points history, and a policy number rather than a derived one. The test asserts the LITERAL 100 rather than reading MAX_POINTS_HISTORY_LIMIT - three mutations survived a review in this repository by deriving their expectation from the constant they were meant to be guarding, including one that widened a concurrency cap.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "export const MAX_POINTS_HISTORY_LIMIT = 100;",
    "mutation": "export const MAX_POINTS_HISTORY_LIMIT = 500;",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.points-history-limit.test.ts",
    "test": "holds the ceiling at the number that was decided"
  },
  {
    "id": "points-history-route-is-capped-too",
    "why": "THE THIRD ROUTE, and the reason this entry exists rather than a general one. cto/AdaptaLabs#17 capped the two leaderboard routes and left this one on a bare `parseInt(req.query.limit as string) || 20` feeding SQL `LIMIT $2` - the finding named an INSTANCE and the class was the file. It is not the leaderboard leak: the value is bound and rows are scoped to the caller's own user_id. What remained is an unbounded read at `?limit=999999999`, and a 500 at `?limit=-5`, because parseInt('-5') is truthy so the fallback never fired and Postgres raises SQLSTATE 2201W, verified against a real Postgres 17. cto/AdaptaLabs#24 then made the ceiling a VISIBLE wall: an over-large limit is refused with a 400 rather than silently clamped. The mutation restores the pre-fix bare `parseInt` AND drops the refuse-guard (so it also type-checks against the new `number | null` return); the route then serves 999999999 rows at 200 instead of a 400, which the named test catches.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "    const limit = pointsHistoryLimit(req.query.limit);\n    // null means the caller asked for more than the ceiling. Refuse visibly\n    // rather than clamp, so a short page is never mistaken for the end of\n    // history (cto/AdaptaLabs#24).\n    if (limit === null) {\n      return res.status(400).json({ error: `limit must not exceed ${MAX_POINTS_HISTORY_LIMIT}` });\n    }",
    "mutation": "    const limit = parseInt(req.query.limit as string) || 20;",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.points-history-limit.test.ts",
    "test": "refuses a limit above the ceiling instead of silently capping it"
  },
  {
    "id": "points-history-keeps-its-own-default",
    "why": "THE TEMPTING ONE-WORD FIX, which is why it is pinned separately from the two entries above. `leaderboardLimit` sits in the same file and clamps to the same ceiling of 100, so borrowing it here caps the route, refuses the negative, and satisfies every assertion about the ceiling - while silently halving the page, because the leaderboard default is 10 and this route's is 20. The parser is shared deliberately; the policy numbers are not, and only a test naming the default can tell the two apart. The mutation swaps in `leaderboardLimit` and drops the #24 refuse-guard (which would not type-check against `leaderboardLimit`'s `number` return, and is dead there anyway since it never returns null); a no-param request then fetches 10 rows instead of 20, which the named test catches.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "    const limit = pointsHistoryLimit(req.query.limit);\n    // null means the caller asked for more than the ceiling. Refuse visibly\n    // rather than clamp, so a short page is never mistaken for the end of\n    // history (cto/AdaptaLabs#24).\n    if (limit === null) {\n      return res.status(400).json({ error: `limit must not exceed ${MAX_POINTS_HISTORY_LIMIT}` });\n    }",
    "mutation": "    const limit = leaderboardLimit(req.query.limit);",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.points-history-limit.test.ts",
    "test": "falls back to the points-history default, which is not the leaderboard default"
  },
  {
    "id": "clicks-total-is-withheld-not-zeroed",
    "why": "A DECISION ABOUT WIRE SHAPE, not about a gate. `clicks_total` is OMITTED for a caller who may not see it, because a colleague's row showing `0` reads as `nobody clicked` rather than `not yours` - the withheld-versus-unknown distinction this repository already got right once on `answer_counts` and got wrong before that. Zeroing it instead is a one-word edit that leaks no data and silently publishes a falsehood, which no gate reading for authorisation would flag.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "        : undefined;\n\n      return {",
    "mutation": "        : 0;\n\n      return {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.clicks-total-scope.test.ts",
    "test": "withholds the key entirely from a non-admin, rather than sending a zero"
  },
  {
    "id": "feedback-reads-stay-open-to-every-admin",
    "why": "The DECISION on #15, pinned in the direction nobody expects: this route LOOKS like it should be tightened, because DELETE beside it is superadmin and the API client comment said `superadmin only`. Both misleading - Admin.tsx renders the Feedback tab for researcher_admins on purpose, so tightening removes a tab they are meant to see. A canary entry rather than only a test because the tightening edit is what a future reader will reach for.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "router.get('/', requireAdmin, asyncHandler",
    "mutation": "router.get('/', requireSuperadmin, asyncHandler",
    "runner": "jest",
    "spec": "src/routes/__tests__/feedback.open-to-all-admins.test.ts",
    "test": "lets a researcher_admin list feedback"
  },
  {
    "id": "cancel-consults-opportunity-ownership",
    "why": "POST /api/bookings/:id/cancel is a WRITE, and its owner term was held by no test: `(isAdmin && isOpportunityOwner(booking, req.user))` neutered to `(isAdmin && true)` passed 919 of 919 jest tests on 6504612. What that admits is any researcher_admin cancelling a colleague's participant out of a session they have nothing to do with - the slot is lost, `booked_count` is decremented, the participant is emailed a cancellation naming the caller, and the real owner is emailed a participant-cancelled notice naming the participant. (This sentence used to say both were emailed a cancellation naming the caller. They are different templates: only `getBookingCancellationTemplate` has a `cancelledBy` parameter, so the owner is never told who cancelled.) No undo, no audit row. The named test carries BOTH the 403 and a no-write assertion, because a 403 raised for some other reason satisfies the status on its own; its control arm in the same file proves the same fixture DOES produce one `UPDATE bookings` for an entitled caller.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "                   (isAdmin && isOpportunityOwner(booking, req.user));",
    "mutation": "                   (isAdmin && true);",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-ownership.test.ts",
    "test": "writes nothing and opens no transaction for a non-owning admin"
  },
  {
    "id": "opportunity-add-sessions-consults-ownership",
    "why": "POST /api/opportunities/:id/sessions lets one researcher_admin add slots to another's recruitment schedule. `const isOwner = true;` passed 919 of 919 on 6504612. THE ANCHOR IS FOUR LINES AND HAS TO BE: the `const isOwner = ...` line alone appears twice in this file at this indentation - here and on the DELETE handler a hundred lines below - so a one-line anchor is ANCHOR_AMBIGUOUS, and a version of it that was not would have mutated whichever came first. The 403 wording is what separates them.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "    const isOwner = isOpportunityOwner(opportunityCheck.rows[0], req.user);\n    const isSuperadmin = req.user!.role === 'superadmin';\n    if (!isSuperadmin && !isOwner) {\n      return res.status(403).json({ error: 'Only the owner can add sessions to this study' });",
    "mutation": "    const isOwner = true;\n    const isSuperadmin = req.user!.role === 'superadmin';\n    if (!isSuperadmin && !isOwner) {\n      return res.status(403).json({ error: 'Only the owner can add sessions to this study' });",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "test": "writes nothing at all for a non-owning admin on POST /api/opportunities/:id/sessions"
  },
  {
    "id": "opportunity-delete-sessions-consults-ownership",
    "why": "The SECOND of two near-identical handlers in one file, and the worse of the pair: DELETE /api/opportunities/:id/sessions removes every session under an opportunity AND every booking attached to them. Its only rail is a probe for bookings in `status = 'booked'`, so anything cancelled or pending goes without a word. Neutering it passed 919 of 919 independently of the POST beside it, which is exactly why it gets its own entry - a fix applied to one of a near-identical pair reads as a fix applied to both, and an entry naming only one would carry that same blind spot into the thing meant to detect it.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "    const isOwner = isOpportunityOwner(opportunityCheck.rows[0], req.user);\n    const isSuperadmin = req.user!.role === 'superadmin';\n    if (!isSuperadmin && !isOwner) {\n      return res.status(403).json({ error: 'Only the owner can delete sessions from this study' });",
    "mutation": "    const isOwner = true;\n    const isSuperadmin = req.user!.role === 'superadmin';\n    if (!isSuperadmin && !isOwner) {\n      return res.status(403).json({ error: 'Only the owner can delete sessions from this study' });",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "test": "writes nothing at all for a non-owning admin on DELETE /api/opportunities/:id/sessions"
  },
  {
    "id": "sessions-create-consults-opportunity-ownership",
    "why": "POST /api/sessions is the THIRD handler with this predicate and the same 403 wording, in a different router from the other two. Same neutering, same result: 919 of 919. It is pinned separately because a reader fixing `opportunities.ts` has no reason to open `sessions.ts`, and the shared wording is what makes the duplicate easy to miss. Unlike POST /api/bookings/:id/cancel, this family DOES carry a superadmin bypass; the spec pins that in both directions so the asymmetry between the two families can only change on purpose.",
    "file": "backend/src/routes/sessions.ts",
    "anchor": "  const isOwner = isOpportunityOwner(opportunityCheck.rows[0], req.user);\n  const isSuperadmin = req.user!.role === 'superadmin';\n  if (!isSuperadmin && !isOwner) {\n    throw new ForbiddenError('Only the owner can add sessions to this study');",
    "mutation": "  const isOwner = true;\n  const isSuperadmin = req.user!.role === 'superadmin';\n  if (!isSuperadmin && !isOwner) {\n    throw new ForbiddenError('Only the owner can add sessions to this study');",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "test": "writes nothing at all for a non-owning admin on POST /api/sessions"
  },
  {
    "id": "session-patch-column-allowlist-holds",
    "why": "PATCH /api/sessions/:id built its SET clause from request-body KEYS - `updateFields.push(`${key} = $${paramCount}`)` interpolated the key and parameterised only the value. Nothing upstream refused an unexpected key: the `const data: UpdateSessionRequest = req.body` annotation erases at runtime, and validateSessionData is positive-only. A key carrying `= (SELECT email FROM users LIMIT 1), capacity` produced valid Postgres and RETURNING * handed the result to the caller, so any researcher_admin with one session they owned could read users and bookings. The same hole allowed `opportunity_id` to be reassigned, moving a session and its bookings under an opportunity the caller did not own. This entry mutates the allow-list to accept everything, which is the exact pre-fix behaviour.",
    "file": "backend/src/routes/sessions.ts",
    "anchor": "  const unknownFields = Object.keys(data ?? {}).filter(\n    (key) => !UPDATABLE_SESSION_COLUMNS.has(key)\n  );",
    "mutation": "  const unknownFields = Object.keys(data ?? {}).filter(() => false);",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.patch-column-allowlist.test.ts",
    "test": "refuses a body key carrying a subquery, and builds no SQL for it"
  },
  {
    "id": "opportunity-patch-column-allowlist-holds",
    "why": "PATCH /api/opportunities/:id has the same raw-key interpolation PATCH /api/sessions/:id had - `updateFields.push(`${key} = $${paramCount}`)` puts the request body's KEY into the SET clause and binds only the value. It was never exploitable for one reason: validateRequest does `req.body = schema.parse(req.body)` and UpdateOpportunitySchema is a bare z.object, which strips unknown keys. A security gate measured what that is worth - changing that one word to `.passthrough()` passed 958 of 958 tests AND reopened the injection, driving the real handler and capturing `UPDATE opportunities SET title = (SELECT email FROM users ORDER BY created_at LIMIT 1), ...` back through RETURNING *. Relying on a parser's default mode is the same mistake as relying on the type annotation that hid the sessions hole. This entry mutates the allow-list to accept everything, which is the pre-fix behaviour; the spec drives the real handler with the stripping neutralised, which is the state the fix exists to survive.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "  const unknownFields = Object.keys(req.body ?? {}).filter(\n    (key) => !UPDATABLE_OPPORTUNITY_COLUMNS.has(key) && !NON_COLUMN_OPPORTUNITY_BODY_KEYS.has(key)\n  );",
    "mutation": "  const unknownFields = Object.keys(req.body ?? {}).filter(() => false);",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.patch-column-allowlist.test.ts",
    "test": "refuses a body key carrying a subquery, and builds no SQL for it"
  },
  {
    "id": "opportunity-patch-allowlist-refuses-a-mixed-body",
    "why": "A boundary allow-list that fires only when EVERY key is unknown is the shape a well-meaning \"do not 400 a mostly-valid save\" refactor produces, and it reopens the injection for the body the exploit actually has: one permitted key alongside one that is not. A review gate measured it - every refusal arm in both specs sent a body of EXACTLY ONE key, so this narrowing survived all 987 tests on BOTH routes and let `SET title = $1, purpose_one_liner = (SELECT email FROM users ...)` through at 200. Checking only the first key survived too. The mixed-body arm is what kills it. Paired with `opportunity-patch-column-allowlist-holds`, which mutates the filter rather than the condition - the two fail apart.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "  if (unknownFields.length > 0) {",
    "mutation": "  if (unknownFields.length > 0 && unknownFields.length === Object.keys(req.body ?? {}).length) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.patch-column-allowlist.test.ts",
    "test": "refuses a mixed body, and builds no SQL for the permitted key either"
  },
  {
    "id": "session-patch-allowlist-refuses-a-mixed-body",
    "why": "A boundary allow-list that fires only when EVERY key is unknown is the shape a well-meaning \"do not 400 a mostly-valid save\" refactor produces, and it reopens the injection for the body the exploit actually has: one permitted key alongside one that is not. A review gate measured it - every refusal arm in both specs sent a body of EXACTLY ONE key, so this narrowing survived all 987 tests on BOTH routes and let `SET title = $1, purpose_one_liner = (SELECT email FROM users ...)` through at 200. Checking only the first key survived too. The mixed-body arm is what kills it. Same entry on the sibling route, because the gap was measured on both and a fix to one is not a fix to the other.",
    "file": "backend/src/routes/sessions.ts",
    "anchor": "  if (unknownFields.length > 0) {",
    "mutation": "  if (unknownFields.length > 0 && unknownFields.length === Object.keys(data ?? {}).length) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.patch-column-allowlist.test.ts",
    "test": "refuses a mixed body, and builds no SQL for the permitted key either"
  },
  {
    "id": "cancel-emails-the-participant-not-the-caller",
    "why": "POST /api/bookings/:id/cancel loaded its booking with ONE join onto users, on o.owner_user_id. It never joined b.user_id, so the participant's identity was not in scope and req.user was the only identity the email block could reach: the cancellation notice went to whoever pressed the button. A participant cancelling their own booking looked correct and hid it. A researcher cancelling somebody out of a session meant the participant heard NOTHING - slot gone, calendar event deleted, first they knew of it was turning up - while the researcher received the participant's copy addressed to themselves. Three separate records described the behaviour the query could not perform. This entry sends the cancellation back to req.user, which is the pre-fix behaviour.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      const cancellationResult = await emailService.sendEmail(\n        { email: booking.participant_email, name: booking.participant_name },\n        cancellationTemplate\n      );",
    "mutation": "      const cancellationResult = await emailService.sendEmail(\n        { email: req.user!.email, name: req.user!.name },\n        cancellationTemplate\n      );",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-notifies-participant.test.ts",
    "test": "sends the cancellation to the participant and not to the caller"
  },
  {
    "id": "cancel-joins-the-participant-into-scope",
    "why": "The root cause of the cancellation notice going to the caller was a MISSING JOIN, not a wrong variable - so the join is pinned separately from the recipient. Deleting `JOIN users pu ON b.user_id = pu.id` puts participant_name and participant_email out of scope again and every message the handler sends is addressed to or names undefined. The named spec models the database's contract rather than hard-coding a fixture: its booking row returns those columns ONLY IF the statement actually asked for them, which is what lets a mocked pool see a missing join at all.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    JOIN users u ON o.owner_user_id = u.id\n    JOIN users pu ON b.user_id = pu.id",
    "mutation": "    JOIN users u ON o.owner_user_id = u.id",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-notifies-participant.test.ts",
    "test": "joins the participant and selects the columns the email block reads"
  },
  {
    "id": "cancel-owner-notice-goes-to-the-owner",
    "why": "The owner's copy of a cancellation was UNPINNED, and the test file said the opposite. Every arm asserting the admin-notice recipient ran as the OWNER, and the fixture's owner_name/owner_email were the caller's own values, so the assertion was an identity on its own fixture: redirecting the owner's copy to `req.user` at both call sites survived all 973 tests. That is the same req.user-versus-loaded-row confusion the participant fix exists to close, left alive on the sibling call. The named test is the only arm where the caller is NOT the owner - the participant cancelling their own booking - which is what makes it the only arm that can see the owner's recipient at all. Anchored on the PREFERRED path; the `catch (prefError)` fallback carries an identical send, so the anchor deliberately includes the surrounding comment to stay unique.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "          await emailService.sendEmail(\n            { email: booking.owner_email, name: booking.owner_name },\n            adminTemplate\n          );\n        }\n      } catch (prefError) {",
    "mutation": "          await emailService.sendEmail(\n            { email: req.user!.email, name: req.user!.name },\n            adminTemplate\n          );\n        }\n      } catch (prefError) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-notifies-participant.test.ts",
    "test": "sends the owner's copy to the owner, who here is not the caller"
  },
  {
    "id": "reschedule-names-the-researcher",
    "why": "POST /api/bookings/:id/reschedule passed `targetSession.owner_name` and `targetSession.owner_email` into getBookingConfirmationTemplate. NEITHER COLUMN EXISTS - targetSession is a SELECT over sessions joined to opportunities, and `owner_name` appears nowhere in db/migrate.ts - so both arguments were undefined at runtime, while the handler had already resolved the right values forty lines above and passed them only to the calendar attendee list. It degraded silently rather than rendering \"undefined\", because the template guards with `${ownerName ? ... : ''}` and generateICSFile guards organizerEmail: a participant who rescheduled got a confirmation with no researcher and a calendar file with no organiser. Found by a review gate reading the handlers BESIDE the cancellation fix; the same defect class, one handler down. Replacing both arguments with a literal `undefined` changed nothing across 973 tests before the spec existed.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "        targetOwnerName,\n        targetOwnerEmail\n      );",
    "mutation": "        targetSession.owner_name,\n        targetSession.owner_email\n      );",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-names-the-researcher.test.ts",
    "test": "puts the researcher on the confirmation, not undefined"
  },
  {
    "id": "display-width-declared-on-a-schema-again",
    "why": "`display_width` is retired: no surface consumes it, because the double-width home page layout was removed by 844bae8 while the schemas had never declared the key at all. The tempting half-fix is to declare it so the save persists. Measured against the real router: that makes PATCH answer 400 and emit no UPDATE, because the key now reaches the allow-list and UPDATABLE_OPPORTUNITY_COLUMNS does not name it, while POST drops it in silence because the create INSERT no longer mentions the column. So the half-fix breaks every save the authoring form makes rather than reviving anything, and it must fail by name.",
    "file": "backend/src/validation/schemas.ts",
    "anchor": "export const UpdateOpportunitySchema = z.object({\n  type: OpportunityTypeSchema.optional(),",
    "mutation": "export const UpdateOpportunitySchema = z.object({\n  display_width: z.enum(['single', 'double']).optional(),\n  type: OpportunityTypeSchema.optional(),",
    "runner": "jest",
    "spec": "src/validation/__tests__/display-width-retired.test.ts",
    "test": "UpdateOpportunitySchema drops display_width"
  },
  {
    "id": "cancel-cancels-one-booking-not-every-booking",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-writes-are-row-scoped.test.ts",
    "anchor": "SET status = 'cancelled', cancelled_at = NOW()\n      WHERE id = $1",
    "mutation": "SET status = 'cancelled', cancelled_at = NOW()\n      WHERE session_id = $1",
    "test": "cancels the booking by its own id, not by any other key",
    "why": "The cancel handler's `UPDATE bookings ... WHERE id = $1` was matched only by tests asking whether an `UPDATE bookings` statement existed. Swapping the key to `session_id` cancels every booking on the session rather than the one the participant asked about, and DELETING the WHERE entirely cancels EVERY BOOKING IN THE TABLE - measured on bedc2fe, that passed the whole backend suite, 1007 of 1007. Asserting the params array alone does not close it: a bound parameter stays bound whether the SQL uses it or not, and every one of these suites mocks `pool`, so an unscoped statement still 'passes the id'. The named test ties the column in the WHERE, the $n placeholder it is compared against, and the value bound at that position together, because any one of the three alone is satisfiable by a statement that hits the wrong rows."
  },
  {
    "id": "cancel-decrements-the-booked-session-only",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-writes-are-row-scoped.test.ts",
    "anchor": "      await client.query(`\n        UPDATE sessions\n        SET booked_count = GREATEST(booked_count - 1, 0)\n        WHERE id = $1",
    "mutation": "      await client.query(`\n        UPDATE sessions\n        SET booked_count = GREATEST(booked_count - 1, 0)\n        WHERE opportunity_id = $1",
    "test": "decrements the booked session, not every session of the opportunity",
    "why": "The capacity decrement is `UPDATE sessions SET booked_count = GREATEST(booked_count - 1, 0) WHERE id = $1`, bound to booking.session_id. Repointing it to `WHERE opportunity_id = $1` decrements EVERY session under the opportunity, freeing phantom capacity on every other session in the study when one person cancels a slot. booked_count is what the participant-facing places-left is computed from."
  },
  {
    "id": "cancel-decrement-keeps-its-floor",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-writes-are-row-scoped.test.ts",
    "anchor": "      await client.query(`\n        UPDATE sessions\n        SET booked_count = GREATEST(booked_count - 1, 0)\n        WHERE id = $1",
    "mutation": "      await client.query(`\n        UPDATE sessions\n        SET booked_count = booked_count - 1\n        WHERE id = $1",
    "test": "keeps the floor on the decrement, so a count cannot go negative",
    "why": "GREATEST(booked_count - 1, 0) is the defensive floor on the decrement. The double-cancel race that once made it load-bearing is now closed by the `WHERE id = $1 AND status = 'booked'` guard plus rowCount gate a few lines above, but the floor is retained: booked_count has a CHECK (booked_count >= 0) constraint, so an underflow from any future path would abort the transaction rather than silently corrupt. Dropping GREATEST passed the whole suite because the test asserted the statement was issued, not what arithmetic it performed."
  },
  {
    "id": "delete-sessions-deletes-only-this-opportunitys-bookings",
    "file": "backend/src/routes/opportunities.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.delete-sessions-is-scoped.test.ts",
    "anchor": "DELETE FROM bookings WHERE session_id IN (SELECT id FROM sessions WHERE opportunity_id = $1)",
    "mutation": "DELETE FROM bookings",
    "test": "deletes only the bookings belonging to this opportunity",
    "why": "`DELETE /api/opportunities/:id/sessions` clears bookings through `session_id IN (SELECT id FROM sessions WHERE opportunity_id = $1)`. Dropping that restriction EMPTIES THE BOOKINGS TABLE - every booking, every opportunity, every user - and it passed 1007 of 1007 on bedc2fe. This is the sharpest of the set because the route is reachable by any researcher_admin who owns one opportunity, and the damage is unrecoverable without a restore. The restricting column is not on the table being written, so the assertion has to follow the id into the subquery rather than look for it in a plain WHERE."
  },
  {
    "id": "delete-sessions-deletes-only-this-opportunitys-sessions",
    "file": "backend/src/routes/opportunities.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.delete-sessions-is-scoped.test.ts",
    "anchor": "DELETE FROM sessions WHERE opportunity_id = $1",
    "mutation": "DELETE FROM sessions",
    "test": "deletes only the sessions belonging to this opportunity",
    "why": "The companion to the bookings delete on the same route: dropping `WHERE opportunity_id = $1` empties the SESSIONS table for every opportunity in the database, and it too passed the whole suite. The file also carries `issues no unrestricted DELETE at all`, which is the arm that survives a future refactor moving these statements around - a per-statement assertion cannot fail for a statement nobody wrote a row for."
  },
  {
    "id": "sessions-patch-consults-ownership",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "anchor": "  const access = await checkSessionOwnership(sessionId, req.user!.id, req.user!.role);\n  enforceSessionOwnership(access, req.user!.role, 'Only the owner can edit this session');",
    "mutation": "  const access = await checkSessionOwnership(sessionId, req.user!.id, req.user!.role);\n  void access;",
    "test": "writes nothing at all for a non-owning admin on PATCH /api/sessions/:id",
    "why": "PATCH /api/sessions/:id is a WRITE and its ownership gate was held by no test: neutering `checkSessionOwnership` to `return true;` passed 1036 of 1036 on d3aa9c6. What it admits is one researcher_admin editing a session under a study they have nothing to do with - moving the time a participant already booked, or cutting capacity. The gate was invisible to the suite because the fixture answered nothing for its query shape: the predicate reaches the owner via `SELECT o.owner_user_id FROM sessions s JOIN opportunities o`, which does not match the `SELECT owner_user_id FROM opportunities` arm the older routes use, so it fell to a catch-all empty result and the handler denied by default. A fixture that answers nothing makes a deny-by-default gate look correct no matter what its predicate says. cto/AdaptaLabs#28 made the gate three-way (missing/forbidden/ok) routed through `enforceSessionOwnership`; the mutation drops the enforcement call (`void access;`) so the handler proceeds and writes for a non-owning admin. The anchor carries the refusal message because the call line is byte-identical to the DELETE handler's."
  },
  {
    "id": "sessions-delete-consults-ownership",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "anchor": "  const access = await checkSessionOwnership(sessionId, req.user!.id, req.user!.role);\n  enforceSessionOwnership(access, req.user!.role, 'Only the owner can delete this session');",
    "mutation": "  const access = await checkSessionOwnership(sessionId, req.user!.id, req.user!.role);\n  void access;",
    "test": "writes nothing at all for a non-owning admin on DELETE /api/sessions/:id",
    "why": "The companion to the PATCH gate on the same helper, and the worse of the two: an unauthorised DELETE removes the session outright. Its only rail is a `booked_count > 0` check, so a session whose bookings are all cancelled or pending goes silently. Same measurement - neutering the helper passed the whole suite. Since cto/AdaptaLabs#28 the gate is enforced through `enforceSessionOwnership`; the mutation drops that call (`void access;`) so the DELETE proceeds for a non-owning admin. Same anchor caveat: the call line is identical to PATCH's, so the refusal message disambiguates."
  },
  {
    "id": "session-ownership-predicate-is-consulted",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "anchor": "  return isOpportunityOwner(result.rows[0], { id: userId }) ? 'ok' : 'forbidden';",
    "mutation": "  return 'ok';",
    "test": "writes nothing at all for a non-owning admin on PATCH /api/sessions/:id",
    "why": "The shared predicate inside `checkSessionOwnership`, one line below the query that feeds it. Pinning the two call sites is not enough on its own: a gate that is consulted and always answers yes is indistinguishable from no gate, and this is the single line both PATCH and DELETE depend on. Since cto/AdaptaLabs#28 it returns a three-way result; mutating it to a bare `return 'ok';` grants every researcher_admin and reopens the same hole `return true;` did before the type changed. Killed by the non-owning-admin arms across both routes."
  },
  {
    "id": "session-superadmin-branch-checks-existence",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.write-ownership.test.ts",
    "anchor": "    const result = await pool.query('SELECT id FROM sessions WHERE id = $1', [sessionId]);\n    return result.rows.length > 0 ? 'ok' : 'missing';",
    "mutation": "    const result = await pool.query('SELECT id FROM sessions WHERE id = $1', [sessionId]);\n    return result.rows.length > 0 ? 'ok' : 'forbidden';",
    "test": "answers 404 to a superadmin for a target that does not exist, on PATCH /api/sessions/:id",
    "why": "The superadmin branch of `checkSessionOwnership` distinguishes a session that EXISTS from one that does not, so `enforceSessionOwnership` can give a superadmin the honest 404 rather than a 403. Every other refusal case in this family runs as a researcher_admin, so this branch is reachable only through the superadmin arm. cto/AdaptaLabs#28 took this from 403 to 404 (option B), following !215 (abea2c6): !215 collapsed the RESULTS routes onto 403 for a caller who may not read and deliberately KEPT the superadmin's accurate 404, pinned by `still tells a superadmin the truth about an id that is not there` - its message records that answering 403 to everybody \"also closes the oracle and passed all 295 tests while losing the truth for the one caller entitled to it\". A superadmin can enumerate every session anyway, so there is no oracle to close against them. The mutation returns 'forbidden' for an absent session, which makes `enforceSessionOwnership` answer 403 instead of 404; the superadmin arm expects 404 and fails. Note a bare `return 'ok';` would NOT be caught here - the downstream existence guards still 404 a missing session - which is why the mutation flips the branch to 'forbidden' rather than neutering it."
  },
  {
    "id": "email-html-escapes-the-angle-brackets",
    "file": "backend/src/services/email.ts",
    "runner": "jest",
    "spec": "src/services/__tests__/email-html-escaping.test.ts",
    "anchor": "      .replace(/</g, '&lt;')",
    "mutation": "      .replace(/</g, '<')",
    "test": "escapes a researcher-authored title in the confirmation a participant receives",
    "why": "The five email templates interpolated user-controlled strings straight into HTML with no escaping at all. `opportunityTitle` and `sessionLocation` are authored by a researcher_admin and land in a PARTICIPANT's inbox; `feedback`, `userName`, `userAgent` and `url` come from any authenticated user and land in an admin's. A title of `</h3><a href=\"https://evil.example\">Reschedule here</a>` rendered as markup inside a message carrying the platform's own From address - a better phishing surface than a page on the site, because the reader has already decided the sender is trustworthy. Most clients drop <script>; none drop <a> or <img>. Removing the angle-bracket replacement alone re-opens the whole class and fails 7 tests."
  },
  {
    "id": "email-html-escapes-attribute-quotes",
    "file": "backend/src/services/email.ts",
    "runner": "jest",
    "spec": "src/services/__tests__/email-html-escaping.test.ts",
    "anchor": "      .replace(/\"/g, '&quot;')",
    "mutation": "      .replace(/\"/g, '\"')",
    "test": "escapes a researcher-authored session location",
    "why": "The quote replacement is the half that matters for values landing inside an attribute - `href`, `download`, and any future `title` or `alt`. Without it a value can close the attribute and add its own, which is the attribute-injection path that does not need a single angle bracket. Kept as its own entry rather than folded into the angle-bracket one because the two fail different subsets and a fix for one reads as a fix for both."
  },
  {
    "id": "email-html-escaping-is-applied-not-just-defined",
    "file": "backend/src/services/email.ts",
    "runner": "jest",
    "spec": "src/services/__tests__/email-html-escaping.test.ts",
    "anchor": "<p style=\"white-space: pre-wrap;\">${this.e(feedback)}</p>",
    "mutation": "<p style=\"white-space: pre-wrap;\">${feedback}</p>",
    "test": "escapes every interpolation in an html body, or proves it safe",
    "why": "A correct escape helper that a template forgets to call is the same defect as no helper. This pins the SOURCE SCAN rather than one field: the named test reads the shipped file and requires every interpolation inside an `html` literal to be escaped or to match a small allow-list of provably-safe expressions (a number, an ISO timestamp, operator-set env config, a ternary over a literal union). A per-field assertion cannot fail for a field nobody wrote an assertion for, and these templates gain fields. THE FIRST VERSION OF THAT SCAN DID NOT DO THIS, and an earlier version of this sentence claimed it did. It judged each `${...}` whole, so a fragment containing one `this.e(` was read as fully escaped and one containing none was read as structural - which made `${notes ? `<p>Notes: ${notes}</p>` : ''}` invisible. That is the idiom email.ts already uses four times for `sessionLocation`, `ownerName` and `ownerEmail`, so it is the shape the next optional field gets copied from; a field added that way rendered a live `<a href>` into a participant's inbox and passed the whole backend suite, 1060 of 1060. The scan now DESCENDS into fragments and the bypass fails by name. Found by an independent security gate that was briefed to break the guard rather than read it."
  },
  {
    "id": "email-to-header-is-an-address-object-not-a-string",
    "why": "recipient.name is user-controlled and was interpolated into the to: header string, so a name of `Ada <attacker@evil.example>, Bob` split into a second SMTP envelope recipient and delivered booking details - carrying the platform's own From address - to an attacker-chosen address. The address-object form makes address the sole recipient and the name a display phrase that is never parsed for addresses. Reverting to the string form is the exact vulnerability.",
    "file": "backend/src/services/email.ts",
    "anchor": "to: { name: recipient.name, address: recipient.email },",
    "mutation": "to: `${recipient.name} <${recipient.email}>`,",
    "runner": "jest",
    "spec": "src/services/__tests__/email-recipient-injection.test.ts",
    "test": "a display name cannot smuggle a second recipient into the envelope"
  },
  {
    "id": "reschedule-moves-one-booking-not-every-booking",
    "why": "The reschedule move UPDATE bookings SET session_id=$1 WHERE id=$2 is keyed on the booking id. Swapping to WHERE session_id=$2 moves EVERY booking on the old session, not the one being rescheduled. Byte-different from cancel's UPDATE bookings (which sets status), so this anchor is unique on its own.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "SET session_id = $1\n      WHERE id = $2",
    "mutation": "SET session_id = $1\n      WHERE session_id = $2",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "test": "moves the booking by its own id, not by any other key"
  },
  {
    "id": "reschedule-decrements-the-old-session-only",
    "why": "The decrement is keyed on the old booking.session_id. WHERE id=$1 -> WHERE opportunity_id=$1 decrements every session of the opportunity, freeing phantom capacity on all of them. The decrement line is byte-identical to cancel's, so the anchor carries reschedule's own preceding comment to avoid matching both sites.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "// Update booked counts atomically (with safeguards)\n    await client.query(`\n      UPDATE sessions \n      SET booked_count = GREATEST(booked_count - 1, 0)\n      WHERE id = $1",
    "mutation": "// Update booked counts atomically (with safeguards)\n    await client.query(`\n      UPDATE sessions \n      SET booked_count = GREATEST(booked_count - 1, 0)\n      WHERE opportunity_id = $1",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "test": "decrements the OLD session, not every session of the opportunity"
  },
  {
    "id": "reschedule-decrement-keeps-its-floor",
    "why": "GREATEST(booked_count - 1, 0) -> booked_count - 1 loses the floor, so a race drives booked_count negative and the participant-facing 'N places left' is computed from it. Same anchor as the scope entry, carrying reschedule's own comment to distinguish it from cancel's identical line.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "// Update booked counts atomically (with safeguards)\n    await client.query(`\n      UPDATE sessions \n      SET booked_count = GREATEST(booked_count - 1, 0)\n      WHERE id = $1",
    "mutation": "// Update booked counts atomically (with safeguards)\n    await client.query(`\n      UPDATE sessions \n      SET booked_count = booked_count - 1\n      WHERE id = $1",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "test": "keeps the floor on the decrement, so a count cannot go negative"
  },
  {
    "id": "reschedule-increments-the-target-session-only",
    "why": "The increment is keyed on the target session id. WHERE id=$1 -> WHERE opportunity_id=$1 over-increments every session of the opportunity. The +1 line is not unique (the booking-create flow has one too), so the anchor includes the [target_session_id] bind line that only the reschedule increment carries.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "SET booked_count = booked_count + 1\n      WHERE id = $1\n    `, [target_session_id]);",
    "mutation": "SET booked_count = booked_count + 1\n      WHERE opportunity_id = $1\n    `, [target_session_id]);",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "test": "increments the TARGET session by its own id"
  },
  {
    "id": "cancel-decrements-only-the-booked-to-cancelled-transition",
    "why": "Two concurrent cancels of one booking both passed the pre-transaction status read and both reached an unconditional decrement, so a single cancellation subtracted TWO from booked_count. Proven on a real Postgres: a session at 2 booked dropped to 0, discarding a second participants slot and freeing phantom capacity to overbook past capacity. GREATEST(...,0) floors the number, it does not make it correct.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      WHERE id = $1 AND status = 'booked'",
    "mutation": "      WHERE id = $1",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-writes-are-row-scoped.test.ts",
    "test": "pins the transition guard on the cancel UPDATE"
  },
  {
    "id": "delete-sessions-locks-rows-so-the-booking-guard-is-not-a-toctou",
    "why": "DELETE /opportunities/:id/sessions checked has-bookings? then deleted, on separate pooled connections with no transaction. A booking created in the window between the check and the DELETE was silently deleted, defeating the guard that exists to protect it. Locking the sessions FOR UPDATE inside the transaction closes the race; removing it reopens it while every mocked assertion still passes.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "        'SELECT id FROM sessions WHERE opportunity_id = $1 ORDER BY id FOR UPDATE',",
    "mutation": "        'SELECT id FROM sessions WHERE opportunity_id = $1',",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.delete-sessions-is-scoped.test.ts",
    "test": "wraps the deletes in a single committed transaction"
  },
  {
    "id": "sync-booked-counts-is-superadmin-only",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.sync-booked-counts.test.ts",
    "anchor": "router.post('/sync-booked-counts', requireSuperadmin, asyncHandler(async (req: Request, res: Response) => {",
    "mutation": "router.post('/sync-booked-counts', requireAdmin, asyncHandler(async (req: Request, res: Response) => {",
    "test": "refuses a researcher_admin, and connects to nothing",
    "why": "POST /api/sessions/sync-booked-counts rewrites booked_count on EVERY session in the database. Under requireAdmin any researcher_admin could trigger writes to every other researcher's sessions - the one session write that was not owner-gated. cto/AdaptaLabs#26 made it superadmin-only, because the value is derived (an operator repair tool) rather than a per-researcher action. Reverting the gate to requireAdmin lets a researcher_admin through, which this test catches by asserting the 403 and that the pool is never borrowed."
  },
  {
    "id": "sync-booked-counts-locks-rows-against-a-racing-booking",
    "file": "backend/src/routes/sessions.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.sync-booked-counts.test.ts",
    "anchor": "    const locked = await client.query('SELECT id FROM sessions ORDER BY id FOR UPDATE');",
    "mutation": "    const locked = await client.query('SELECT id FROM sessions ORDER BY id');",
    "test": "locks the rows and rewrites them inside one committed transaction",
    "why": "The repair rewrites booked_count from a count of bookings; without locking the session rows first, a booking committed between the count and the write is clobbered - the sweep overwrites a correct increment with a stale count and CAUSES the drift it exists to fix (cto/AdaptaLabs#26). POST /api/bookings and cancel take FOR UPDATE on the same row, so locking here serialises against them. A mocked pool cannot race, so this pins the LOCK by shape: dropping FOR UPDATE is caught by the assertion that the locking SELECT was issued. The real guarantee is proven against a real Postgres."
  },
  {
    "id": "delete-sessions-locks-rows-in-id-order-against-deadlock",
    "file": "backend/src/routes/opportunities.ts",
    "runner": "vitest",
    "spec": "src/routes/__tests__/opportunities.delete-sessions-lock-order-postgres.test.ts",
    "anchor": "        'SELECT id FROM sessions WHERE opportunity_id = $1 ORDER BY id FOR UPDATE',",
    "mutation": "        'SELECT id FROM sessions WHERE opportunity_id = $1 FOR UPDATE',",
    "test": "locks the sessions in id order, so a transaction holding them in the other order cannot deadlock",
    "needsDatabase": true,
    "why": "delete-sessions and the sync-booked-counts sweep both lock many session rows; if they acquire shared rows in a different order they can deadlock (40P01). ORDER BY id makes both ascending so no cycle can form (cto/AdaptaLabs#34). This entry USED TO NAME the same jest test as delete-sessions-locks-rows-so-the-booking-guard-is-not-a-toctou, and that test cannot see the property: it asserts two independent substrings, one of them a dedicated ORDER BY id assertion, so ORDER BY id -> ORDER BY id DESC reintroduces the deadlock and STILL CONTAINS THE SUBSTRING. It passes the old jest test and fails the new Postgres test by name. That is the hole the new test closes (cto/AdaptaLabs#52). It now names a real-Postgres test that forces a lock cycle: measured interleaved, five rounds each, ordered 5/5 pass and this mutation 5/5 fail by name."
  },
  {
    "id": "reschedule-locks-the-old-session-nowait-against-deadlock",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "anchor": "      SELECT * FROM sessions WHERE id = $1 FOR UPDATE NOWAIT",
    "mutation": "      SELECT * FROM sessions WHERE id = $1 FOR UPDATE",
    "test": "locks the old session NOWAIT, so a reschedule never waits on a session lock",
    "why": "reschedule locks two session rows (target then current); the target is already FOR UPDATE NOWAIT. Making the current lock NOWAIT too means a reschedule never holds one session lock while WAITING on another, so it cannot be a party in a deadlock cycle with the multi-row lockers (sync-booked-counts, delete-sessions). cto/AdaptaLabs#34. Dropping NOWAIT reintroduces a blocking wait; the test asserts the current-session lock is FOR UPDATE NOWAIT and fails without it. A contended old session then fails fast as a retryable 409 via the existing 55P03 handler."
  },
  {
    "id": "cancel-decrements-the-current-session-not-the-stale-read",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cancel-writes-are-row-scoped.test.ts",
    "anchor": "        WHERE id = $1\n      `, [currentSessionId]);",
    "mutation": "        WHERE id = $1\n      `, [booking.session_id]);",
    "test": "decrements the session read back from the locked cancel UPDATE, not the pre-transaction read - cto/AdaptaLabs#31",
    "why": "Cancel decrements booked_count for the session the booking holds. It bound that decrement to booking.session_id from an UNLOCKED pre-transaction read (~line 353), so a reschedule committing in the window before the transaction moved the booking S_old -> S_new (status stays booked, so the conditional UPDATE still matches rowCount 1) while cancel decremented the stale S_old: S_old under-counted, S_new left a phantom slot until sync-booked-counts ran (cto/AdaptaLabs#31). The fix reads session_id back from the conditional UPDATE via RETURNING - that statement holds the booking row lock, so a racing reschedule blocks and the re-read yields the current session. Reverting the decrement param to booking.session_id restores the stale-read bug; it still type-checks (booking.session_id is a string), so the canary reports KILLED only because the named jest test drives distinct pre-read (S_old) and RETURNING (S_new) session ids and asserts the decrement is bound to S_new."
  },
  {
    "id": "reschedule-locks-the-booking-row-for-update",
    "file": "backend/src/routes/bookings.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.reschedule-writes-are-row-scoped.test.ts",
    "anchor": "      WHERE b.id = $1 AND b.user_id = $2 AND b.status = 'booked'\n      FOR UPDATE OF b",
    "mutation": "      WHERE b.id = $1 AND b.user_id = $2 AND b.status = 'booked'",
    "test": "locks the booking row FOR UPDATE OF b, serialising against a racing cancel - cto/AdaptaLabs#36",
    "why": "reschedule loads the booking with an unlocked SELECT (status='booked' filter) and its move UPDATE (UPDATE bookings SET session_id ... WHERE id) carries no status guard, so a cancel committing in the window after the read flips the row to cancelled while reschedule still moves it - moving a CANCELLED booking, decrementing the old session twice and leaving the target a phantom slot until sync-booked-counts runs (cto/AdaptaLabs#36). It also let the old-session decrement read a stale session_id when two reschedules of one booking raced. FOR UPDATE OF b locks the booking row (OF b, so the joined session/opportunity rows are NOT locked and the old-session FOR UPDATE NOWAIT from #34 still governs deadlock-freedom) - the loser of a race blocks, re-reads the committed row, and if status is no longer 'booked' the WHERE matches zero rows and the handler 404s. Dropping the clause restores the unlocked read; it still type-checks (it is inside a template literal), so the canary reports KILLED only because the named jest test asserts the booking load SQL contains FOR UPDATE OF b."
  },
  {
    "id": "admin-gate-rereads-live-role-from-db",
    "file": "backend/src/middleware/authenticate.ts",
    "runner": "jest",
    "spec": "src/middleware/__tests__/authenticate.test.ts",
    "anchor": "    return resolveEffectiveRole(result.rows[0].role as AdminRole, req.session!.user!.email);",
    "mutation": "    return req.session!.user!.role as AdminRole;",
    "test": "rejects a revoked admin whose database role was downgraded after login",
    "why": "requireAdmin and requireSuperadmin trusted req.session.user.role, stamped at login and never refreshed, so DELETE /api/admin/admins answered 'revoked successfully' while the revoked admin's cookie kept working for up to SESSION_MAX_AGE_MS = 24h (cto/AdaptaLabs#14). The fix re-reads the live role from the users table on every admin-gated request and rejects when it no longer qualifies - the message is true because the DB is the source of truth. Returning the session role instead of the queried row restores the stale-read; it still type-checks (both are the same union), so the canary reports KILLED only because the named jest test downgrades the DB role after login and asserts a 403."
  },
  {
    "id": "calendar-events-guards-calendar-id-ownership",
    "file": "backend/src/routes/calendar.ts",
    "runner": "jest",
    "spec": "src/routes/__tests__/calendar.events-ownership.test.ts",
    "anchor": "    return result.rows[0].calendar_id === calendarId;",
    "mutation": "    return true;",
    "test": "refuses an admin naming a calendar_id they do not own and never reaches the service",
    "why": "Neutering the ownership check to `return true` accepts any calendar_id from the query string under requireAdmin alone. calendarService.getCalendarEvents is a stub today but the same class makes real Google calls (events.insert); once the read stub is filled this hands any researcher_admin the attendee names+emails, titles and locations of any calendar id they name. The guard must exist before the stub - #18."
  },
  {
    "id": "cancel-admin-branch-reads-live-role",
    "why": "POST /api/bookings/:id/cancel admits the participant cancelling their own booking OR an admin who owns the opportunity, so it cannot sit on requireAdmin and #14 never reached it. Its inline isAdmin came from req.user.role, which requireAuth copies off the session stamped at login, so a revoked admin kept the admin-cancel branch - cancelling another participant out of a session, decrementing capacity and emailing them - for up to SESSION_MAX_AGE_MS = 24h (cto/AdaptaLabs#37). withLiveRole re-reads the role before the handler computes isAdmin. Reading the session role instead still type-checks - it is the same union, annotated to keep the null comparison legal - and restores exactly the stale read, so the canary reports KILLED only because the named jest test downgrades the DB role after login and asserts a 403.",
    "file": "backend/src/middleware/authenticate.ts",
    "anchor": "  const liveRole = await currentDbRole(req, res);",
    "mutation": "  const liveRole: AdminRole | null = req.session.user.role;",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.inline-admin-gates-read-live-role.test.ts",
    "test": "refuses the admin cancel branch to a downgraded admin"
  },
  {
    "id": "opportunities-list-limit-probe-is-detectable",
    "why": "Without the +1 the `>` comparison is unsatisfiable, so the 413 becomes unreachable and a catalogue of 1001 opportunities quietly returns the first 1000 as a JSON array that parses. Refusal turned into truncation, on an optionalAuth route with no cursor, no offset and no has_more - so the short list is indistinguishable from the end of the catalogue. cto/AdaptaLabs#22.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "LIMIT ${MAX_OPPORTUNITIES_RETURNED + 1}",
    "mutation": "LIMIT ${MAX_OPPORTUNITIES_RETURNED}",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.list-bound.test.ts",
    "test": "asks the database for one opportunity more than it will return"
  },
  {
    "id": "cron-secret-compares-digests-not-raw-bytes",
    "why": "timingSafeEqual THROWS on unequal-length buffers, so comparing the raw header against the raw expected value turns every wrong-length guess into a 500 while a right-length guess gets a 401 - a louder oracle than the timing side channel the fix removed. Hashing both sides to a fixed 32 bytes is what makes the comparison total; guarding it with a length check instead would leak the secret's length. cto/AdaptaLabs#22.",
    "file": "backend/src/routes/cron.ts",
    "anchor": "  return timingSafeEqual(digest(authHeader ?? ''), digest(`Bearer ${cronSecret}`));",
    "mutation": "  return timingSafeEqual(Buffer.from(authHeader ?? ''), Buffer.from(`Bearer ${cronSecret}`));",
    "runner": "jest",
    "spec": "src/routes/__tests__/cron.constant-time-secret.test.ts",
    "test": "refuses a bearer token far shorter than the secret without throwing"
  },
  {
    "id": "sessions-batch-ceiling-is-detectable",
    "why": "Widening the batch ceiling makes the refusal unreachable and restores the state the ticket found: POST /api/sessions runs one overlap query AND one INSERT per element, sequentially and outside a transaction, bounded only by express's inherited 100 kB body default. A ceiling a test derives from its own constant cannot see the constant move, so this moves it. cto/AdaptaLabs#22.",
    "file": "backend/src/routes/sessions.ts",
    "anchor": "  if (sessions.length > MAX_TIME_SLOTS_PER_REQUEST) {",
    "mutation": "  if (sessions.length > MAX_TIME_SLOTS_PER_REQUEST * 1000) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/time-slot-batch-bound.test.ts",
    "test": "refuses a sessions array over the batch ceiling"
  },
  {
    "id": "legacy-duration-header-rejects-negatives",
    "why": "Number.isFinite alone admits a negative, which is what let -1e9 reach a DOUBLE PRECISION column through the streaming upload while the S3 path next to it said z.number().nonnegative on the same quantity. The two upload paths disagreeing about what a duration is is the whole defect, and this line is the only place the older one decides. cto/AdaptaLabs#22.",
    "file": "backend/src/routes/firsthand-session.ts",
    "anchor": "  if (!Number.isFinite(parsed) || parsed < 0) return null;",
    "mutation": "  if (!Number.isFinite(parsed)) return null;",
    "runner": "jest",
    "spec": "src/routes/__tests__/firsthand-session.test.ts",
    "test": "keeps the recording but drops a negative duration"
  },
  {
    "id": "repeated-query-parameter-is-refused",
    "why": "A repeated query parameter arrives as a string[], so restoring the `typeof === 'string'` shape skips the 200-character bound on ?q= entirely and comma-joins the array into a leading-wildcard ILIKE on an unauthenticated route. Measured before the fix: fifty 190-character values produced a 9551-character pattern and a 200. The mutation reads as a stricter guard, which is what makes it worth pinning. cto/AdaptaLabs#22, found by the refute gate on !253.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "    (name) => req.query[name] !== undefined && typeof req.query[name] !== 'string'",
    "mutation": "    (name) => req.query[name] !== undefined && typeof req.query[name] === 'object' && req.query[name] === null",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.list-bound.test.ts",
    "test": "refuses a repeated q rather than letting it bypass the length bound"
  },
  {
    "id": "cron-secret-does-no-length-short-circuit",
    "why": "A length pre-check in front of the digests keeps both createHash and timingSafeEqual, type-checks, and passed the whole 1158-test jest suite when a refute gate tried it - while reintroducing exactly the oracle the guard's own docblock claims to have closed, because an early return on length tells a caller how long the secret is. It returns the same boolean as the real thing for every input, so only the count of sha256 digests can see it.",
    "file": "backend/src/routes/cron.ts",
    "anchor": "  return timingSafeEqual(digest(authHeader ?? ''), digest(`Bearer ${cronSecret}`));",
    "mutation": "  const supplied = authHeader ?? '';\n  const expected = `Bearer ${cronSecret}`;\n  if (supplied.length !== expected.length) return false;\n  return timingSafeEqual(digest(supplied), digest(expected));",
    "runner": "jest",
    "spec": "src/routes/__tests__/cron.constant-time-secret.test.ts",
    "test": "still hashes both sides for a token far shorter than the secret"
  },
  {
    "id": "session-listing-refuses-a-repeated-from",
    "why": "GET /:id/sessions is optionalAuth and pushes ?from= straight into a pool.query parameter array against a timestamp column, so a repeat sent Postgres the array literal and answered an unauthenticated 500 - while the mock branch beside it built an Invalid Date and carried on. Two rounds of fixing the named parameters walked past this one; the class was the file.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "    if (refusedRepeatedParameters(req, res, SINGLE_VALUE_SESSION_FILTERS)) return;",
    "mutation": "",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.list-bound.test.ts",
    "test": "refuses a repeated from rather than sending an array to Postgres"
  },
  {
    "id": "leaderboard-publishes-no-user-id",
    "why": "THE SELECT LIST IS THE PAYLOAD, and that is the whole reason this is pinned in SQL rather than in a type. The leaderboards are unauthenticated by product decision (cto/AdaptaLabs#17, PUBLISHED in the trust model in routes/firsthand.ts), and the route hands `result.rows` from `pg` straight to `res.json`. So `user_id` was published to anyone on the network - the join key that lets an anonymous caller line a name and a participation volume up against anything else they hold. Removing it from `LeaderboardEntry` alone would leave it on the wire with `tsc` clean and every route test green, because a type is a claim about a query and only the query is the payload. The mutation puts `up.user_id` back into the SELECT list; nothing but a real query can see it, which is why the spec is a real-Postgres one.",
    "file": "shared/services/gamification.ts",
    "anchor": "      SELECT\n        u.name,\n        up.total_points,\n        up.monthly_points,\n        up.level,\n        ROW_NUMBER() OVER (ORDER BY up.total_points DESC) as rank",
    "mutation": "      SELECT\n        up.user_id,\n        u.name,\n        up.total_points,\n        up.monthly_points,\n        up.level,\n        ROW_NUMBER() OVER (ORDER BY up.total_points DESC) as rank",
    "runner": "vitest",
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "publishes no user_id on the all-time board",
    "needsDatabase": true
  },
  {
    "id": "leaderboard-monthly-publishes-no-user-id",
    "why": "THE NEAR-IDENTICAL SIBLING, pinned separately for the reason this file keeps relearning: `getLeaderboard` and `getMonthlyLeaderboard` are twenty-six lines apart and differ in one ORDER BY, and cto/AdaptaLabs#17's own limit cap reached one route and not the other. A single entry on the all-time board would report the class closed while the monthly board still published the id, and `monthly_points` narrows a participant's participation volume to the current month, so it is the sharper of the two. The mutation puts `up.user_id` back into the monthly SELECT list only.",
    "file": "shared/services/gamification.ts",
    "anchor": "      SELECT\n        u.name,\n        up.total_points,\n        up.monthly_points,\n        up.level,\n        ROW_NUMBER() OVER (ORDER BY up.monthly_points DESC) as rank",
    "mutation": "      SELECT\n        up.user_id,\n        u.name,\n        up.total_points,\n        up.monthly_points,\n        up.level,\n        ROW_NUMBER() OVER (ORDER BY up.monthly_points DESC) as rank",
    "runner": "vitest",
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "publishes no user_id on the monthly board",
    "needsDatabase": true
  },
  {
    "id": "leaderboard-excludes-a-zero-point-arrival",
    "why": "A PUBLIC BOARD IS EARNED, NOT JOINED BY ARRIVING. `getUserProfile` is a GET handler that INSERTs a user_profiles row when none exists, and both leaderboard routes are UNAUTHENTICATED (cto/AdaptaLabs#17, a deliberate product decision) and select u.name. So without this filter, merely OPENING /gamification published the visitor's real name to any anonymous caller. MEASURED on a real Postgres before the fix: a single GET /api/gamification/profile took the public board from one row to two, the second a 0-point newcomer who had done nothing but load a page. The mechanism pre-dated the AdaptaBits menu change; what that change did was give every signed-in staff member and superadmin a signpost to it, which is why the filter was taken rather than the wider blast radius. The mutation deletes the WHERE clause, restoring the leak. It is valid SQL and the board still returns rows, so nothing fails to build and only a real query can see it. This entry is the ALL-TIME board, filtered on total_points, the metric it ranks by. Its monthly sibling is pinned separately and filters monthly_points instead: the pair is twenty-six lines apart and near-identical, which is the exact shape where a fix applied to one reads as a fix applied to both, and this file's own docblock records #17 hitting that trap already. A shared arm would hide it, so there are two.",
    "file": "shared/services/gamification.ts",
    "anchor": "      WHERE up.total_points > 0\n",
    "mutation": "",
    "runner": "vitest",
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "keeps a zero-total newcomer off the all-time board",
    "needsDatabase": true
  },
  {
    "id": "leaderboard-monthly-excludes-a-zero-point-arrival",
    "why": "A PUBLIC BOARD IS EARNED, NOT JOINED BY ARRIVING. `getUserProfile` is a GET handler that INSERTs a user_profiles row when none exists, and both leaderboard routes are UNAUTHENTICATED (cto/AdaptaLabs#17, a deliberate product decision) and select u.name. So without this filter, merely OPENING /gamification published the visitor's real name to any anonymous caller. MEASURED on a real Postgres before the fix: a single GET /api/gamification/profile took the public board from one row to two, the second a 0-point newcomer who had done nothing but load a page. The mechanism pre-dated the AdaptaBits menu change; what that change did was give every signed-in staff member and superadmin a signpost to it, which is why the filter was taken rather than the wider blast radius. The mutation deletes the WHERE clause, restoring the leak. It is valid SQL and the board still returns rows, so nothing fails to build and only a real query can see it. This entry is the MONTHLY board, filtered on monthly_points rather than total_points, and it is the arm that matters in practice: after a monthly reset everyone sits at 0, ordering among ties is unspecified, and a brand-new profile can surface in the visible top 20. The pinned test also asserts that a lifetime scorer with no monthly points is absent HERE and present on the all-time board, so a copy-paste slip filtering total_points in this query fails by name rather than passing as a near-enough fix.",
    "file": "shared/services/gamification.ts",
    "anchor": "      WHERE up.monthly_points > 0\n",
    "mutation": "",
    "runner": "vitest",
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "keeps a zero-month participant off the monthly board, including one with all-time points",
    "needsDatabase": true
  },
  {
    "id": "points-history-asks-for-one-row-more-than-it-returns",
    "why": "WITHOUT THE +1, `has_more` IS UNSATISFIABLE and silently always false - the exact defect cto/AdaptaLabs#23 was opened for, wearing the fix's clothes. A full page and the end of a caller's own transaction history are the same response unless one extra row is fetched to tell them apart. The mutation binds `limit` instead of `limit + 1`: `rows.length > limit` can then never be true, every page reports the history as finished, and the caller with fifty transactions on the default twenty is told those twenty are all there is. Same shape as `MAX_CSV_PARTICIPANTS + 1`, and it survived a review there once already. Only a real query can see it - the route-level spec mocks the service and would return whatever the mock was told to. ANCHOR RENAMED BY cto/AdaptaLabs#47, which added the keyset cursor and so appended two bind parameters to this line. The PROPERTY is untouched and the entry keeps its id: `+ 1` is still the probe row and it is still the only thing making `has_more` satisfiable. Recorded here because this is what an ANCHOR_MISSING is FOR - the harness reported it by name on a full run, where a manifest filtered to one branch's own new entries could not have seen it, and a silent re-add under a new id would have been a rename undone.",
    "file": "shared/services/gamification.ts",
    "anchor": "    `, [userId, limit + 1, before?.created_at ?? null, before?.id ?? null]);",
    "mutation": "    `, [userId, limit, before?.created_at ?? null, before?.id ?? null]);",
    "runner": "vitest",
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "reports has_more when older rows exist beyond the page",
    "needsDatabase": true
  },
  {
    "id": "points-history-page-reaches-the-wire",
    "why": "A DECISION ABOUT WIRE SHAPE, not about a gate, and the last place `has_more` can be lost. `GET /api/gamification/points-history` answers with `{ transactions, has_more }` rather than a bare array (cto/AdaptaLabs#23), which is a breaking change taken deliberately because the old array could not distinguish a truncated page from the end of history. `res.json(history.transactions)` is a one-word edit that type-checks, restores the old shape byte for byte, and leaves every limit, ceiling, default and authentication assertion in the spec passing - the silent truncation back on the wire with the service still computing the boolean nobody sends. Pinned at the route because the service-level entries above cannot see what the handler chooses to serialise.",
    "file": "backend/src/routes/gamification.ts",
    "anchor": "    res.json(history);",
    "mutation": "    res.json(history.transactions);",
    "runner": "jest",
    "spec": "src/routes/__tests__/gamification.points-history-limit.test.ts",
    "test": "answers with the page and carries a true has_more onto the wire"
  },
  {
    "id": "authorisation-inventory-sees-a-route-it-lists-disappear",
    "why": "DIRECTION TWO of #13, and the half that stops the table rotting into a list nobody trusts. An inventory asserted one way only keeps a verdict for the largest answer surface in the product long after the route moved, and reads as coverage. Renaming the outputs path leaves the table holding a verdict with nothing behind it.",
    "file": "backend/src/routes/session-outputs.ts",
    "anchor": "  '/:id/sessions/:sessionId/outputs',",
    "mutation": "  '/:id/sessions/:sessionId/outputs-moved',",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "has a written-down verdict for every route, and no verdict without a route"
  },
  {
    "id": "authorisation-inventory-sees-a-router-leave-the-mount-graph",
    "why": "The enumeration is mechanical or it is a hand-written import list with extra steps. Router files are read off disk and every one must be reachable from the three roots index.ts mounts, so unmounting a router is red here rather than a route silently leaving the inventory. This is the guard that makes the table exhaustive rather than merely long.",
    "file": "backend/src/routes/api.ts",
    "anchor": "router.use('/stats', statsRouter);",
    "mutation": "",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "reaches every router file on disk from the app roots"
  },
  {
    "id": "authorisation-inventory-sees-a-new-answer-route-with-no-verdict",
    "why": "DIRECTION ONE of #13. A route added to a router no table imported shipped green: the answers-probe on session-outputs.ts passed 843 jest, 625 vitest and tsc on !217, while the identical route on firsthand.ts failed two suites. The mutation carries requireAdmin, so what it lacks is a WRITTEN-DOWN VERDICT rather than a gate - the point being that role-gated is not the same as decided, and an owner check lives in the handler on both real routes beside it.",
    "file": "backend/src/routes/session-outputs.ts",
    "anchor": "const attemptQuerySchema = z.coerce.number().int().positive().optional();",
    "mutation": "const attemptQuerySchema = z.coerce.number().int().positive().optional();\n\nrouter.get(\n  '/:id/sessions/:sessionId/answers-probe',\n  requireAdmin,\n  asyncHandler(async (req: Request, res: Response) => {\n    return res.json(await getRuntimeSession(req.params.sessionId));\n  })\n);",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "has a written-down verdict for every route, and no verdict without a route"
  },
  {
    "id": "authorisation-inventory-sees-a-path-scoped-terminal-handler",
    "why": "A refute gate served a live unauthenticated 200 through router.use('/path', handler) while this suite reported green. The layer has no .stack, so it is not a router, and the fallthrough filed it as router-level middleware: the endpoint vanished from the map AND its handler was credited as an inherited guard to every route registered after it. Two wrong answers from one missing branch, so the walk refuses instead.",
    "file": "backend/src/routes/api.ts",
    "anchor": "router.use('/opportunities', opportunitiesRouter);",
    "mutation": "router.use('/opportunities', opportunitiesRouter);\nrouter.use('/answers-probe', (_req: Request, res: Response) => { res.json({ transcript: 'leak' }); });",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "has a written-down verdict for every route, and no verdict without a route"
  },
  {
    "id": "authorisation-inventory-sees-one-route-shadow-another",
    "why": "Express dispatches to the FIRST matching layer; assignment into a map keeps the LAST. So an ungated duplicate registered ahead of a gated one was recorded with the GATED verdict, the route count did not move, and the ungated handler was what served - proven twice by a gate, same-file and across two routers sharing a mount prefix, both live 200s with no session. A duplicate key is a bug or a shadow and never something to overwrite quietly.",
    "file": "backend/src/routes/api.ts",
    "anchor": "// GET /api/me - Get current user information",
    "mutation": "router.get('/me', (_req, res) => { res.json({ leaked: true }); });\n\n// GET /api/me - Get current user information",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "has a written-down verdict for every route, and no verdict without a route"
  },
  {
    "id": "dev-only-login-routes-stay-out-of-production",
    "why": "auth.ts registers /demo-login, /admin-login, /superadmin-login and /demo-user-2-login behind a NODE_ENV check. All four take no credential and mint a session at the role their name says, superadmin included. setup.ts sets NODE_ENV to test before auth.ts is imported, so the inventory could never observe them and nothing anywhere pinned their absence - the exhaustiveness claim was overstated by exactly the four routes where being wrong costs most. Widening the guard must be red.",
    "file": "backend/src/routes/auth.ts",
    "anchor": "// Demo routes for development\nif (process.env.NODE_ENV === 'development') {",
    "mutation": "// Demo routes for development\nif (process.env.NODE_ENV !== 'nope-never-matches') {",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "registers none of the four when NODE_ENV is production"
  },
  {
    "id": "authorisation-inventory-reads-the-real-app-mount-table",
    "why": "A gate got five live router mounts past a source regex over index.ts: double quotes, a template literal, a path held in a variable, app.use(router) with no path, and an ordinary INDENTED app.use inside an if block - index.ts already contains two indented app.use calls, so that last one is the realistic shape and had zero coverage. The check now reads app._router.stack instead of parsing text, which is why this mutation is red in whichever spelling it is written.",
    "file": "backend/src/index.ts",
    "anchor": "app.use('/api', apiRoutes);",
    "mutation": "if (process.env.NODE_ENV !== 'nope') {\n  const probeRouter = express.Router();\n  probeRouter.get('/leak', (_req: express.Request, res: express.Response) => { res.json({ transcript: 'leak' }); });\n  app.use(\"/probe\", probeRouter);\n}\n\napp.use('/api', apiRoutes);",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "mounts exactly the four routers ROOTS claims, at exactly those prefixes"
  },
  {
    "id": "app-level-routes-outside-every-router-stay-inventoried",
    "why": "/api/health and /health are registered with app.get and belong to no router file, so the router-walking table could not account for them at all - two live unauthenticated endpoints outside the inventory. They are deliberately public, a health check behind a session being useless to monitoring, but a THIRD app-level route must not be able to join them silently.",
    "file": "backend/src/index.ts",
    "anchor": "app.get('/health', (req, res) => {",
    "mutation": "app.get('/leak', (_req: express.Request, res: express.Response) => { res.json({ transcript: 'leak' }); });\n\napp.get('/health', (req, res) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "serves exactly two endpoints straight off the app, both public"
  },
  {
    "id": "authorisation-inventory-sees-an-app-level-path-scoped-handler",
    "why": "The app-level twin of the path-scoped-handler defect. Both app checks enumerated a FILTERED SUBSET - isRouter(handle) for mounts, layer.route for routes - and nothing asserted over the remainder, so app.use('/answers-probe', handler) fell between them and served a live 200 with no session while the suite reported green. inventory()'s throw cannot be reused here because index.ts mounts authLimiter as exactly this shape, so the bucket is ENUMERATED instead: two entries, both the limiter, both directions.",
    "file": "backend/src/index.ts",
    "anchor": "app.use('/api', apiRoutes);",
    "mutation": "app.use('/answers-probe', (_req: express.Request, res: express.Response) => { res.json({ transcript: 'leak' }); });\n\napp.use('/api', apiRoutes);",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "scopes exactly two path-scoped handlers to a prefix, both the auth limiter"
  },
  {
    "id": "authorisation-inventory-sees-a-mount-behind-an-env-branch",
    "why": "Reading the real app bought a dependence on WHICH real app: it was imported once, under NODE_ENV=test, and index.ts already branches on process.env.NODE_ENV three times for app.listen and both cron schedules, so a mount inside such a branch was invisible. The production arm re-imports under an isolated registry with node-cron stubbed and the listen socket captured and closed, which is what makes this red. It also found a real conditional route nobody had written down, GET /api/csrf-token.",
    "file": "backend/src/index.ts",
    "anchor": "app.use('/api', apiRoutes);",
    "mutation": "if (process.env.NODE_ENV !== 'test') {\n  const probeDirect = express.Router();\n  probeDirect.get('/leak', (_req: express.Request, res: express.Response) => { res.json({ transcript: 'leak' }); });\n  app.use('/probe', probeDirect);\n}\n\napp.use('/api', apiRoutes);",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "mounts the same four routers when NODE_ENV is production"
  },
  {
    "id": "results-read-per-caller-limit-is-detectable",
    "why": "The number IS the policy. Every other test in the block derives its expectation from the constant, so all of them keep passing at a limit of fifty - which is no limit at all on a surface studyResultsLimiter allows ten requests a minute. cto/AdaptaLabs#9 measured what an unbounded caller does: another admin's results requests refused for as long as the attacker cares to, entirely inside the rate limits.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "export const MAX_IN_FLIGHT_RESULTS_READS_PER_USER = 2;",
    "mutation": "export const MAX_IN_FLIGHT_RESULTS_READS_PER_USER = 50;",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "holds the per-caller limit at the number that was decided"
  },
  {
    "id": "results-read-per-caller-limit-binds",
    "why": "A per-caller counter that is incremented and never compared is decorative, and the symptom is invisible: the counts read correctly, the leak detector passes, and one admin can still put a minute's worth of rate limit into a FIFO queue ahead of the superadmin. Widened rather than deleted so it still type-checks and still counts, which is the shape of mutation that survives a suite.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "  if (alreadyInFlight >= MAX_IN_FLIGHT_RESULTS_READS_PER_USER) {",
    "mutation": "  if (alreadyInFlight >= MAX_IN_FLIGHT_RESULTS_READS_PER_USER + 1000) {",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "refuses one caller a third read while two are in flight"
  },
  {
    "id": "results-read-early-release-is-real",
    "why": "The whole of #9's fix is one function call doing something. A no-op body type-checks, leaves every caller and every import in place, and puts the measured outage straight back - one admin holding the only results-read permit for the drain timeout, ten times a minute, refusing every other admin including the superadmin.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "  earlyRelease.get(res)?.();",
    "mutation": "  void res;",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "admits the next caller while the first is still streaming"
  },
  {
    "id": "results-read-early-release-keeps-the-caller-charged",
    "why": "The early release must hand back the GLOBAL permit and NOT the per-caller slot. Releasing both type-checks - the two closures have the same signature - and trades the availability bound for a memory one: nothing would then bound how many CSV streams hold an id list of up to 200,000 session ids at once, on a single-replica 2Gi pod.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "      earlyRelease.set(res, releaseGate);",
    "mutation": "      earlyRelease.set(res, releaseHeld);",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "keeps the caller charged for their still-streaming read"
  },
  {
    "id": "results-read-slot-returns-on-a-queue-refusal",
    "why": "The refusal arm removes the close listener before answering, so a slot not given back there is never given back at all - one 503 would lock that admin out of all four results routes for the life of the process. The same shape of leak this file's permit accounting shipped with once already, and it is silent: the 503 looks correct.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "      // routes for the life of the process by a single 503.\n      releaseHeld();",
    "mutation": "      // routes for the life of the process by a single 503.",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "gives the caller their slot back when the queue refuses them"
  },
  {
    "id": "results-read-csv-route-releases-at-the-boundary",
    "why": "cto/AdaptaLabs#9. The study CSV route hands the global results permit back at the preflight-to-stream boundary, so the client-paced drain is not inside the permit - #9 measured one admin holding it for the whole drain, ten a minute, refusing every other admin. #85 added a SECOND releaseResultsReadPermit(res) to this file (the aggregate route), so the bare-line anchor is now ambiguous; re-pointed to the unique comment+call. Dropping the CSV release reverts the route to holding the permit through the drain, caught by the all-four structural test.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "  // AFTER the last thing that can throw, so a refusal still runs inside the\n  // permit and cannot be raced by the caller who was queued behind it.\n  releaseResultsReadPermit(res);",
    "mutation": "  // AFTER the last thing that can throw, so a refusal still runs inside the\n  // permit and cannot be raced by the caller who was queued behind it.",
    "runner": "jest",
    "spec": "src/routes/__tests__/runtime-pool-admission-routes.test.ts",
    "test": "releases the permit early on all four results routes"
  },
  {
    "id": "results-read-per-caller-limit-is-not-tighter-than-decided",
    "why": "The limit has to be pinned in BOTH directions. Widening it is caught by the literal and by the third-read test; narrowing it back to one is caught by nothing else, and one is precisely the value that answers 429 to a researcher who downloads a CSV and then reloads their own results view - the ordinary workflow, and the reason two was measured and chosen.",
    "file": "backend/src/middleware/results-read-concurrency.ts",
    "anchor": "  if (alreadyInFlight >= MAX_IN_FLIGHT_RESULTS_READS_PER_USER) {",
    "mutation": "  if (alreadyInFlight >= MAX_IN_FLIGHT_RESULTS_READS_PER_USER - 1) {",
    "runner": "jest",
    "spec": "src/middleware/__tests__/results-read-concurrency.test.ts",
    "test": "lets one caller hold a second read at the same time"
  },
  {
    "id": "admin-dashboard-identity-scope-reads-live-role",
    "why": "GET /api/admin/dashboard sat on requireAuth and made THREE decisions from the role stamped into the session at login, of which the gate is the least (cto/AdaptaLabs#38). A superadmin demoted to researcher_admin is STILL an admin, so the gate admits them correctly and can never catch this - but participantIdentityOwnerId then read a stale 'superadmin' and resolved to null, which is no owner filter at all, so recent_bookings kept carrying every other researcher's participant names and emails for up to SESSION_MAX_AGE_MS = 24h. withLiveRole now attaches the live role before the handler runs. Reading req.session.user.role here restores exactly the stale scope read while LEAVING THE GATE LIVE, which is the point: the gate and the scope are two decisions and an entry pinning the gate cannot speak for the scope. It type-checks (same union), so the canary reports KILLED only because the named jest test demotes the DB role and asserts the other researcher's participant does not cross the wire.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  const participantIdentityOwnerId = user.role === 'superadmin' ? null : userId;",
    "mutation": "  const participantIdentityOwnerId = req.session!.user!.role === 'superadmin' ? null : userId;",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.dashboard-and-request-read-live-role.test.ts",
    "test": "scopes a demoted superadmin to their own participants"
  },
  {
    "id": "admin-dashboard-counts-scope-reads-live-role",
    "why": "The SECOND scope constant in the same handler, and it needs its own entry for the reason the handler's own docblock gives: countsOwnerId and participantIdentityOwnerId are deliberately separate declarations, so a mutation of one says nothing about the other. Same stale read as its sibling (cto/AdaptaLabs#38) - a demoted superadmin kept receiving GLOBAL counts across every researcher's opportunities, bookings, participants and sessions. Reading the session role here type-checks and restores the stale widening; the named jest test demotes the DB role and asserts every count query binds the caller's own id.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  const countsOwnerId = user.role === 'superadmin' ? null : userId;",
    "mutation": "  const countsOwnerId = req.session!.user!.role === 'superadmin' ? null : userId;",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.dashboard-and-request-read-live-role.test.ts",
    "test": "scopes a demoted superadmin counts to their own opportunities"
  },
  {
    "id": "admin-dashboard-gate-reads-live-role",
    "why": "The dashboard's in-handler admin gate, which stays in the handler because this is the 'am I an admin' surface every signed-in user loads and the 403 is part of its contract - requireAdmin cannot cover it. Pre-#38 it read the session role, so a FULLY revoked admin kept the whole dashboard for up to SESSION_MAX_AGE_MS = 24h. Distinct from the two scope entries above: this one pins the refusal, they pin what is withheld from a caller the refusal correctly admits. Reading the session role type-checks and restores the stale gate; the named jest test sets the DB role to employee and asserts a 403.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  if (!isAdminRole(user.role)) {",
    "mutation": "  if (!isAdminRole(req.session!.user!.role)) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.dashboard-and-request-read-live-role.test.ts",
    "test": "refuses an admin whose database role was revoked after login"
  },
  {
    "id": "admin-request-reads-live-role",
    "why": "POST /api/admin/request is how a non-admin ASKS to become one, so it admits non-admins and cannot sit on requireAdmin either. Read off the session it told a REVOKED admin 'You already have admin access' and refused the request, leaving them unable to ask for it back until their cookie expired (cto/AdaptaLabs#38). No escalation, but the same stale read and wrong in a user-visible way. Reading the session role type-checks and restores the dead end; the named jest test sets the DB role to employee and asserts the request is accepted.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "  if (isAdminRole(user.role)) {",
    "mutation": "  if (isAdminRole(req.session!.user!.role)) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.dashboard-and-request-read-live-role.test.ts",
    "test": "lets a revoked admin ask for access again"
  },
  {
    "id": "admin-gate-carries-live-role-to-the-handler",
    "why": "THE PREMISE THE WHOLE #14/#37/#38 FAMILY RESTS ON, and until #38 it was the one link with no guard. requireAdmin re-reads the live role and gates on it - that half was already pinned by admin-gate-rereads-live-role-from-db - but it must also CARRY that role onto req.user, because every in-handler 'is this caller a superadmin' branch behind the gate reads req.user.role: GET /api/admin/export/bookings, session-outputs.ts, firsthand.ts's requireSuperadminForStudyResults, and a dozen scope decisions in opportunities.ts. Dropping only the carry - the gate left completely intact - passed 1227 jest and 650 vitest tests and re-opened #38's exact defect on the export route: a demoted superadmin gets a global CSV of every participant's name and email. #38's own report excused that route as 'safe because it sits on requireAdmin', which is a claim about verification that nothing verified. It type-checks (the spread is still a SessionUser), so the canary reports KILLED only because the named jest test demotes the DB role and asserts the rival participant's row is absent from the CSV bytes. NOTE the sibling carry in requireSuperadmin is deliberately NOT pinned: that gate has already forced role === 'superadmin' and no route behind it reads req.user.role, so dropping it passes 1242/1242 - an equivalent mutation, measured, and an entry for it would be a false pin.",
    "file": "backend/src/middleware/authenticate.ts",
    "anchor": "  // sub-gates) see the live value, not the stale session one.\n  req.user = { ...req.session.user, role };",
    "mutation": "  // sub-gates) see the live value, not the stale session one.\n  req.user = { ...req.session.user };",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.dashboard-and-request-read-live-role.test.ts",
    "test": "scopes the exported CSV for a demoted superadmin"
  },
  {
    "id": "cleanup-cancelled-route-stays-deleted",
    "why": "cto/AdaptaLabs#49 DELETED POST /api/bookings/cleanup-cancelled rather than re-gating it: one owner-scoped SELECT, no caller anywhere in the tree, and a commented-out bulk DELETE waiting to be uncommented on the weakest gate in the vocabulary. A deletion is only a decision if something notices it being undone, and a tombstone comment is not that something. This re-adds the route on requireAuth, in the shape it had, and the named arm must go red - that arm asserts 404 AND asserts no statement was issued, so a re-add fails on both. The mutation keeps the original try/catch on purpose: an unguarded async throw in an Express 4 handler never answers, and a hang is a job timeout with no named failing test.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "// Pinned in bookings.cleanup-cancelled-is-gone.test.ts, which fails BY NAME if",
    "mutation": "router.post('/cleanup-cancelled', requireAuth, async (req: Request, res: Response) => {\n  try {\n    const found = await pool.query(\"SELECT b.id FROM bookings b WHERE b.user_id = $1 AND b.status = 'cancelled'\", [req.user!.id]);\n    res.json({ cancelled_bookings: found.rows });\n  } catch (error) {\n    logger.error('Error cleaning up cancelled bookings', { error });\n    res.status(500).json({ error: 'Failed to clean up cancelled bookings' });\n  }\n});\n\n// Pinned in bookings.cleanup-cancelled-is-gone.test.ts, which fails BY NAME if",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cleanup-cancelled-is-gone.test.ts",
    "test": "answers 404 on POST api bookings cleanup-cancelled and runs no handler"
  },
  {
    "id": "node-env-fails-closed-on-an-unrecognised-value",
    "why": "Every security control in backend/src/index.ts is keyed off config.NODE_ENV === 'production' - helmet, CSRF, cookie.secure, cookie.sameSite, cookie.domain and trust proxy, which also keys every rate limiter. This field ended in .catch(() => 'development'), so 'prod', 'staging' or 'Production' resolved silently to development and turned all of it off in a deployment that believed it was production. Reproduced at the real entry point: NODE_ENV=prod started the server and logged \"environment\":\"development\", \"csrfEnabled\":false and Helmet disabled. The mutation restores that exact .catch(), so it type-checks and the parse succeeds where it must now throw.",
    "file": "shared/config/environment.ts",
    "anchor": "  NODE_ENV: z.enum(NODE_ENVS).default('development'),",
    "mutation": "  NODE_ENV: z.enum(NODE_ENVS).catch(() => 'development' as const),",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "names the offending value and lists the permitted ones"
  },
  {
    "id": "opportunities-catalogue-branch-reads-live-role",
    "why": "Four optionalAuth routes on opportunities.ts compute `isAdmin` inline from req.user?.role - the role stamped into the session at LOGIN - to decide whether the caller sees drafts, unredacted owner identity, clicks_total, an unpublished study, a draft's sessions and its joining link, and a draft study's pre-consent brief. #14 and #37 never reached them because withLiveRole 401s without a session and these routes are the participant catalogue, which must answer a signed-out browser. So a revoked researcher_admin kept the whole admin view for up to SESSION_MAX_AGE_MS = 24h (cto/AdaptaLabs#45). withLiveRoleIfPresent re-reads the role before the handlers run. Reading the session role instead still type-checks - the same union, annotated to keep the null comparison legal - and restores exactly the stale read, so the canary reports KILLED only because the named jest test downgrades the DB role after login and asserts the participant view.",
    "file": "backend/src/middleware/authenticate.ts",
    "anchor": "  const liveAdminRole = await currentDbRole(req, res);",
    "mutation": "  const liveAdminRole: AdminRole | null = req.session!.user!.role;",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.inline-admin-gates-read-live-role.test.ts",
    "test": "gives an admin whose database role was revoked after login the participant view"
  },
  {
    "id": "catalogue-live-role-read-is-narrowed-to-admin-sessions",
    "why": "THE PERFORMANCE HALF OF #45's DECISION, and it needs its own entry because the entry above cannot speak for it: re-reading MORE often keeps every liveness assertion green. GET /api/opportunities is the catalogue every signed-in user loads, and currentDbRole's own docblock used to justify its price on the grounds that it never touched a hot participant path. The narrowing is what keeps that true - a re-read can only take privilege AWAY, so a session whose stored role is not already an admin role has nothing to gain from the query and is handed straight on. UPDATED (2026-09-04): 'stored role' here means the EFFECTIVE stored role, resolved through the CORTEX_BETA_ALL_ADMIN lift before this check runs - a beta-lifted employee session does gain a query, and the narrowing only skips one for a session that gains nothing even after that lift is applied. The mutation is option 1 from the issue, re-reading for any session at all: it type-checks, it is a defensible design, and it silently puts one SELECT on every authenticated catalogue load. Only the query-count arms can see it, and they pin 0 and 1 as literals rather than deriving them from the code.",
    "file": "backend/src/middleware/authenticate.ts",
    "anchor": "  if (!isAdminRole(storedRole)) {",
    "mutation": "  if (storedRole === undefined) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.inline-admin-gates-read-live-role.test.ts",
    "test": "issues no role query for a signed-in participant, on any of the four routes"
  },
  {
    "id": "catalogue-listing-carries-the-live-role-middleware",
    "why": "The middleware being correct is not the same as it being MOUNTED, and #38 made that point expensively one layer down: authenticate.ts:70, the single carry the whole live-role family rests on, was unpinned, and dropping it passed 1227 tests. The two entries above pin what withLiveRoleIfPresent does; this one pins that GET /api/opportunities actually chains it. Removing it from the chain leaves the other three routes covered, type-checks cleanly, and re-opens the largest disclosure of the four - drafts, owner identity and clicks_total on the listing every signed-in user loads.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "router.get('/', optionalAuth, withLiveRoleIfPresent, asyncHandler(async (req: Request, res: Response) => {",
    "mutation": "router.get('/', optionalAuth, asyncHandler(async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.inline-admin-gates-read-live-role.test.ts",
    "test": "gives an admin whose database role was revoked after login the participant view"
  },
  {
    "id": "is-admin-role-includes-superadmin",
    "why": "cto/AdaptaLabs#57: the admin-role SET now lives in one predicate (shared/types isAdminRole) instead of being spelled inline at 16 sites. Dropping `superadmin` here makes every admin gate stop recognising a superadmin. Pinned by the direct unit assertion, NOT an integration test: because the same predicate now feeds both withLiveRoleIfPresent and the handler's own isAdmin, dropping superadmin makes BOTH treat a superadmin as a participant, so the end-to-end \"revoked superadmin gets the participant view\" outcome is still reached by a different path and the integration test cannot see the change (measured: it SURVIVED). Its sibling is-admin-role-includes-researcher-admin pins the other direction; a single assertion cannot see the other role removed.",
    "file": "shared/types/index.ts",
    "anchor": "  return role === 'researcher_admin' || role === 'superadmin';",
    "mutation": "  return role === 'researcher_admin';",
    "runner": "jest",
    "spec": "src/types/__tests__/isAdminRole.test.ts",
    "test": "treats superadmin as an admin role"
  },
  {
    "id": "is-admin-role-includes-researcher-admin",
    "why": "cto/AdaptaLabs#57: the mirror of is-admin-role-includes-superadmin. Drops `researcher_admin` from the single admin-role predicate, so every admin gate stops recognising an ordinary research admin. Killed by the named unit test asserting isAdminRole('researcher_admin') is true. Pinned as its own entry because the superadmin-direction test cannot see researcher_admin being removed - the property is BOTH roles in the set, and each direction needs its own arm.",
    "file": "shared/types/index.ts",
    "anchor": "  return role === 'researcher_admin' || role === 'superadmin';",
    "mutation": "  return role === 'superadmin';",
    "runner": "jest",
    "spec": "src/types/__tests__/isAdminRole.test.ts",
    "test": "treats researcher_admin as an admin role"
  },
  {
    "id": "catalogue-listing-chain-order-is-load-bearing",
    "why": "MEMBERSHIP IS NOT ORDER. Express runs the chain in order, and optionalAuth running LAST overwrites req.user with the session copy - throwing away the live role withLiveRoleIfPresent just read, and restoring #45's defect in full. The refute gate on !264 measured the swap at 1 failed / 1300, caught by this single arm, because verdictOf's only primitive was then `stack.some(...)` and the route stayed filed as `optional-session+live-role` while no longer being one. cto/AdaptaLabs#56 has since made verdictOf POSITIONAL, so the authorisation inventory now refuses this swap table-wide and five of its arms go red too (measured, 5 failed / 24 on this exact mutation, against 0 before). This entry stays because it pins the BEHAVIOURAL arm - the inventory reads a middleware stack and can never observe that a revoked admin gets the admin view, which is the defect itself rather than its shape.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "router.get('/', optionalAuth, withLiveRoleIfPresent, asyncHandler(async (req: Request, res: Response) => {",
    "mutation": "router.get('/', withLiveRoleIfPresent, optionalAuth, asyncHandler(async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.inline-admin-gates-read-live-role.test.ts",
    "test": "gives an admin whose database role was revoked after login the participant view"
  },
  {
    "id": "the-all-bookings-route-projects-columns-rather-than-spreading-the-row",
    "why": "THE SECOND TIME THIS EXACT LEAK HAS BEEN WRITTEN IN THIS FILE. learnings.md:349 records `SELECT b.*` on `/bookings/my/bookings` sending admin_notes - a researcher's free-text judgement of a participant - to that participant, invisible in the UI and fully readable in the Network tab. The sibling was fixed at the PROJECTION (participantBookingColumns); `/my/bookings/all` was left on `b.*` with only the response map keeping the three fields off the wire, and a refute gate on !270 proved the map was an unguarded guard: adding `admin_notes: booking.admin_notes` to it measured 1313 passed / 1313 on both the mutated and the baseline tree, interleaved, so the leak was invisible to all 78 backend suites. This mutation is that gate's mutation. The route now projects five named columns, which closes the class rather than the instance - a column never selected cannot be mapped out by accident - and the arm named below feeds a row carrying all three fields so it can still tell a narrowed projection from a lucky one.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "        status: booking.status,\n        created_at: booking.created_at.toISOString(),",
    "mutation": "        status: booking.status,\n        admin_notes: booking.admin_notes,\n        created_at: booking.created_at.toISOString(),",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cleanup-cancelled-is-gone.test.ts",
    "test": "sends no researcher-only column to the caller, whatever the row carries"
  },
  {
    "id": "the-superset-route-49-rests-on-cannot-quietly-leave",
    "why": "cto/AdaptaLabs#54. `GET /api/bookings/my/bookings/all` has no client caller anywhere in the tree. SCOPE STATED AS MEASURED, because a first draft of this sentence was wider than the check behind it: `git grep -n \"my/bookings\"` at 150911d across every tracked file, no path or extension filter, returns 9 hits in 7 files OUTSIDE backend/ - demo/demo-server.ts:283,284, docs/CORTEX-FOR-CLAUDEL-UNMODERATED-PLANNING.md:128, e2e/accessibility.test.ts:419,431, frontend/src/api/client.ts:389, learnings.md:349, plan.md:124 and shared/types/index.ts:194. Every one of them refers to the SIBLING route `/my/bookings`; none names the debug path, so the conclusion holds and only the stated scope was wrong. `git grep -n \"bookings/debug\"` over the same tree returns 5 hits, all backend, all accounted for. So it looks exactly like the no-op route #49 deleted, and it is not: `bookings.cleanup-cancelled-is-gone.test.ts` rests the whole argument that #49's deletion cost nothing on this route returning a superset. #54 renamed it out of `/debug` for that reason; this entry is the alarm that says deleting it is red BY NAME rather than a map diff, because a name is a hint and a failing test is not.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "router.get('/my/bookings/all', requireAuth, async (req: Request, res: Response) => {",
    "mutation": "router.get('/my/bookings/all-removed', requireAuth, async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.cleanup-cancelled-is-gone.test.ts",
    "test": "leaves the caller their cancelled bookings on the all route scoped to their own id"
  },
  {
    "id": "authorisation-inventory-checks-chain-ORDER-not-membership",
    "why": "cto/AdaptaLabs#56, and the entry has to be here rather than on a route because the property is table-wide. verdictOf used to derive every verdict from `stack.some(...)`, which is membership: it said a handler was somewhere in the chain and nothing about where. Reversing the real catalogue chain in opportunities.ts left the whole inventory at 21/21 green while `optional-session+live-role` had become a false statement about the route (measured, both arms). The fix makes the primitive positional, and THIS mutation restores membership semantics exactly - indexOf becomes a boolean dressed as a position, so it can never be less than the copier's index and the throw can never fire. EVERY REAL ROUTE STAYS GREEN UNDER IT, which is the point: the only things that can see it are the throwaway-probe arms, measured at 3 failed / 25 - `refuses a chain whose ORDER defeats the gate it carries`, `names the right mechanism, which is not the same one for all five` and `sees the order across the router-level and per-route halves of a chain`. The fourth ordering arm, `accepts every one of those pairs the right way round`, is the CONTROL and correctly stays green, which is why the figure is three and not four. Those arms are what must not be deleted.",
    "file": "backend/src/routes/__tests__/authorisation-inventory.test.ts",
    "anchor": "      const runsAt = handlers.indexOf(dependant);",
    "mutation": "      const runsAt = handlers.includes(dependant) ? Number.MAX_SAFE_INTEGER : -1;",
    "runner": "jest",
    "spec": "src/routes/__tests__/authorisation-inventory.test.ts",
    "test": "refuses a chain whose ORDER defeats the gate it carries"
  },
  {
    "id": "test-http-client-pools-its-sockets",
    "why": "The pooled agent is installed once in setup.ts and every other suite benefits without mentioning it. Dropping the call reverts the whole repo to one TCP connection per supertest request - measured, 1858 sockets in TIME_WAIT a run against a 16384-port ephemeral range - and nothing would fail, because no assertion anywhere else looks at a connection count (cto/AdaptaLabs#44).",
    "file": "backend/src/__tests__/setup.ts",
    "anchor": "installPooledTestAgent();",
    "mutation": "// installPooledTestAgent();",
    "runner": "jest",
    "spec": "src/__tests__/pooled-agent.test.ts",
    "test": "reuses the socket ACROSS tests, not merely within one"
  },
  {
    "id": "listening-server-never-closes-a-pooled-socket",
    "why": "Node closes an idle keep-alive connection after keepAliveTimeout, and a client reusing one it has just closed gets ECONNRESET - which superagent reports as `socket hang up`, the exact text of the flake the pooling exists to remove. Restoring Node's 5000ms default reintroduces it invisibly: measured 5 resets in 6 at 5000, 0 in 6 at 0 (cto/AdaptaLabs#44).",
    "file": "backend/src/__tests__/helpers/listening.ts",
    "anchor": "  server.keepAliveTimeout = 0;",
    "mutation": "  server.keepAliveTimeout = 5000;",
    "runner": "jest",
    "spec": "src/__tests__/listening-helper.test.ts",
    "test": "disables the server keep-alive timeout, so a pooled socket is never closed under the client"
  },
  {
    "id": "port-fails-closed-rather-than-becoming-nan",
    "why": "cto/AdaptaLabs#48. PORT ended in .catch(() => 3001), and that .catch NEVER FIRED for a non-numeric value - z.string().transform(Number) returns NaN rather than throwing, so there was nothing for it to catch. Measured on the schema before the change: PORT=abc parsed to NaN and PORT= parsed to 0. The live consumer is app.listen(config.PORT) in backend/src/index.ts, and listen(NaN) binds an EPHEMERAL port, so on Kubera the readiness probe on 3001 never passes and no log line names the cause. The mutation restores that exact .catch(): it type-checks and PORT stays a number, so nothing fails to build. MEASURED, not asserted, and RE-MEASURED on the #59 branch after the spec grew: applied, it fails 12 of 52 in this spec - all nine 'refuses to boot on the unusable PORT' arms, the named ceiling arm, the named NaN arm, and the message arm - and the unset, valid and CORS_ORIGIN arms stay green. It was 8 of 37 before #59 added the three over-the-ceiling arms and the ceiling arm, and the spec has since grown again; the figure is restated rather than left, because a count nobody re-measures is how a note starts lying. An earlier version of this note claimed only the NaN arm could see it, which was a claim about verification that the measurement refuted. The NaN arm is still the one worth naming here, because it is the only one that pins the SHAPE of the old defect - a value that parsed successfully into NaN rather than a value that was refused - and NaN === NaN is false, so it has to be written as a refusal check rather than as toBe(NaN).",
    "file": "shared/config/environment.ts",
    "anchor": "  PORT: z.coerce.number().int().positive().max(65535).default(3001),",
    "mutation": "  PORT: z.string().transform(Number).catch(() => 3001),",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "never yields NaN for a non-numeric PORT, which listen would bind as an ephemeral port"
  },
  {
    "id": "session-fan-out-probe-row-keeps-the-413-reachable",
    "why": "cto/AdaptaLabs#41. GET /api/opportunities bounds its outer dimension at 1000 opportunities and then fanned out over those ids with no LIMIT at all, so the worst case was 1000 opportunities times however many sessions each holds in one result set. The fix bounds the product and REFUSES above it. The probe row is the whole reason the refusal is reachable: drop the plus one and the comparison rows.length > MAX_SESSIONS_RETURNED can never be true, the 413 becomes dead code, and the refusal silently becomes the truncation it exists to prevent - with no test able to see it, because every request-level arm mocks pool.query and gets its row count from the mock rather than from the SQL. Only an assertion on the SQL text can, which is why that arm exists. Same shape and same reason as MAX_CSV_PARTICIPANTS + 1 and the listing query's own probe row.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "         LIMIT ${MAX_SESSIONS_RETURNED + 1}`,",
    "mutation": "         LIMIT ${MAX_SESSIONS_RETURNED}`,",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.list-bound.test.ts",
    "test": "asks the database for one session more than it will return"
  },
  {
    "id": "points-history-keyset-keeps-its-id-tiebreak",
    "why": "cto/AdaptaLabs#47. The keyset cursor compares (created_at, id) as a ROW, and the id half is the only thing separating two transactions awarded by the same statement - points_transactions.created_at is TIMESTAMPTZ with no uniqueness, and awardPoints can write two rows on one instant. Drop the tiebreak and a page boundary landing between twins excludes BOTH when it excludes the one just read, so a row the caller owns disappears from their history for ever with nothing reporting it. The mutation keeps $4 referenced so the statement still binds four parameters and every other arm stays green - measured 1 failed / 18 against a real Postgres, and 11 failed when a sloppier version left $4 unused, which is the difference between proving the tiebreak matters and proving the SQL still parses. Only the twin arm can see this: it is the one that seeds two rows at the same microsecond and pages with limit 1 so a boundary falls between them.",
    "file": "shared/services/gamification.ts",
    "anchor": "        AND ($3::timestamptz IS NULL OR (created_at, id) < ($3::timestamptz, $4::uuid))",
    "mutation": "        AND ($3::timestamptz IS NULL OR (created_at < $3::timestamptz AND $4::uuid IS NOT NULL))",
    "runner": "vitest",
    "needsDatabase": true,
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "returns both of two transactions that share a created_at exactly once"
  },
  {
    "id": "points-history-cursor-is-rendered-in-utc",
    "why": "cto/AdaptaLabs#47, raised by the refute gate on !267 as the single most load-bearing token in the query and the one with no guard. to_char renders a TIMESTAMPTZ in the SESSION's TimeZone, so without AT TIME ZONE 'UTC' next_before is a wall-clock reading in whatever zone the connection carries - labelled Z regardless - and is fed back into a $3::timestamptz that reads an offsetless value as being in that same zone. The round trip then closes only by coincidence, when the session is UTC, which it is in CI and in every container this repo starts: the gate measured the removal passing 18 of 18 before this arm existed. The gate's own probe measured Pacific/Kiritimati at +14 paging 50 pages and seeing ONE unique row, and America/New_York silently losing 5 of 8. Neither raised an error. The mutation is a pure SQL-string edit so it always builds, and only the arm that reads through a pool pinned to a non-UTC session can see it - measured 1 failed alone, failing with the test's own 50-page bound rather than hanging, and the same arm passes with the token present.",
    "file": "shared/services/gamification.ts",
    "anchor": "             to_char(created_at AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"') AS cursor_at",
    "mutation": "             to_char(created_at, 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"') AS cursor_at",
    "runner": "vitest",
    "needsDatabase": true,
    "spec": "src/services/__tests__/gamification-postgres.test.ts",
    "test": "renders the same cursor under a non-UTC session TimeZone"
  },
  {
    "id": "port-refuses-a-value-above-the-16-bit-ceiling",
    "why": "cto/AdaptaLabs#59. .int().positive() alone answers a wider question than 'is this a port': every positive integer passed, so PORT=1e5 coerced to 100000 and validated CLEANLY, then failed inside app.listen() - a boot failure moved from the place that names the variable to the place that does not. Measured on the schema before this change, on ab8e605: PORT=1e5 -> 100000 accepted, PORT=99999 -> 99999 accepted, PORT=65536 -> 65536 accepted. 65535 is not a policy number somebody picked, it is the largest value a 16-bit port field holds. The mutation removes .max(65535) and nothing else, so it type-checks and PORT is still a number - the same shape as the defect it restores. MEASURED, not asserted, on this branch: applied, it fails 4 of 52 in this spec - the '65536', '99999' and '1e5' arms of 'refuses to boot on the unusable PORT', plus the named ceiling arm - and every other arm including the 65535 control stays green. The ceiling arm is the one named here because it is the only one that pins 65535 and 65536 as LITERALS on both sides; the it.each arms would still read as a pass if the ceiling moved to 70000.",
    "file": "shared/config/environment.ts",
    "anchor": "  PORT: z.coerce.number().int().positive().max(65535).default(3001),",
    "mutation": "  PORT: z.coerce.number().int().positive().default(3001),",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "names 65535 as the ceiling and 65536 as over it"
  },
  {
    "id": "cors-origin-must-be-an-http-scheme",
    "why": "cto/AdaptaLabs#59. zod 3's .url() is new URL(), which parses ANY scheme, so the validator was too loose rather than bypassed - a different defect from #48 and not covered by its fix. Measured on the schema before this change, on ab8e605, ALL ACCEPTED: CORS_ORIGIN=localhost:3000 (read as the scheme 'localhost:' with the path '3000', which is exactly what somebody types when they mean http://localhost:3000), htp://example.com, ftp://example.com and javascript:alert(1). The live consumer is the cors() origin in backend/src/index.ts and a browser Origin header is always scheme://host[:port] with a lower-cased scheme, so each of those boots happily and then matches nothing, reaching an operator as browser CORS errors on every request rather than as a config fault. The mutation DELETES the .refine line, which is what a revert or a tidy-up would do, and the remaining chain still type-checks. MEASURED, not asserted, on this branch: applied, it fails 6 of 52 in this spec - all six arms of 'refuses to boot on the non-http scheme CORS_ORIGIN', including the single-slash http:/x.com arm - and the valid-origin control arms and the unset default stay green, which is what says the refine narrows rather than refuses everything.",
    "file": "shared/config/environment.ts",
    "anchor": "    .refine((value) => /^https?:\\/\\//.test(value), 'CORS origin must be an http(s) URL')\n",
    "mutation": "",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "refuses to boot on the non-http scheme CORS_ORIGIN \"localhost:3000\""
  },
  {
    "id": "cors-origin-is-trimmed-before-it-is-validated",
    "why": "cto/AdaptaLabs#59, the half raised by the refute gate on !267. Measured on the schema before this change, on ab8e605: CORS_ORIGIN=' https://x.com ' was ACCEPTED and kept its spaces, because the WHATWG URL parser strips surrounding whitespace while zod returns the ORIGINAL string. A padded value matches no Origin header for the same reason a bad scheme does not, and it is the likelier accident of the two - a trailing space in a chart value or an .env line is invisible in review. The assertion is on the RESULTING VALUE rather than on the parse succeeding: the padded value PARSES EITHER WAY, so a test that only checked 'does not throw' would be decorative. A PREVIOUS VERSION OF THIS NOTE claimed the .trim()/.url() ORDER was what this entry protected, and the refute gate on !272 refuted it - the swapped schema passed all 47 arms of the spec. The order does matter, but only for non-ASCII whitespace, and it now has its own entry (cors-origin-is-trimmed-before-the-url-parser-sees-it) with an arm that can actually see it. MEASURED, not asserted, on this branch: applied, it fails 4 of 52 in this spec - the three trimming arms plus the process.env write-back arm - and everything else stays green. The space arm is the one pinned because the canary selects with jest's -t, which is a regex, and a title rendering a tab is not a usable pattern.",
    "file": "shared/config/environment.ts",
    "anchor": "  CORS_ORIGIN: z\n    .string()\n    .trim()\n",
    "mutation": "  CORS_ORIGIN: z\n    .string()\n",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "trims a space-padded CORS_ORIGIN rather than carrying the padding into the cors origin"
  },
  {
    "id": "cors-origin-is-trimmed-before-the-url-parser-sees-it",
    "why": "cto/AdaptaLabs#59, and it exists because the refute gate on !272 REFUTED the claim that the .trim()/.url() order was load-bearing. Measured: swapping to .string().url().trim() passed all 47 arms the spec then had, because new URL() strips leading and trailing C0-and-space itself, so .url() never sees ASCII padding as a problem under either order, and the .refine sits on the ZodEffects wrapping the string so it receives the trimmed value either way. The orders diverge ONLY on whitespace String.prototype.trim() strips and the WHATWG parser does not. Measured on both: CORS_ORIGIN='\\u00a0https://x.com\\u00a0' (U+00A0) and '\\u3000https://x.com' (U+3000) are ACCEPTED and normalised with .trim() first, and REFUSED as malformed with .url() first. A non-breaking space is what a value pasted out of a rendered wiki page or a Slack message carries and is invisible in every review tool there is, so the shipped order is the deliberately permissive one. The mutation is the swap itself rather than a deletion, which is what makes this entry the standing proof of the order rather than a note asserting it. MEASURED on this branch: applied, it fails exactly 1 of 52 - the named non-breaking-space arm - and 51 pass, which is the whole point: before that arm existed the swap was a NO-OP against this spec.",
    "file": "shared/config/environment.ts",
    "anchor": "  CORS_ORIGIN: z\n    .string()\n    .trim()\n    .url('CORS origin must be a valid URL')\n",
    "mutation": "  CORS_ORIGIN: z\n    .string()\n    .url('CORS origin must be a valid URL')\n    .trim()\n",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "trims a non-breaking space, which the URL parser leaves in place"
  },
  {
    "id": "cors-origin-normalisation-reaches-process-env",
    "why": "cto/AdaptaLabs#59, raised by the refute gate on !272. zod parses a COPY of process.env and never writes back, so trimming CORS_ORIGIN in the schema fixes config.CORS_ORIGIN - two readers, both backend/src/index.ts - and leaves process.env.CORS_ORIGIN padded for the ELEVEN readers that use it directly: routes/auth.ts x6, routes/userCalendar.ts x2, services/userCalendar.ts x3, every one a redirect target or an OAuth callback URL built by concatenation. The SCHEME half of #59 covers all thirteen transitively, because a bad scheme stops the boot before any of them runs; the PADDING half did not. Measured against real express before the write-back existed: res.redirect(process.env.CORS_ORIGIN) at auth.ts:300 answered 302 with location '%20https://x.com%20', a relative-path redirect to a route that does not exist. The mutation deletes the write-back entirely, which is what a tidy-up removing an apparently pointless self-assignment would do, and the remaining module still builds and still boots. MEASURED on this branch: applied, it fails exactly 1 of 52 in this spec - the named arm - and the two control arms beside it (unset stays unset, an already-clean value comes back byte-identical) stay green, which is what says the write-back narrows a value somebody wrote rather than inventing one.",
    "file": "backend/src/config/index.ts",
    "anchor": "if (process.env.CORS_ORIGIN !== undefined) {\n  process.env.CORS_ORIGIN = config.CORS_ORIGIN;\n}\n",
    "mutation": "",
    "runner": "jest",
    "spec": "src/config/__tests__/environment.test.ts",
    "test": "writes the trimmed origin back, so a padded value cannot reach a redirect"
  },
  {
    "id": "all-bookings-page-reports-has-more-when-there-is-more",
    "why": "cto/AdaptaLabs#66. `GET /api/bookings/my/bookings/all` was an unbounded read whose whole contract is \"all of them, cancelled ones included\", so a bare LIMIT would have converted it into a SILENTLY TRUNCATED one - the caller cannot tell a complete answer from a clipped one, and had no way to ask for the rest. `has_more` is the half that makes the truncation visible, and hardcoding it `false` is the mutation that reinstates the original defect while leaving the page arithmetic, the ordering and the cursor all intact. The route follows the points-history disposition (report `has_more` where the rows are the caller's OWN) rather than the leaderboard clamp or `GET /api/opportunities`' 413, so this is the same guard as `points-history-page-reaches-the-wire` on the sibling surface.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      has_more: hasMore,",
    "mutation": "      has_more: false,",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.all-is-paged.test.ts",
    "test": "reports has_more with a cursor when the probe row comes back"
  },
  {
    "id": "all-bookings-page-reports-has-more-false-when-there-is-not",
    "why": "THE CONTROL ON THE ENTRY ABOVE, and it is not a duplicate of it: `has_more: true` hardcoded satisfies every truncation assertion in the file, and a `has_more` that is ALWAYS TRUE is as useless as one that is always absent - it just fails in the other direction, telling a caller their history continues when it has ended and handing them a cursor that can only come back empty. Both arms are pinned because exactly one arm is what a `has_more` regression looks like. cto/AdaptaLabs#66; the same both-arms rule is written out at gamification.points-history-limit.test.ts:175-177.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      has_more: hasMore,",
    "mutation": "      has_more: true,",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.all-is-paged.test.ts",
    "test": "reports has_more false and no cursor on a short page"
  },
  {
    "id": "all-bookings-keyset-keeps-its-id-tiebreak",
    "why": "cto/AdaptaLabs#66, and the same defect `points-history-keyset-keeps-its-id-tiebreak` pins on the ledger. `bookings.created_at` is not unique - two bookings written by one request share it to the microsecond - so a page boundary falling between two equal timestamps DROPS ONE FOR EVER under `created_at` alone, and nothing anywhere reports it: the response is a well-formed page of the right length with a row missing. Unobservable against the mocked pool jest gives this route, and unobservable against distinct timestamps, which is why the named test seeds the tie ON the first page boundary. The mutation is the shape that keeps `$4` bound so it is a real comparison change rather than a broken statement.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "        AND ($3::timestamptz IS NULL OR (b.created_at, b.id) < ($3::timestamptz, $4::uuid))",
    "mutation": "        AND ($3::timestamptz IS NULL OR (b.created_at < $3::timestamptz AND $4::uuid IS NOT NULL))",
    "runner": "vitest",
    "needsDatabase": true,
    "spec": "src/routes/__tests__/bookings-all-keyset-postgres.test.ts",
    "test": "returns both of two bookings that share a created_at exactly once, across a page boundary"
  },
  {
    "id": "all-bookings-asks-for-one-row-more-than-it-returns",
    "why": "cto/AdaptaLabs#66. Without the `+ 1` probe row, a FULL PAGE and the EXACT END OF HISTORY are the same response, so `has_more` can only be derived from `rows.length === ALL_BOOKINGS_PAGE_SIZE` - which is wrong for the caller who has exactly a page of bookings, and wrong in the direction that INVENTS rows that do not exist and hands out a cursor to reach them. The same line and the same mutation as `points-history-asks-for-one-row-more-than-it-returns` and `LIMIT MAX + 1`, one of the mutations that survived 824 jest and 602 vitest tests on !210.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    `, [userId, ALL_BOOKINGS_PAGE_SIZE + 1, before?.created_at ?? null, before?.id ?? null]);",
    "mutation": "    `, [userId, ALL_BOOKINGS_PAGE_SIZE, before?.created_at ?? null, before?.id ?? null]);",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.all-is-paged.test.ts",
    "test": "asks the database for one row more than a page, and for no more than that"
  },
  {
    "id": "all-bookings-page-size-is-the-number-that-was-decided",
    "why": "cto/AdaptaLabs#66. The page size is a POLICY NUMBER - 100, chosen because this is the flat \"everything I have ever booked\" view rather than a scrolling feed, so one page answers every realistic history in one request while still refusing to grow without limit. A test that derived its expectation from the constant could not see the constant change; three mutations survived exactly that way in one session here, including one that widened a concurrency cap. The named test asserts the LITERAL, so widening the page silently fails by name.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "export const ALL_BOOKINGS_PAGE_SIZE = 100;",
    "mutation": "export const ALL_BOOKINGS_PAGE_SIZE = 500;",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.all-is-paged.test.ts",
    "test": "holds the page size at the number that was decided"
  },
  {
    "id": "all-bookings-cursor-is-rendered-in-utc",
    "why": "cto/AdaptaLabs#66, carrying over the finding `points-history-cursor-is-rendered-in-utc` recorded on #47. `to_char` renders a TIMESTAMPTZ in the SESSION's TimeZone, so without the conversion the cursor is a wall-clock reading in whatever zone the connection happens to carry, labelled `Z` regardless, and fed back into a `$3::timestamptz` that reads a `Z` suffix as UTC. Under a UTC session - CI, and every container this repo starts - the round trip closes by coincidence and nothing notices. The named test therefore runs the whole route suite against a database whose TimeZone is Pacific/Kiritimati (+14), with a control asserting through the APP POOL that the zone really applied; under the mutation the cursor is fourteen hours ahead of the row it describes and the walk repeats its first page until the bounded loop fails by name rather than hanging.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "             to_char(b.created_at AT TIME ZONE 'UTC', 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"') AS cursor_at",
    "mutation": "             to_char(b.created_at, 'YYYY-MM-DD\"T\"HH24:MI:SS.US\"Z\"') AS cursor_at",
    "runner": "vitest",
    "needsDatabase": true,
    "spec": "src/routes/__tests__/bookings-all-keyset-postgres.test.ts",
    "test": "walks the whole booking history with the cursor, with no duplicates and no gaps"
  },
  {
    "id": "rollback-failure-does-not-mask-the-original-error",
    "why": "cto/AdaptaLabs#67. The transaction on POST /api/opportunities/:id/sessions ended `catch { await client.query('ROLLBACK'); throw error; }` with nothing on the ROLLBACK, and a dropped connection is one of the very failures the transaction exists to survive - it makes the ROLLBACK throw too, and THAT error propagates instead of the one that caused it. Damage is diagnostic rather than data: the server aborts the transaction itself when the connection dies, so nothing is half-committed, but the cause is gone from both the response and the log. The mutation is the exact pre-fix line, which is also what a tidy-up deleting an apparently pointless empty catch would produce. This handler's OUTER catch flattens every non-AppError to a flat 500, so the STATUS CODE is 500 either way and is structurally blind to this - the named test therefore asserts on `logger.error`'s payload, which is where the difference actually lands, and its control arm proves the ROLLBACK is still issued on a live connection so the assertion cannot pass against a handler that stopped rolling back. Measured on this branch: applied, it fails exactly 1 of 9 in this spec - the named arm - and the other eight, including the three sibling sites and the negative control, stay green.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "      // Same fix and same reason as routes/sessions.ts:278 (!271).\n      await client.query('ROLLBACK').catch(() => {});",
    "mutation": "      // Same fix and same reason as routes/sessions.ts:278 (!271).\n      await client.query('ROLLBACK');",
    "runner": "jest",
    "spec": "src/routes/__tests__/rollback-failure-does-not-mask-the-original-error.test.ts",
    "test": "POST /api/opportunities/:id/sessions logs the constraint violation, not the dead connection"
  },
  {
    "id": "csv-export-destroys-rather-than-ending-tidily",
    "why": "cto/AdaptaLabs#65. Once the header row of a streamed CSV is written the status is 200 and cannot be taken back, so a batch read that fails part-way through cannot become a 500. What it must NOT do is finish tidily: `res.end()` there hands an admin a shorter CSV that parses perfectly, with nothing on the page or in the file saying it is incomplete - someone computing a total over part of their data and unable to tell. `res.destroy()` makes it a broken download, which is the truth and the only signal left. The mutation is the exact tidy-end a reader would write who saw `destroy` and thought it abrupt. Measured on this branch: applied, it fails exactly 1 of 11 in this spec - the named arm - and the other ten stay green, including the control proving a healthy two-batch export still completes normally.",
    "file": "backend/src/utils/csv-stream.ts",
    "anchor": "    res.destroy();",
    "mutation": "    res.end();",
    "runner": "jest",
    "spec": "src/routes/__tests__/csv-exports-are-streamed.test.ts",
    "test": "destroys the connection when a batch fails part-way through, rather than ending tidily"
  },
  {
    "id": "csv-cell-neutralises-formulas-for-human-authored-text",
    "why": "cto/AdaptaLabs#65. Excel and Google Sheets execute a cell beginning =, +, - or @ as a formula. Before #65 `routes/admin.ts` and `routes/feedback.ts` each carried a PRIVATE `escapeCsvField` that quoted and stopped there, while the survey export next door had already been fixed - so a feedback body of `=HYPERLINK(\"http://evil.test\")`, free text submitted by ANY authenticated user, reached a superadmin's spreadsheet as a live formula. That is untrusted-to-trusted running from the least privileged user in the system to the most. The mutation makes the neutraliser the identity function, which is what deleting an apparently pointless regex looks like. Measured on this branch: applied, it fails 8 of 31 across the two specs named below; the `humanAuthored` FLAG is pinned separately because for a uuid or an ISO timestamp the two branches are provably the same function, so a route-level control could not see it.",
    "file": "backend/src/utils/csv-cell.ts",
    "anchor": "  /^[=+\\-@]/.test(value) ? `\\t${value}` : value;",
    "mutation": "  value;",
    "runner": "jest",
    "spec": "src/utils/__tests__/csv-cell.test.ts",
    "test": "prefixes a value beginning = with a tab"
  },
  {
    "id": "csv-cell-leaves-generated-values-unprefixed",
    "why": "cto/AdaptaLabs#65, and it is the CONTROL for the entry above. \"It was neutralised\" passes just as well against a helper that tab-prefixes EVERY cell - which would stop timestamps being dates and ids being ids in the spreadsheet, the entire reason to export them. This mutation is that over-prefixing. It is pinned at the HELPER rather than through a route on purpose: an earlier version of this control ran through the feedback export and asserted its uuid was unprefixed, and flipping that route's `csvCell(id, false)` to `true` passed 11 of 11, because no uuid or ISO timestamp can begin with =, + or @ and the two branches are therefore the same function there. Equivalent mutant, not a coverage gap - so the branch is asserted where the outputs actually differ. Measured: applied, it fails exactly 1 of 20 in this spec.",
    "file": "backend/src/utils/csv-cell.ts",
    "anchor": "  escapeCsvValue(humanAuthored ? neutraliseCsvFormula(value) : value);",
    "mutation": "  escapeCsvValue(neutraliseCsvFormula(value));",
    "runner": "jest",
    "spec": "src/utils/__tests__/csv-cell.test.ts",
    "test": "does NOT neutralise when the value is one we generated"
  },
  {
    "id": "csv-export-batch-size-is-the-number-that-was-decided",
    "why": "cto/AdaptaLabs#65. The batch size is the bound: it is what turns an unbounded read that materialised a whole table into a walk holding one batch. A test that DERIVES its expectation from the constant cannot see the constant change, so both the unit arm and the two route arms write 500 out as a literal. This mutation halves it, which is what a well-meaning tuning change looks like, and it is caught rather than absorbed. Measured on this branch: applied, it fails exactly 1 of 11 in this spec.",
    "file": "backend/src/utils/csv-stream.ts",
    "anchor": "export const CSV_EXPORT_BATCH_ROWS = 500;",
    "mutation": "export const CSV_EXPORT_BATCH_ROWS = 250;",
    "runner": "jest",
    "spec": "src/routes/__tests__/csv-exports-are-streamed.test.ts",
    "test": "holds the batch size at the number that was decided"
  },
  {
    "id": "bookings-export-keeps-the-uuid-tiebreak-in-its-keyset",
    "why": "cto/AdaptaLabs#65. The bookings export orders by `s.start_time DESC, b.created_at DESC, b.id DESC` and NEITHER timestamp is unique - two people booking the same session in one request share both to the microsecond. Drop `b.id` from the comparison and a batch boundary landing between two equal rows makes the next batch's predicate match nothing, so the walk stops early and the export silently contains one batch instead of the whole table. That is precisely the silent truncation #65 says a bare LIMIT would have caused, reintroduced through the fix. The mutation is the tidier-looking two-column key. Its named test is the real-Postgres walk, because a mocked pool returns whatever it was told to and cannot see this at all.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "                OR (s.start_time, b.created_at, b.id) < ($2::timestamptz, $3::timestamptz, $4::uuid))",
    "mutation": "                OR (s.start_time, b.created_at) < ($2::timestamptz, $3::timestamptz))",
    "runner": "vitest",
    "spec": "src/routes/__tests__/csv-exports-keyset-postgres.test.ts",
    "test": "exports every booking exactly once when the whole key but the uuid is constant",
    "needsDatabase": true
  },
  {
    "id": "feedback-export-keeps-the-uuid-tiebreak-in-its-keyset",
    "why": "cto/AdaptaLabs#65, the same defect on the simpler of the two exports. `feedback.created_at` is not unique, so under `created_at` alone a batch boundary inside a group of equal timestamps ends the walk early and the CSV holds 500 rows instead of every row. The named test seeds 1,200 rows across only THREE distinct timestamps precisely so every boundary lands inside such a group, and it asserts each id appears exactly once rather than only counting - a count alone passes against a walk that repeated one batch and dropped another. Real Postgres, because the predicate is evaluated by the database and by nothing else.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "                OR (created_at, id) < ($1::timestamptz, $2::uuid))",
    "mutation": "                OR created_at < $1::timestamptz)",
    "runner": "vitest",
    "spec": "src/routes/__tests__/csv-exports-keyset-postgres.test.ts",
    "test": "exports every feedback row exactly once across batch boundaries",
    "needsDatabase": true
  },
  {
    "id": "admin-requests-validates-the-query-shape",
    "why": "cto/AdaptaLabs#43. An array-valued ?status walked past `typeof === 'string'` and SILENTLY DROPPED the WHERE filter - superadmin-only and cosmetic, so not a scope widening, but a behaviour nobody chose. Unwiring validateQuery here reopens exactly that, with every other suite green. Proven KILLED before entry: the named arm answers 200 instead of 400 under this mutation.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "router.get('/requests', requireSuperadmin, validateQuery(adminRequestsQuerySchema), asyncHandler(async (req: Request, res: Response) => {",
    "mutation": "router.get('/requests', requireSuperadmin, asyncHandler(async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.query-shapes-are-refused.test.ts",
    "test": "refuses a repeated status without touching the database"
  },
  {
    "id": "google-callback-validates-the-query-shape",
    "why": "cto/AdaptaLabs#43, the UNAUTHENTICATED site. An array ?code is truthy past `if (!code)` and lands in the token exchange - the caller picks their status code on the auth surface, the same shape !253 measured on GET /:id/sessions. Unwiring the validator is one middleware argument and nothing else notices.",
    "file": "backend/src/routes/auth.ts",
    "anchor": "router.get('/google-callback', validateQuery(oauthCallbackQuerySchema), async (req, res) => {",
    "mutation": "router.get('/google-callback', async (req, res) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/auth.google-callback-query-shapes.test.ts",
    "test": "refuses a repeated code with the validator 400"
  },
  {
    "id": "calendar-availability-validates-the-query-shape",
    "why": "cto/AdaptaLabs#43. Before the validator, ?duration_minutes=30&duration_minutes=60 answered 200 - parseInt read the comma-join '30,60' as 30, a value nobody sent - measured live before the fix. This mutation restores that answer while the rest of the suite stays green.",
    "file": "backend/src/routes/calendar.ts",
    "anchor": "router.get('/availability', requireAdmin, validateQuery(calendarAvailabilityQuerySchema), async (req: Request, res: Response) => {",
    "mutation": "router.get('/availability', requireAdmin, async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/calendar.query-shapes-are-refused.test.ts",
    "test": "refuses a repeated duration_minutes without reaching the service"
  },
  {
    "id": "user-calendar-callback-validates-the-query-shape",
    "why": "cto/AdaptaLabs#43. The live risk is an array ?code, which is truthy past the missing-code check and would land in the token exchange; validateQuery refuses the shape at the boundary. The named arm also asserts NO pool connection was taken, so a validator mounted after the handler's first line - which is `pool.connect()` - fails it too. That property matters more since #89 removed `requireAuth` from this route (the app session cookie is SameSite=Strict and cannot ride the redirect back from Google), because the validator is now the FIRST thing an unauthenticated caller meets. The anchor lost its `requireAuth` argument in #89 and this entry went ANCHOR_MISSING in CI - re-pointed, not deleted.",
    "file": "backend/src/routes/userCalendar.ts",
    "anchor": "router.get('/auth/callback', validateQuery(oauthCallbackQuerySchema), asyncHandler(async (req: Request, res: Response) => {",
    "mutation": "router.get('/auth/callback', asyncHandler(async (req: Request, res: Response) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/userCalendar.query-shapes-are-refused.test.ts",
    "test": "refuses a repeated code without taking a database connection"
  },
  {
    "id": "validate-query-actually-parses",
    "why": "cto/AdaptaLabs#43. validateQuery is the mechanism every wired route now leans on, and this mutation turns it into a pass-through that still mounts, still nexts, and still satisfies the structural scan - which proves a mechanism is MENTIONED, not that it runs. Only a behavioural arm can kill it; the nested-object shape is the one that walks past Array.isArray thinking too.",
    "file": "backend/src/validation/schemas.ts",
    "anchor": "      req.query = schema.parse(req.query);",
    "mutation": "      req.query = req.query;",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.query-shapes-are-refused.test.ts",
    "test": "refuses a nested-object status without touching the database"
  },
  {
    "id": "request-id-reflection-holds-its-shape",
    "why": "cto/AdaptaLabs#43's sixth site - a header, not a query. Dropping the shape test reflects any bare string: unbounded length back into a response header, and a CR/LF in the value makes setHeader THROW a 500 out of the logging middleware. The typeof half alone survives every other test in the suite.",
    "file": "backend/src/utils/logger.ts",
    "anchor": "        typeof rawRequestId === 'string' && /^[A-Za-z0-9._-]{1,128}$/.test(rawRequestId)",
    "mutation": "        typeof rawRequestId === 'string'",
    "runner": "jest",
    "spec": "src/utils/__tests__/logger.request-id.test.ts",
    "test": "replaces a value over the length bound with a generated id"
  },
  {
    "id": "admin-requests-authz-runs-before-the-validator",
    "why": "cto/AdaptaLabs#43, a property this MR CREATES: validateQuery sits AFTER requireSuperadmin, so an anonymous hostile-shaped request answers 401 (the gate), not 400 (the validator). Swap the order and the whole 1484-test suite stays green while a pre-auth oracle opens - a 400 confirming the superadmin route exists and naming its parameters to a caller with no role. Proven KILLED before entry: the swap reddens the mount-order arm by name.",
    "file": "backend/src/routes/admin.ts",
    "anchor": "router.get('/requests', requireSuperadmin, validateQuery(adminRequestsQuerySchema), asyncHandler",
    "mutation": "router.get('/requests', validateQuery(adminRequestsQuerySchema), requireSuperadmin, asyncHandler",
    "runner": "jest",
    "spec": "src/routes/__tests__/admin.query-shapes-are-refused.test.ts",
    "test": "answers 401, not the validator 400, for an anonymous hostile-shaped GET /requests"
  },
  {
    "id": "aggregate-results-body-is-bounded-by-open-text-chars",
    "why": "cto/AdaptaLabs#9 option A. The two JSON aggregate results readers buffer every open-text answer verbatim, and surveyAnswerSchema.text has no write-time length cap, so MAX_RESPONSE_ROWS does not bound the body - a few multi-megabyte answers is a few-hundred-MB res.json that OOMs live sessions on the 2Gi pod and stalls the results permit. This entry weakens the character ceiling so an over-limit body is served; the named test asserts the 413, and it is the only thing standing between the reader and the unbounded body. Proven KILLED before entry.",
    "file": "backend/src/firsthand/survey-results-repository.ts",
    "anchor": "        if (totalTextChars > MAX_AGGREGATE_RESPONSE_CHARS) {",
    "mutation": "        if (totalTextChars > MAX_AGGREGATE_RESPONSE_CHARS * 1000) {",
    "runner": "vitest",
    "spec": "src/firsthand/survey-results-repository.test.ts",
    "test": "refuses rather than serving a body past the character ceiling"
  },
  {
    "id": "feedback-list-fetches-the-sentinel-row-that-measures-has-more",
    "why": "cto/AdaptaLabs#81. The route fetches FEEDBACK_LIST_LIMIT + 1 rows and derives has_more from the presence of the extra row. Drop the + 1 and has_more can never be true: the database hands back at most the cap, the sentinel is never seen, and a table past the cap presents as a complete list - the silent truncation the issue exists to prevent, reintroduced with every test of the visible rows still green. Proven KILLED before entry: the parameter-pinning arm fails by name, asserting the bound value 1001 as a literal.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "    [FEEDBACK_LIST_LIMIT + 1]",
    "mutation": "    [FEEDBACK_LIST_LIMIT]",
    "runner": "jest",
    "spec": "src/routes/__tests__/feedback.list-bounded.test.ts",
    "test": "asks the database for the cap plus one rows, as a bound parameter"
  },
  {
    "id": "feedback-list-has-more-is-strict-at-exactly-the-cap",
    "why": "cto/AdaptaLabs#81. has_more compares the fetched row count against the cap with a strict greater-than: at exactly FEEDBACK_LIST_LIMIT rows the sentinel is absent, nothing was cut off, and the flag must be false. Weaken the comparison to >= and a table holding exactly the cap tells the admin rows were truncated when none were - a notice pointing at an export that holds nothing the page does not. Proven KILLED before entry: the exactly-the-cap arm fails by name.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "  const hasMore = result.rows.length > FEEDBACK_LIST_LIMIT;",
    "mutation": "  const hasMore = result.rows.length >= FEEDBACK_LIST_LIMIT;",
    "runner": "jest",
    "spec": "src/routes/__tests__/feedback.list-bounded.test.ts",
    "test": "returns exactly the cap with has_more false when the table holds exactly the cap"
  },
  {
    "id": "feedback-list-keeps-the-sentinel-row-off-the-wire",
    "why": "cto/AdaptaLabs#81. The cap+1th row exists only to measure has_more and must be sliced off before the response. Ship it and the route sends one more row than its stated cap - a lie small enough to survive every eyeball, and the constant the UI copy and the pinning test agree on quietly becomes a different number on the wire. Proven KILLED before entry: the overflow arm fails by name, asserting exactly 1000 rows beside has_more true.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "  const rows = hasMore ? result.rows.slice(0, FEEDBACK_LIST_LIMIT) : result.rows;",
    "mutation": "  const rows = result.rows;",
    "runner": "jest",
    "spec": "src/routes/__tests__/feedback.list-bounded.test.ts",
    "test": "returns exactly the cap with has_more true when the table overflows it"
  },
  {
    "id": "feedback-list-cap-keeps-the-newest-rows",
    "why": "cto/AdaptaLabs#81. A cap without a cursor means anything only because it keeps the NEWEST rows - the UI copy says 'the most recent 1000' and the truncation notice points admins past the cap to the export. Delete the ORDER BY and an unordered LIMIT hands back an arbitrary 1000 rows: silent truncation of an unpredictable subset, the exact failure the issue exists to stop, and the review gate measured the whole 1497-test suite green under it. Proven KILLED before entry: the bound-parameter arm now asserts the clause and fails by name.",
    "file": "backend/src/routes/feedback.ts",
    "anchor": "     ORDER BY created_at DESC, id DESC\n     LIMIT $1`,",
    "mutation": "     LIMIT $1`,",
    "runner": "jest",
    "spec": "src/routes/__tests__/feedback.list-bounded.test.ts",
    "test": "asks the database for the cap plus one rows, as a bound parameter"
  },
  {
    "id": "csv-writer-timeout-rejects-rather-than-resolves",
    "why": "cto/AdaptaLabs#80. A drain timeout that RESOLVES lets the loop carry on to the next batch and reach res.end() - a short CSV that parses, handed to a reader with no way to tell. Rejecting routes it into the caller's catch, which destroys the socket, and the broken download is the truth. Guards both exports since the #80 fold. Proven KILLED before entry: the never-drains arm of the admin mechanics suite fails by name, measured as this MR's null control before the extraction and re-measured against the shared module after it.",
    "file": "backend/src/utils/bounded-socket-write.ts",
    "anchor": "      const onTimeout = done(() =>\n        reject(\n          makeTimeoutError(\n            remainingBudgetMs() <= 0 ? 'export_deadline' : 'drain_timeout'\n          )\n        )\n      );",
    "mutation": "      const onTimeout = done(() => resolve());",
    "runner": "jest",
    "spec": "src/utils/__tests__/csv-stream.mechanics.test.ts",
    "test": "destroys within the drain budget when the socket never drains"
  },
  {
    "id": "csv-writer-clamps-a-late-drain-wait-to-the-deadline",
    "why": "cto/AdaptaLabs#80 follow-through from the security gate on the fold: the drain timer is armed with min(drain budget, remaining export budget), and dropping the clamp lets the LAST stalled write of a long export overshoot the whole-export deadline by up to the full drain budget - on the survey caller that is the only results-read permit held past the ceiling it was sized against. The gate proved the admin caller's side of this seam had no guard at all: a never-expiring budget closure survived 1501 jest and 715 vitest tests. Proven KILLED before entry: the late-stall arm walks the clock to ten seconds short of the deadline and fails by name under both the clamp removal and the never-expiring-closure mutation.",
    "file": "backend/src/utils/bounded-socket-write.ts",
    "anchor": "        Math.min(drainTimeoutMs, budgetMs)",
    "mutation": "        drainTimeoutMs",
    "runner": "jest",
    "spec": "src/utils/__tests__/csv-stream.mechanics.test.ts",
    "test": "lets the deadline win a late stall instead of granting the full drain budget"
  },
  {
    "id": "google-callback-validates-oauth-state",
    "why": "cto/AdaptaLabs#82. GET /auth/google-callback must enforce the browser-bound OAuth state before minting a session; the check lives in consumeOAuthState and the callback must ACT on its result. This mutation calls the helper for its side effects but drops the guard, so a forged callback (attacker's code, no matching cookie) proceeds to a 302 + session cookie - the login-CSRF the fix closes. Proven KILLED before entry against the production path.",
    "file": "backend/src/routes/auth.ts",
    "anchor": "    const stateResult = consumeOAuthState(req, res, state);\n    if (!stateResult.ok) {\n      logger.error('OAuth state validation failed', { reason: stateResult.error });\n      return res.status(400).json({ error: stateResult.error });\n    }",
    "mutation": "    consumeOAuthState(req, res, state);",
    "runner": "jest",
    "spec": "src/routes/__tests__/auth.google-callback-state.test.ts",
    "test": "refuses a forged callback with NO state and creates no session"
  },
  {
    "id": "google-callback-consumes-oauth-state-once",
    "why": "cto/AdaptaLabs#82's replay half. RE-POINTED by cto/AdaptaLabs#92: the old anchor was `stateData.used = true` on a per-process Map, and that Map is gone - state is now sealed into the browser cookie so any pod can complete a callback. Single use is now recorded here instead. Delete this line and a state re-presented WITH its original cookie is accepted a second time (session fixation). The named test isolates that from the cookie-clearing by re-sending the state cookie, so only this line can refuse the replay. The entry went ANCHOR_MISSING in CI when the rewrite landed, which is the manifest doing its job: a moved security line is re-pointed, never deleted.",
    "file": "backend/src/utils/oauthState.ts",
    "anchor": "      spent.set(envelope.n, Date.now());",
    "mutation": "",
    "runner": "jest",
    "spec": "src/routes/__tests__/auth.google-callback-state.test.ts",
    "test": "refuses a consumed state even when the browser re-sends the state cookie"
  },
  {
    "id": "oauth-state-is-browser-bound",
    "why": "cto/AdaptaLabs#82, the login-CSRF fix itself. RE-POINTED by cto/AdaptaLabs#92: the binding used to be `state !== bound` against a value in a process-global store; the state now travels AES-256-GCM sealed in the cookie, so the binding starts by REQUIRING that cookie. Drop the presence check (keeping `state` referenced so it still builds) and a callback carrying an attacker-minted state reaches the flow from a browser that never began it - the exploit the gate demonstrated, logging the victim into the attacker's identity. Killed by hand twice before this re-point. The sibling entry `oauth-state-is-bound-to-the-nonce-in-the-cookie` guards the OTHER half, the nonce comparison; both halves are load-bearing and neither implies the other.",
    "file": "backend/src/utils/oauthState.ts",
    "anchor": "      if (typeof state !== 'string' || !sealed) {",
    "mutation": "      if (typeof state !== 'string') {",
    "runner": "jest",
    "spec": "src/routes/__tests__/auth.google-callback-state.test.ts",
    "test": "refuses an attacker-minted state replayed from a different browser"
  },
  {
    "id": "oauth-state-cookie-keeps-the-host-prefix",
    "why": "cto/AdaptaLabs#94. Since !299 the calendar callback takes its userId from the state's server-side payload rather than the session, which makes this cookie the SOLE authenticator. Without the __Host- prefix any sibling *.adaptavist.net host can set Domain=.adaptavist.net and toss a state in; the victim then completes consent against the attacker's state and THEIR Google refresh token lands on the ATTACKER's user_calendar_tokens row, readable via GET /api/calendar/my-events. This is the mutation to fear rather than dropping `secure` or adding a `domain`: those make the browser refuse the cookie, so the flow breaks loudly, whereas removing the prefix leaves every flow working exactly as before and silently reopens the hole. The prefix is applied here, once, for every flow. Proven KILLED before entry: the named test reports \"adaptalabs_oauth_state\" against the expected \"__Host-adaptalabs_oauth_state\".",
    "file": "backend/src/utils/oauthState.ts",
    "anchor": "  const cookieName = `${HOST_COOKIE_PREFIX}${options.cookieName}`;",
    "mutation": "  const cookieName = options.cookieName;",
    "runner": "jest",
    "spec": "src/utils/__tests__/oauthState.test.ts",
    "test": "prefixes the cookie name with __Host-, so a subdomain cannot toss one in"
  },
  {
    "id": "session-cookie-keeps-the-host-prefix",
    "why": "cto/AdaptaLabs#97, the session half of #94. In production adaptalabs_session is host-only with no Domain, so any sibling *.adaptavist.net host can set Domain=.adaptavist.net and toss a duplicate; `cookie` parses a duplicate name first-wins, so the planted cookie shadows the victim's post-login cookie indefinitely - a denial-of-login (the fixation escalation is separately blocked by req.session.regenerate() on both login paths). The __Host- prefix makes a sibling unable to express a cookie that lands here (it needs a Domain, which the prefix forbids). The prefix is CONDITIONAL, unlike #94's, because express-session refuses to set a Secure cookie over http, so a __Host- name outside production would emit no cookie and break local login; the sibling-toss threat is production-origin only. This is the mutation to fear rather than dropping `secure`: that breaks the flow loudly, whereas dropping the prefix leaves every flow working and silently reopens the hole. Proven KILLED before entry: with the prefix dropped, sessionCookieName('production') returns \"adaptalabs_session\" against the expected \"__Host-adaptalabs_session\".",
    "file": "backend/src/utils/hostCookie.ts",
    "anchor": "  return isSecureCookieEnv(nodeEnv) ? `${HOST_COOKIE_PREFIX}${SESSION_COOKIE_BASENAME}` : SESSION_COOKIE_BASENAME;",
    "mutation": "  return SESSION_COOKIE_BASENAME;",
    "runner": "jest",
    "spec": "src/utils/__tests__/hostCookie.test.ts",
    "test": "prefixes the session cookie with __Host- in production so a sibling subdomain cannot toss one in"
  },
  {
    "id": "csrf-cookie-keeps-the-host-prefix",
    "why": "cto/AdaptaLabs#97, the CSRF half. adaptalabs_csrf is the same host-only shape as the session cookie and tossable the same way; a shadowed CSRF cookie desyncs the double-submit pair and denies mutating requests. The prefix is keyed off csrf-csrf's secureCookies flag (itself NODE_ENV==='production') rather than being unconditional, because a __Host- name requires Secure and supertest's cookie jar silently drops Secure cookies over http - an unconditional prefix would break the double-submit round-trip in tests. Dropping the prefix is the silent regression: the cookie keeps working while a sibling regains the ability to toss one. Proven KILLED before entry: with the prefix dropped, csrfCookieName(true) returns \"adaptalabs_csrf\" against the expected \"__Host-adaptalabs_csrf\".",
    "file": "backend/src/utils/hostCookie.ts",
    "anchor": "  return secureCookies ? `${HOST_COOKIE_PREFIX}${CSRF_COOKIE_BASENAME}` : CSRF_COOKIE_BASENAME;",
    "mutation": "  return CSRF_COOKIE_BASENAME;",
    "runner": "jest",
    "spec": "src/utils/__tests__/hostCookie.test.ts",
    "test": "prefixes the CSRF cookie with __Host- when cookies are secure so a sibling subdomain cannot toss one in"
  },
  {
    "id": "results-read-firsthand-aggregate-releases-before-json",
    "why": "cto/AdaptaLabs#85, the residual availability half of #9. GET /studies/:studyId/results buffered its res.json body and held the single results permit through the whole drain, which a slow reader controls - denying another admin's aggregate reads. !288 bounded the body so early release is now safe. This mutation drops releaseResultsReadPermit(res) before res.json, reverting to permit-held-through-drain; caught by the all-four structural test. Anchored on the #85 comment+call so it is unique against the CSV route's release in the same file.",
    "file": "backend/src/routes/firsthand.ts",
    "anchor": "  // #85 (residual of #9): read and aggregation done, and !288 bounded the\n  // aggregate body (MAX_AGGREGATE_RESPONSE_CHARS), so hand the global permit back\n  // BEFORE the client-paced `res.json` drain - the same lever the .csv twin\n  // below took out of the permit. The per-caller slot (held to `close`) bounds\n  // concurrent buffered bodies. See releaseResultsReadPermit.\n  releaseResultsReadPermit(res);",
    "mutation": "  // #85 (residual of #9): read and aggregation done, and !288 bounded the\n  // aggregate body (MAX_AGGREGATE_RESPONSE_CHARS), so hand the global permit back\n  // BEFORE the client-paced `res.json` drain - the same lever the .csv twin\n  // below took out of the permit. The per-caller slot (held to `close`) bounds\n  // concurrent buffered bodies. See releaseResultsReadPermit.",
    "runner": "jest",
    "spec": "src/routes/__tests__/runtime-pool-admission-routes.test.ts",
    "test": "releases the permit early on all four results routes"
  },
  {
    "id": "results-read-opportunity-aggregate-releases-before-json",
    "why": "cto/AdaptaLabs#85, the residual availability half of #9. GET /:id/survey-results buffered its res.json body and held the single results permit through the whole drain (slow-reader controlled), denying another admin's aggregate reads. !288 bounded the body so early release is now safe. This mutation drops releaseResultsReadPermit(res) before res.json; caught by the all-four structural test. Anchored on the #85 comment+call, unique against the CSV release in the same file.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "  // #85 (residual of #9): the DB read and aggregation are done, and !288 bounded\n  // the aggregate body (MAX_AGGREGATE_RESPONSE_CHARS), so hand the global permit\n  // back BEFORE the client-paced `res.json` drain - the same lever the .csv twin\n  // below took out of the permit. The per-caller slot (held to `close`) bounds\n  // concurrent buffered bodies. See releaseResultsReadPermit.\n  releaseResultsReadPermit(res);",
    "mutation": "  // #85 (residual of #9): the DB read and aggregation are done, and !288 bounded\n  // the aggregate body (MAX_AGGREGATE_RESPONSE_CHARS), so hand the global permit\n  // back BEFORE the client-paced `res.json` drain - the same lever the .csv twin\n  // below took out of the permit. The per-caller slot (held to `close`) bounds\n  // concurrent buffered bodies. See releaseResultsReadPermit.",
    "runner": "jest",
    "spec": "src/routes/__tests__/runtime-pool-admission-routes.test.ts",
    "test": "releases the permit early on all four results routes"
  },
  {
    "id": "moderated-consent-is-for-moderated-types",
    "why": "cto/AdaptaLabs#79 step 1a. Consent fields on an opportunity are for the two moderated types and nothing else; resolveModeratedConsentWrite's type gate is the single point refusing them everywhere else, on all five write surfaces (create x2 branches, PATCH x2, duplicate). Dropping it lets a consent_text be stored on an unmoderated or survey opportunity - a row whose participants are governed by the STUDY's consent, so a second, contradictory consent home appears silently and nothing else notices: proven by mutation before entry, this exact if->false left every suite green except the named test, which fails 200-vs-400 asserting the exported sentence.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "  if (!MODERATED_CONSENT_TYPES.has(effectiveType)) {",
    "mutation": "  if (false) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.moderated-consent.test.ts",
    "test": "refuses consent_text on a poll PATCH, by the exact sentence"
  },
  {
    "id": "oidc-callback-validates-the-query-shape",
    "why": "cto/AdaptaLabs#84. GET /auth/callback reads its query through oidcClient.callbackParams(req), which parses req.url and yields arrays for repeated parameters - invisible to #43's req.query-read scan. validateQuery refuses a non-string code/state SHAPE with 400 before the handler, so an array never reaches the token exchange. Unwiring the validator lets a repeated code through to the handler's own 500 instead of the validator's 400. Proven KILLED before entry.",
    "file": "backend/src/routes/auth.ts",
    "anchor": "router.get('/callback', validateQuery(oauthCallbackQuerySchema), async (req, res) => {",
    "mutation": "router.get('/callback', async (req, res) => {",
    "runner": "jest",
    "spec": "src/routes/__tests__/auth.oidc-callback-query-shapes.test.ts",
    "test": "refuses a repeated code with the validator 400"
  },
  {
    "id": "log-redaction-matches-the-decoded-parameter-name",
    "why": "cto/AdaptaLabs#84. redactSensitiveUrl must match sensitive parameter names on the DECODED name: qs reads ?%63ode= as `code`, so a raw-string match leaks the value. This mutation stops decoding the name (decodedName = rawName), so a percent-encoded name is no longer recognised and the credential reaches the log. Proven KILLED before entry.",
    "file": "backend/src/utils/logger.ts",
    "anchor": "      let decodedName: string;\n      try {\n        decodedName = decodeURIComponent(rawName.replace(/\\+/g, ' '));\n      } catch {\n        decodedName = rawName;\n      }",
    "mutation": "      const decodedName: string = rawName;",
    "runner": "jest",
    "spec": "src/utils/__tests__/logger.test.ts",
    "test": "redacts a percent-encoded parameter name"
  },
  {
    "id": "log-redaction-matches-the-bracket-base-name",
    "why": "cto/AdaptaLabs#84. qs reads ?code[0]=/code[foo]= as the `code` parameter, so redaction must match the base name before any '['. This mutation drops the split('['), so bracket notation is no longer recognised and the value leaks to the log. Proven KILLED before entry.",
    "file": "backend/src/utils/logger.ts",
    "anchor": "      const baseName = decodedName.split('[')[0].toLowerCase();",
    "mutation": "      const baseName = decodedName.toLowerCase();",
    "runner": "jest",
    "spec": "src/utils/__tests__/logger.test.ts",
    "test": "redacts a bracket-notation parameter name"
  },
  {
    "id": "researcher-notes-write-consults-ownership",
    "why": "cto/AdaptaLabs#79. PUT /:bookingId/notes writes a researcher's free-text judgement about a named colleague onto their booking. This mutation opens the write to ANY live-role admin on any opportunity. The named test asserts both the 403 and that no UPDATE ran; under the mutation the handler answers 200 and runs the UPDATE. Proven KILLED before entry.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "  if (!isSuperadmin && !isOpportunityOwner(bookingResult.rows[0], req.user)) {\n    throw new ForbiddenError('You can only edit notes for your own studies');\n  }",
    "mutation": "  if (false) {\n    throw new ForbiddenError('You can only edit notes for your own studies');\n  }",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.researcher-notes.test.ts",
    "test": "refuses a researcher_admin who does not own the opportunity, without writing"
  },
  {
    "id": "researcher-notes-write-reads-live-role",
    "why": "cto/AdaptaLabs#79, same liveness family as #14: the role that decides is the one in the users table now, not the one snapshotted into the session at login. This mutation keeps the existence check and drops the role check, so a demoted-to-employee session with a stale admin claim writes notes about a colleague. The named test's session claims researcher_admin while the live-role query answers employee. Proven KILLED before entry.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "  if (userResult.rows.length === 0 || !isAdminRole(userResult.rows[0].role)) {\n    throw new ForbiddenError('Only admins can edit researcher notes');\n  }",
    "mutation": "  if (userResult.rows.length === 0) {\n    throw new ForbiddenError('Only admins can edit researcher notes');\n  }",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.researcher-notes.test.ts",
    "test": "refuses a caller whose LIVE role is employee, whatever the session says"
  },
  {
    "id": "researcher-notes-ceiling-is-pinned",
    "why": "cto/AdaptaLabs#79. The notes ceiling is policy: refuse over it, never truncate. A test deriving its expectation from the constant cannot see the constant change (three mutations survived exactly that way on 2026-08-22), so the ceiling is pinned as a LITERAL in both the backend suite and the frontend textarea maxlength test. This mutation widens the ceiling 1000x; the backend literal pin fails by name. Proven KILLED before entry.",
    "file": "shared/constants/index.ts",
    "anchor": "  MAX_RESEARCHER_NOTES_CHARS: 20000,",
    "mutation": "  MAX_RESEARCHER_NOTES_CHARS: 20000000,",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.researcher-notes.test.ts",
    "test": "pins the ceiling at 20000 characters"
  },
  {
    "id": "researcher-notes-write-superadmin-half-reads-live-role",
    "why": "cto/AdaptaLabs#79. The ADMIN half of this route's liveness check has its own entry; the SUPERADMIN half had none, and a refute gate measured the gap: deciding `isSuperadmin` from `req.user!.role` - the LOGIN SNAPSHOT, since the route sits on `requireAuth` alone - passed all 1537 backend tests while letting a superadmin demoted since login write notes on any opportunity for the life of their session. Same family as the `authenticate.ts:70` finding. Proven KILLED before entry.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "  const isSuperadmin = userResult.rows[0].role === 'superadmin';\n  if (!isSuperadmin && !isOpportunityOwner(bookingResult.rows[0], req.user)) {",
    "mutation": "  const isSuperadmin = req.user!.role === 'superadmin';\n  if (!isSuperadmin && !isOpportunityOwner(bookingResult.rows[0], req.user)) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.researcher-notes.test.ts",
    "test": "refuses a caller whose LIVE role dropped from superadmin, on an opportunity they do not own"
  },
  {
    "id": "opportunity-bookings-roster-returns-the-note",
    "why": "cto/AdaptaLabs#79's READ half. The write is heavily pinned; the read was not, and a refute gate proved it: making this roster answer `researcher_notes: null` passed 93 suites / 1537 tests. The sibling route's own test file campaigns for replacing `SELECT b.*` with explicit column lists, so a future narrowing here would render empty boxes over stored notes - and a researcher typing into one would overwrite what was there. Proven KILLED before entry.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      researcher_notes_updated_at: booking.researcher_notes_updated_at\n        ? booking.researcher_notes_updated_at.toISOString()\n        : null,",
    "mutation": "      researcher_notes: null,\n      researcher_notes_updated_at: booking.researcher_notes_updated_at\n        ? booking.researcher_notes_updated_at.toISOString()\n        : null,",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.opportunity-bookings-ownership.test.ts",
    "test": "returns the researcher note and its timestamp to the owner"
  },
  {
    "id": "my-bookings-projection-excludes-researcher-columns",
    "why": "cto/AdaptaLabs#79. `GET /api/bookings/my/bookings` is the route on which this repo ALREADY shipped `admin_notes` to the participant it was about (learnings.md:349). Its sibling has been pinned since #54; this one was not, and a refute gate appended `b.researcher_notes` to `participantBookingColumns` and measured 93 suites / 1537 passing. This mutation is that exact widening. Proven KILLED before entry, after the response map was also narrowed from a spread to named fields so the class is closed at both layers.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      b.id, b.user_id, b.session_id, b.status, b.completion_status, b.completed_at,\n      b.cancelled_at, b.gcal_event_id, b.reminder_sent_at, b.created_at, b.updated_at",
    "mutation": "      b.id, b.user_id, b.session_id, b.status, b.completion_status, b.completed_at,\n      b.cancelled_at, b.gcal_event_id, b.reminder_sent_at, b.created_at, b.updated_at,\n      b.researcher_notes",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.my-bookings-projection.test.ts",
    "test": "narrows at the PROJECTION, which is the outer of the two guards"
  },
  {
    "id": "calendar-callback-takes-its-user-from-the-state",
    "why": "cto/AdaptaLabs#89. GET /api/calendar/auth/callback has NO requireAuth, deliberately: it is entered by a top-level navigation redirected from accounts.google.com, and the app session cookie is SameSite=Strict in production, so it is withheld across that redirect chain - requireAuth would 401 the researcher AFTER they granted Google access. The OAuth state carries the initiating user's id server-side and is the sole authority for whose calendar this is. This mutation prefers the session when one happens to be present, which is how a flow started by one researcher could be written against whoever's session arrives. Proven KILLED before entry: the named test drives a state minted for researcher-7 through an app whose session is someone-else-9 and asserts the INSERT parameters carry researcher-7. That test exists because this exact mutation SURVIVED first time round - in every other test the session and the state agree, so nothing could tell them apart.",
    "file": "backend/src/routes/userCalendar.ts",
    "anchor": "      userId = stateResult.payload.userId;",
    "mutation": "      userId = req.session?.user?.id ?? stateResult.payload.userId;",
    "runner": "jest",
    "spec": "src/routes/__tests__/userCalendar.connect-flow.test.ts",
    "test": "writes against the STATE's user even when a different session is present"
  },
  {
    "id": "migration-nulls-only-the-fabricated-event-ids",
    "why": "cto/AdaptaLabs#89. Until this release CalendarService.createEvent simulated its writes and the booking route stored the fabricated `demo-event-<now>` id in bookings.gcal_event_id, so production rows record Google events that never existed - and cancel and reschedule are both gated on that column being truthy, so each chases a phantom. This migration nulls them. The mutation drops the WHERE predicate so it nulls EVERY id on every boot. That is harmless while no stored id is real, which is exactly why nothing could see it: a gate proved both deleting the statement and deleting only its WHERE passed 1553 jest and 716 vitest. Once the per-user OAuth work lands and real Google ids exist it is silent data loss on every pod restart. Proven KILLED before entry against a real Postgres 17: 6 of 7 arms fail, the named one first.",
    "file": "backend/src/db/migrate.ts",
    "anchor": "      UPDATE bookings\n      SET gcal_event_id = NULL\n      WHERE gcal_event_id LIKE 'demo-event-%'",
    "mutation": "      UPDATE bookings\n      SET gcal_event_id = NULL",
    "runner": "vitest",
    "spec": "src/db/__tests__/migrate-nulls-only-fabricated-event-ids-postgres.test.ts",
    "test": "LEAVES A REAL GOOGLE EVENT ID ALONE",
    "needsDatabase": true
  },
  {
    "id": "calendar-connect-is-rate-limited",
    "why": "cto/AdaptaLabs#89. GET /api/calendar/auth/connect MINTS OAuth state, and every call allocates a store entry living up to one TTL on a single-replica pod - measured at 173 bytes each - while /api carries no limiter of its own. A security gate proved the limiter was a fix nothing enforced: deleting it from the route passed all 1582 backend tests. This repo's standard for a limiter (routes/firsthand.ts) is a named test PLUS a canary entry, and this is the second half. Proven KILLED before entry: the eleventh call in a minute returns 302 instead of 429.",
    "file": "backend/src/routes/userCalendar.ts",
    "anchor": "router.get('/auth/connect', requireAuth, calendarConnectLimiter, asyncHandler",
    "mutation": "router.get('/auth/connect', requireAuth, asyncHandler",
    "runner": "jest",
    "spec": "src/routes/__tests__/userCalendar.connect-flow.test.ts",
    "test": "refuses the eleventh attempt in a minute with the route's own message"
  },
  {
    "id": "calendar-callback-takes-its-pool-connection-late",
    "why": "cto/AdaptaLabs#89. The callback is unauthenticated by design (the app session cookie is SameSite=Strict and cannot ride the redirect back from Google), so `pool.connect()` as its first line gave any anonymous caller a pool checkout before any refusal - and the connection was then HELD across the outbound token exchange, which has no AbortSignal. `pool.options.max` is pg's default 10 and POOL_STATEMENT_TIMEOUT_MS bounds queries rather than checkouts, so ten stalled flows wedge the whole backend rather than just this route. routes/auth.ts already orders it state-check, exchange, connect. Proven KILLED before entry: the refusal path takes a connection again.",
    "file": "backend/src/routes/userCalendar.ts",
    "anchor": "  let dbClient: PoolClient | undefined;",
    "mutation": "  let dbClient: PoolClient | undefined = await pool.connect();",
    "runner": "jest",
    "spec": "src/routes/__tests__/userCalendar.connect-flow.test.ts",
    "test": "takes NO pool connection when it refuses"
  },
  {
    "id": "demo-calendar-conflicts-do-not-move",
    "why": "The demo calendar is a fixture and its whole value is being the same twice. It was built with Math.random() per request, so the slot picker reshuffled which slots it dimmed on every page load and a researcher could not tell a demo fixture from a calendar that had genuinely changed (cto/AdaptaLabs#96). Re-introducing variety here is an easy and plausible future edit, and it reads as an improvement.",
    "file": "backend/src/services/userCalendar.ts",
    "anchor": "      { hour: 10, minute: 0, durationMinutes: 60, title: 'Team Standup' },",
    "mutation": "      { hour: 9 + Math.floor(Math.random() * 6), minute: 0, durationMinutes: 60, title: 'Team Standup' },",
    "runner": "jest",
    "spec": "src/services/__tests__/userCalendar.demo-events-are-deterministic.test.ts",
    "test": "returns the identical set of events for the same user and window, ten times over"
  },
  {
    "id": "task-window-refuses-an-unsafe-target",
    "why": "The task page is opened as window.open(\"\") - a same-origin about:blank - and then navigated by assigning location.href, so a javascript: target would execute in Cortex's own origin with the participant's session token and stored responses. This is the SINK, the last of four places isSafeTargetUrl is enforced and the one a refactor is most likely to remove as redundant. It rested on exactly one test.",
    "file": "frontend/src/lib/recording/task-window.ts",
    "anchor": "if (!isSafeTargetUrl(url)) {",
    "mutation": "if (!url) {",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/lib/recording/task-window.test.ts",
    "test": "refuses an unsafe target without opening anything"
  },
  {
    "id": "authoring-form-refuses-an-unsafe-target-url",
    "why": "The first of four places isSafeTargetUrl is enforced, and the one a researcher actually meets: without it an unsafe target_url is accepted by the authoring form and only refused deeper in, or not at all if a later schema is widened. Like the sink, it rested on exactly one test - against six and seven at the two schema layers.",
    "file": "frontend/src/pages/OpportunityForm.tsx",
    "anchor": "if (targetUrl && !isSafeTargetUrl(targetUrl)) {",
    "mutation": "if (false) {",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/pages/__tests__/OpportunityForm.test.tsx",
    "test": "sends the starting url, and blocks one that could run against the session"
  },
  {
    "id": "booking-consent-acceptance-gate",
    "why": "cto/AdaptaLabs#79 step 1b. An opportunity carrying consent wording must refuse a booking whose body does not carry explicit acceptance - the gate reads consent off the SAME locked row as every other booking guard, and it is the only thing standing between a participant and a booked live session whose consent they never saw. Dropping it books every session as if no consent existed and writes NO acceptance record, so step 2's artefact ingest gate then reads an absence that looks identical to a pre-consent booking: proven by mutation before entry, this exact if->false left every suite green except the three named refusal tests, which fail 400-vs-201 asserting the exported sentence.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "    if (consentText && req.body?.consent_accepted !== true) {",
    "mutation": "    if (false) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.consent-acceptance.test.ts",
    "test": "refuses a body without acceptance, by the exact sentence, before any INSERT"
  },
  {
    "id": "booking-consent-echo-gate",
    "why": "cto/AdaptaLabs#79 step 1b. Acceptance must be OF the wording the participant read: the client echoes the displayed text and this comparison refuses a booking whose echo differs from the locked row's trimmed wording. Dropping it re-opens the accept-what-you-saw race a review gate named - a researcher edit between page load and Accept records acceptance with a hash asserting the participant agreed to wording they never saw, which is exactly the falsehood the snapshot exists to prevent. Proven by mutation before entry: this exact comparison->false fails the named test 201-vs-400 asserting the exported CONSENT_WORDING_CHANGED sentence, everything else green.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "      if (seen !== consentText) {",
    "mutation": "      if (false) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.consent-acceptance.test.ts",
    "test": "refuses an acceptance whose echoed wording differs, by its own sentence"
  },
  {
    "id": "booking-artifact-consent-gate",
    "why": "cto/AdaptaLabs#79 step 2. Ingesting a recording or transcript of a moderated session requires recorded consent acceptance on the booking OR an explicit typed attestation that consent was obtained outside Cortex (decision D3, narrowed by F13). This refusal is the single point enforcing that at BOTH presign and finalize - resolveConsentGate is the shared gate. Dropping it stores research recordings of real participants with no consent record and no attestation, silently, which is the governance gap #79 exists to close. Proven by mutation before entry: this exact refusal->return-null left every suite green except the two named refusal tests; the named one fails 200-vs-400 asserting the exported ARTIFACT_CONSENT_REQUIRED sentence.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "    throw new ValidationError(ARTIFACT_CONSENT_REQUIRED);",
    "mutation": "    return null;",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "refuses presign with neither, by the exact sentence"
  },
  {
    "id": "booking-artifact-ownership-gate",
    "why": "cto/AdaptaLabs#79 step 3 (gate created in step 2, pinned here with the media route). loadOwnedBooking is the single owner-or-superadmin gate ALL five booking-artifact routes share - presign, finalize, list, delete and the media route; this line is where it refuses. Neutering it lets any researcher_admin ingest, list, stream and delete recordings of another researcher's participants - a cross-tenant participant-data leak on every route at once. Proven by mutation before entry: appending '&& false' to the condition (compiles, gate never fires) failed exactly the five named ownership tests, one per route, each asserting the exported refusal sentence.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "  if (!isSuperadmin && !isOpportunityOwner(booking, req.user)) {",
    "mutation": "  if (!isSuperadmin && !isOpportunityOwner(booking, req.user) && false) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "presign refuses a researcher who does not own the opportunity"
  },
  {
    "id": "booking-artifact-media-scope-binding",
    "why": "cto/AdaptaLabs#79 step 3, added on a review gate's demonstration: with the artefact lookup's booking scope-binding deleted, any admin who owns ONE booking can stream any artefact in the system by artefact id - loadOwnedBooking only proves ownership of the booking in the PATH. The gate proved the original test vacuous (the fixture's null did the refusing, not the clause); the suite's pool mock now honours parameters AND the named test pins the clause by TEXT, because a mutated SQL still receives both params so only the text can see the clause go. Proven by mutation before entry: this exact narrowing failed only the named test, asserting the WHERE clause string.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "      WHERE a.id = $1 AND a.booking_id = $2",
    "mutation": "      WHERE a.id = $1",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "scope-binds the artefact to the booking in the path - 404, and storage never consulted"
  },
  {
    "id": "recording-media-content-disposition-encoded",
    "why": "cto/AdaptaLabs#79 step 3, added after a re-gate proved the fix one line from a no-op: reverting this call site to the raw `inline; filename=\"${asset.fileName}\"` interpolation left the FULL backend green (743 vitest + 1707 jest, identical totals to the fixed tree) while reintroducing the defect - undici header values are Latin-1, so any stored file name with a codepoint above U+00FF (CJK, Cyrillic, emoji; BOTH media paths store the raw client/researcher name) throws at serve and the recording is a permanent generic 500. The encoder's 14 unit tests cannot see the call site; this entry pins the WIRING. Proven by mutation before entry: this exact revert compiles and fails only the named test, which compares the response header against buildInlineContentDisposition's output.",
    "file": "backend/src/firsthand/runtime-object-storage-s3.ts",
    "anchor": "    \"Content-Disposition\": buildInlineContentDisposition(asset.fileName),",
    "mutation": "    \"Content-Disposition\": `inline; filename=\"${asset.fileName}\"`,",
    "runner": "vitest",
    "spec": "src/firsthand/__tests__/content-disposition.test.ts",
    "test": "puts the ENCODED Content-Disposition on the response, never the raw name"
  },
  {
    "id": "nginx-entry-document-is-no-cache",
    "why": "index.html names content-hashed chunks that stop existing at the next deploy. Served with an etag and no cache-control, browsers apply heuristic freshness and reuse a stale copy, so an open tab 404s on its own chunks after every release (cto/AdaptaLabs#91). This header is the whole fix, nothing executes nginx in any suite, and the symptom only appears in production one deploy later.",
    "file": "frontend/nginx.conf",
    "anchor": "        add_header Cache-Control \"no-cache\" always;",
    "mutation": "",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/__tests__/nginx-entry-document-cache.test.ts",
    "test": "serves the SPA fallback with Cache-Control no-cache"
  },
  {
    "id": "nginx-spa-block-restates-security-headers",
    "why": "nginx does not inherit server-level add_header into a location block that declares its own, so the moment location / gained Cache-Control it had to restate all four security headers. A tidy-up that trims the 'duplicates' strips nosniff and frame protection from every SPA route with everything else green - the exact silent loss the version.json block documents.",
    "file": "frontend/nginx.conf",
    "anchor": "        add_header Cache-Control \"no-cache\" always;\n        add_header X-Frame-Options \"SAMEORIGIN\" always;",
    "mutation": "        add_header Cache-Control \"no-cache\" always;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/__tests__/nginx-entry-document-cache.test.ts",
    "test": "restates all four security headers beside the Cache-Control it declares"
  },
  {
    "id": "test-port-probe-verifies-the-v4-loopback",
    "why": "The whole reservoir rests on the probe binding 127.0.0.1 exactly: the kernel then only hands out ports free on the address supertest dials. A probe that bound the wildcard would verify ports whose v4 side limactl or a leftover postgres owns - measured 2026-08-31 at 3000 of 20000 wildcard binds landing on squatted ports against 0 of 20000 loopback binds - which is the cto/AdaptaLabs#44 flake reinstated with every suite green.",
    "file": "backend/src/__tests__/helpers/listening.ts",
    "anchor": "    probe.listen(0, '127.0.0.1', () => resolve(probe));",
    "mutation": "    probe.listen(0, () => resolve(probe));",
    "runner": "jest",
    "spec": "src/__tests__/listening-helper.test.ts",
    "test": "verifies ports on the v4 loopback exactly, where supertest dials"
  },
  {
    "id": "listening-binds-from-the-verified-reservoir",
    "why": "listening() must consume a verified port, not fall back to listen(0): the wildcard bind is what gets assigned a port whose 127.0.0.1 side another process owns, sending that suite's requests to a stranger - the cto/AdaptaLabs#44 red run, reproduced on demand by parking the ephemeral counter below a squatted band. Reverting this call is invisible to every functional test because every server still works whenever the counter is elsewhere.",
    "file": "backend/src/__tests__/helpers/listening.ts",
    "anchor": "  const server = createServer(app as RequestListener);\n  bindVerifiedPort(server);",
    "mutation": "  const server = createServer(app as RequestListener).listen(0);",
    "runner": "jest",
    "spec": "src/__tests__/listening-helper.test.ts",
    "test": "consumes the reservoir rather than binding wildcard-ephemeral"
  },
  {
    "id": "new-session-past-start-rule-exists",
    "why": "The create-only refusal is the whole of cto/AdaptaLabs#90: without this line every session-create route accepts a 1999 start from an authenticated admin, and because the booking guard reads END time, a past-start session with a future end is fully bookable. Deleting the push leaves an empty if-block that builds and passes everything except the named refusal arms.",
    "file": "backend/src/validation/schemas.ts",
    "anchor": "    errors.push('Start time must not be in the past');",
    "mutation": "",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.create-refuses-the-past.test.ts",
    "test": "answers 400 to a past session and opens no transaction"
  },
  {
    "id": "past-start-grace-is-bound-at-the-use-site",
    "why": "The literal pin on NEW_SESSION_PAST_GRACE_MS cannot see a use-site widening: multiplying here turns the one-minute clock-skew grace into an hour with the constant test still green - the review gate proved the pair of guards is only complete with both arms.",
    "file": "backend/src/validation/schemas.ts",
    "anchor": "  if (start.getTime() < Date.now() - NEW_SESSION_PAST_GRACE_MS) {",
    "mutation": "  if (start.getTime() < Date.now() - NEW_SESSION_PAST_GRACE_MS * 60) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/sessions.create-refuses-the-past.test.ts",
    "test": "accepts within the grace window and refuses beyond it"
  },
  {
    "id": "logger-stringify-applies-the-redaction",
    "why": "The replacer on this line is the whole of cto/AdaptaLabs#88: unwire it and every failed API call serialises the axios error raw - a researcher's note about a named colleague on a failed notes save, and the live CSRF token on every failed mutating call. Both review gates proved the unwired form passes every functional test that existed before this suite.",
    "file": "frontend/src/utils/logger.ts",
    "anchor": "      return JSON.stringify(logEntry, redactRequestBodies);",
    "mutation": "      return JSON.stringify(logEntry);",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/utils/__tests__/logger.redacts-request-bodies.test.ts",
    "test": "a production log line names the failure but never the note"
  },
  {
    "id": "reschedule-not-configured-short-circuits-calendar-update",
    "why": "cto/AdaptaLabs#93 gap 1. POST /api/bookings/:id/reschedule short-circuits its calendar UPDATE when updateEvent reports CALENDAR_NOT_CONFIGURED, instead of falling through to delete+create. Deleting the short-circuit was proved by a gate to pass the whole backend suite (1553 tests), because nothing exercised the reschedule not-configured branch - the cost being three false WARNs per reschedule of any legacy booking (update failed / could not delete / recreation failed), permanently, in the log stream the create path was cleaned up to protect. This mutation makes the branch never fire (`else if (false)`), so the reschedule falls through to the WARN; caught by the named test. Anchored on `updateResult` so it is unique against the create (`createResult`) and cancel (`deleteResult`) not-configured branches in the same file.",
    "file": "backend/src/routes/bookings.ts",
    "anchor": "        } else if (updateResult.error === CALENDAR_NOT_CONFIGURED) {",
    "mutation": "        } else if (false) {",
    "runner": "jest",
    "spec": "src/routes/__tests__/bookings.calendar-not-configured.test.ts",
    "test": "short-circuits the calendar update at INFO rather than three delete-and-create WARNs"
  },
  {
    "id": "manual-slot-prune-keeps-protected-slots-first",
    "why": "cto/AdaptaLabs#93 gap 2. pruneOverlaps sorts protected (hand-entered) slots first so that when a manual slot overlaps a generated one, the manual one is added to the non-overlapping set and the generated one is dropped - not the other way round. Dropping this comparator discards a hand-entered slot before rendering while it stays selected and creatable, so the researcher confirms a slot they can no longer see. jsdom has no layout, but the DROP is a data effect: the named test asserts the manual slot is still drawn. This mutation neuters the protected-first tiebreak.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "    if (a.isProtected !== b.isProtected) return a.isProtected ? -1 : 1;",
    "mutation": "    // protected-first removed",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.manual-slots.test.tsx",
    "test": "draws a hand-entered slot the generated grid cannot express"
  },
  {
    "id": "manual-slot-is-marked-protected-at-a-generated-instant",
    "why": "cto/AdaptaLabs#93 gap 2. A hand-entered slot is ALWAYS added to manualSlotKeys, even when it coincides exactly with a generated grid instant - the third shape of the same defect: an unprotected slot at a generated instant is pruned away by a protected neighbour and stays selected. This mutation drops slotKey from the set (`new Set([...prev])`), so the manual slot is not protected and loses the prune; caught by the on-boundary named test. Anchored on the two lines together because the bare `setManualSlotKeys(prev => {` recurs at the deselect site with a different body.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "    setManualSlotKeys(prev => {\n      const next = new Set([...prev, slotKey]);",
    "mutation": "    setManualSlotKeys(prev => {\n      const next = new Set([...prev]);",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.manual-slots.test.tsx",
    "test": "keeps drawing an on-boundary manual slot after the duration control moves"
  },
  {
    "id": "out-of-range-sessions-are-drawn-where-they-can-be-clicked",
    "why": "The whole of cto/AdaptaLabs#95 is this predicate: with it false, a session outside the drawn 07:00-23:00 goes back onto the timeline, where getTimePosition clamps it to ~0.01% height on an edge - present in the DOM, counted, and impossible to click. The per-slot click is the ONLY per-session removal on this screen, so such a session is stuck there permanently. jsdom has no layout, so clicking the sliver in a test SUCCEEDS: only the placement and geometry arms can see this, and nothing else in the suite asserts them.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "  return startHours < TIMELINE_START_HOUR || endHours > TIMELINE_END_HOUR;",
    "mutation": "  return false;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.out-of-range-sessions.test.tsx",
    "test": "draws one before the timeline starts in the gutter row, at full size"
  },
  {
    "id": "oauth-state-is-bound-to-the-nonce-in-the-cookie",
    "why": "cto/AdaptaLabs#82's browser binding, and it had NO oracle at all before the arms added with cto/AdaptaLabs#92: deleting this comparison passed all 55 tests across the module suite and both flow suites, because every other arm either presents the matching state or presents no cookie and is refused one check earlier. Without it `consume` accepts ANY state string as long as one of our envelopes opens, so a victim who has begun their own login - and therefore holds a valid cookie - can be lured to the callback carrying an attacker's authorization code.",
    "file": "backend/src/utils/oauthState.ts",
    "anchor": "        !crypto.timingSafeEqual(presented, expected)",
    "mutation": "        false",
    "runner": "jest",
    "spec": "src/utils/__tests__/oauthState.test.ts",
    "test": "refuses a state that is not the nonce inside the cookie"
  },
  {
    "id": "opportunities-fanout-reads-only-actionable-sessions-for-participants",
    "why": "The whole of cto/AdaptaLabs#62. Drop this and the participant catalogue fans out over every session ever created again, so the 5000 ceiling it feeds becomes a date rather than a risk - and because frontend/src/pages/Home.tsx issues the same request, that date is a whole-product 413 with no smaller request to retry. Invisible to every mocked suite: the sibling arms in opportunities.list-bound.test.ts assert SQL TEXT, and a text assertion cannot see which rows a predicate excludes. Only the real-Postgres spec inserts a finished session and looks.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "WHERE s.opportunity_id = ANY($1::uuid[])${isAdmin ? ADMIN_RECENT_SESSIONS_ONLY : UPCOMING_SESSIONS_ONLY}",
    "mutation": "WHERE s.opportunity_id = ANY($1::uuid[])",
    "runner": "vitest",
    "spec": "src/routes/__tests__/opportunities.list-upcoming-sessions-postgres.test.ts",
    "test": "leaves the finished session out of the anonymous catalogue",
    "needsDatabase": true
  },
  {
    "id": "opportunities-admin-fanout-windows-the-recent-tail",
    "why": "cto/AdaptaLabs#103. The admin branch of the same fan-out used to be UNFILTERED, so its 5000 ceiling grew monotonically over the platform's lifetime - a date rather than a risk. #103's decision windows it to the live schedule plus a 14-day tail (ADMIN_RECENT_SESSIONS_ONLY). This mutation reverts the admin branch to unfiltered, which is exactly the tidy-up a reader wanting lifetime admin totals back would make - and it reopens the growth. Invisible to every mocked suite (the list-bound arms assert SQL TEXT); only the real-Postgres spec inserts a session older than the window and proves it absent for the admin. Distinct from the participant entry above, which shares this line but tests the anonymous branch.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "WHERE s.opportunity_id = ANY($1::uuid[])${isAdmin ? ADMIN_RECENT_SESSIONS_ONLY : UPCOMING_SESSIONS_ONLY}",
    "mutation": "WHERE s.opportunity_id = ANY($1::uuid[])${isAdmin ? '' : UPCOMING_SESSIONS_ONLY}",
    "runner": "vitest",
    "spec": "src/routes/__tests__/opportunities.list-upcoming-sessions-postgres.test.ts",
    "test": "hands an admin the recent and live sessions but not one past the window",
    "needsDatabase": true
  },
  {
    "id": "booking-artifact-media-is-rate-limited",
    "why": "cto/AdaptaLabs#99. GET /:bookingId/artifacts/:artifactId/media runs a HeadObject on EVERY request - including each Range request a seeking player fires - then a GetObject, and until this entry the route carried no ceiling at all, the specific gap #99 named alongside the ETag's own MD5-collision limitation. This repo's standard for a limiter (routes/firsthand.ts) is a named test PLUS a canary entry; this is that second half. Proven KILLED before entry: with artifactMediaLimiter removed from the route, the 121st request in a minute answers whatever the handler would (200/404/409) instead of 429.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "  requireAdmin,\n  artifactMediaLimiter,\n  asyncHandler(async (req: Request, res: Response) => {\n    const booking = await loadOwnedBooking(req);\n    const { artifactId } = req.params;",
    "mutation": "  requireAdmin,\n  asyncHandler(async (req: Request, res: Response) => {\n    const booking = await loadOwnedBooking(req);\n    const { artifactId } = req.params;",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "refuses a 121st media request in a minute, and not the 120th - the gap #99 named as unthrottled"
  },
  {
    "id": "booking-artifact-limiter-sits-after-the-auth-gate",
    "why": "The !201 ordering rule (routes/firsthand.ts), re-proven for this router's own three limiter instances: mounted ahead of requireAdmin a limiter spends a bucket on requests that never authenticated, and every one of them keys to the same 'unauthenticated' fallback - one shared bucket any anonymous caller could exhaust to refuse every researcher_admin in the estate. The table test's per-route 'ordered' check is what catches a reversal on any one of the five routes; this entry anchors the media route as the representative case.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "  requireAdmin,\n  artifactMediaLimiter,",
    "mutation": "  artifactMediaLimiter,\n  requireAdmin,",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "mounts the RIGHT limiter on every route, after the auth gate"
  },
  {
    "id": "booking-artifact-media-scope-binding-against-real-postgres",
    "why": "cto/AdaptaLabs#99. Same defect as booking-artifact-media-scope-binding, proven against a REAL database rather than a mock. That entry's own gate found the mocked pool's first version vacuous (a fixture null did the refusing, not the clause) and fixed it with a parameter-honouring mock plus a text pin - but a mock, however faithful, still cannot execute a real join across two real rows. This spec seeds two bookings under ONE owner (so a 404 can only be the booking_id clause, never the ownership gate) and a real artefact row on booking B, then requests it through booking A. Proven KILLED before entry: widening the clause to `(a.booking_id = $2 OR TRUE)` turns the 404 into a 200 serving booking B's artefact through booking A's path.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "      WHERE a.id = $1 AND a.booking_id = $2",
    "mutation": "      WHERE a.id = $1 AND (a.booking_id = $2 OR TRUE)",
    "runner": "vitest",
    "spec": "src/routes/__tests__/booking-artifacts-scope-postgres.test.ts",
    "test": "404s an artefact requested through a booking that does not own it",
    "needsDatabase": true
  },
  {
    "id": "booking-artifact-finalize-copies-to-an-unpresigned-key",
    "why": "cto/AdaptaLabs#99's core fix, replacing the ETag tripwire this manifest previously carried a separate entry for: finalize copies the pending upload to a booking-artifacts/finalized/ key that no presigned PUT URL was ever issued for, and stores THAT key as the artefact's relative_path - not the pending key the client's URL still addresses. Revert to storing the pending key and a re-PUT to that still-valid URL, at any point after finalize, silently swaps the bytes the media route will serve forever after; nothing downstream would ever know. Proven by mutation before entry: storing pending.relative_path here instead of finalizedKey failed only the named test, which asserts the stored key matches the finalized/ pattern (and, via the same call, that copyS3ObjectIfMatch received the inspected ETag as its condition).",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "        finalizedKey,",
    "mutation": "        pending.relative_path,",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "copies the pending object to a finalized/ key, deletes the old key, and stores the new one"
  },
  {
    "id": "booking-artifact-media-never-reverifies-against-s3",
    "why": "cto/AdaptaLabs#99's cost half: because finalize now copies to a key no presigned URL addresses, the media route's per-request HeadObject-plus-ETag-comparison (the tripwire this replaces) has nothing left to verify - the object cannot have been swapped. Reintroducing that call, even harmlessly, silently reopens the doubled S3 call per request the issue named (including once per Range request a seeking player fires). Proven by mutation before entry: inserting a HeadObject call before the response is built failed only the named test, which asserts headS3ObjectStat is never called when serving media.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "    let mediaResponse: globalThis.Response;",
    "mutation": "    await headS3ObjectStat(artifact.relative_path);\n    let mediaResponse: globalThis.Response;",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "never re-verifies the object against S3 before serving - finalize already made that check unnecessary"
  },
  {
    "id": "booking-artifact-finalize-refuses-a-changed-object-retryably",
    "why": "cto/AdaptaLabs#99: copyS3ObjectIfMatch conditions its copy on the ETag finalize inspected, so an object that changed between that HeadObject and the copy (the finalize-time race the conditional copy exists to close) raises ArtifactCopyRaceError rather than silently copying whatever is there now. Swallowing that error - or letting it fall through to a generic 500 that still commits an artefact row - would finalize bytes nobody reviewed under the reviewed row's identity, or leave the pending row consumed with no artefact to show for it. Proven by mutation before entry: catching ArtifactCopyRaceError and proceeding to INSERT anyway (using the pre-copy objectStat data) failed only the named test, which asserts the row is left uncommitted and the pending row survives for a retry.",
    "file": "backend/src/routes/booking-artifacts.ts",
    "anchor": "      if (error instanceof ArtifactCopyRaceError) {\n        // The pending row and its object are left exactly as they were - a\n        // retry re-reads whatever is there now and races again, no cleanup\n        // needed on this arm.\n        return res.status(409).json({ error: ARTIFACT_CHANGED_BEFORE_FINALIZE });\n      }\n      throw error;",
    "mutation": "      if (error instanceof ArtifactCopyRaceError) {\n        // mutated: race swallowed, finalize proceeds as if the copy succeeded\n      } else {\n        throw error;\n      }",
    "runner": "jest",
    "spec": "src/routes/__tests__/booking-artifacts.test.ts",
    "test": "refuses by sentence, 409, when the object changed just before it could be copied - and leaves the pending row for a retry"
  },
  {
    "id": "copy-s3-object-if-match-conditions-on-the-etag",
    "why": "cto/AdaptaLabs#99's whole security property, and until this entry it was proven ONLY by s3-upload-integration.test.ts's real-MinIO tests - which every CI backend job and the local mutation canary both skip (FIRSTHAND_SKIP_S3_TESTS=1, that file's own docblock and mutation-canary.mjs's runNamedTest). Every other reference mocks copyS3ObjectIfMatch wholesale, so nothing CI-runnable inspected what was actually sent to S3. A security gate measured this directly: deleting CopySourceIfMatch from the CopyObjectCommand left the full CI-runnable suite green - 1787 jest, 691 vitest, typecheck unchanged - meaning a future refactor could remove the finalize-time race guard with a fully green pipeline, reopening the HeadObject-then-copy window a re-PUT can land in. Proven by mutation before entry: this exact deletion failed only the named test, which asserts the CopyObjectCommand's own input carries CopySourceIfMatch.",
    "file": "backend/src/firsthand/runtime-object-storage-s3.ts",
    "anchor": "        CopySource: encodeURI(`${config.bucket}/${sourceKey}`),\n        CopySourceIfMatch: expectedEtag",
    "mutation": "        CopySource: encodeURI(`${config.bucket}/${sourceKey}`)",
    "runner": "vitest",
    "spec": "src/firsthand/__tests__/copy-s3-object-if-match.test.ts",
    "test": "conditions the copy on the ETag the caller inspected - the finalize-time race guard itself"
  },
  {
    "id": "native-delivery-covers-the-one-question-type",
    "why": "The whole of cto/AdaptaLabs#78 is this predicate's type set. Narrowing it back to poll and survey is a two-word edit that compiles, leaves the authoring form offering the Question step, lets the author write their question and publish - and then answers the participant's mint with a 404, so the control on the study page is the one thing that fails. One predicate now decides this at the publish gate, the API boundary, the wizard's step shape and the participant's call to action, which is what makes a single silent edit reach all four.",
    "file": "shared/firsthand/delivery.ts",
    "anchor": "  QUESTION_CARRYING_TYPES.has(type) && (deliveryMode ?? \"external\") === \"native\";",
    "mutation": "  (type === \"poll\" || type === \"survey\") && (deliveryMode ?? \"external\") === \"native\";",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.test.ts",
    "test": "mints for a one-question opportunity"
  },
  {
    "id": "one-question-cap-is-a-cap",
    "why": "A POLICY CONSTANT, pinned as a literal because a test that derives its expectation from the constant cannot see the constant move. Without the cap, `question` and a native `survey` are the same product under two names, and a researcher can publish twelve questions under a badge the participant reads as \"One question\". The mutation is the shape a tidy-up would produce - one ceiling for every type - and it leaks nothing, breaks no schema and refuses nobody, so no gate reading for authorisation or validity would flag it.",
    "file": "shared/firsthand/survey-authoring.ts",
    "anchor": "  type === \"question\" ? 1 : INLINE_STUDY_LIMITS.maxSteps;",
    "mutation": "  INLINE_STUDY_LIMITS.maxSteps;",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.test.ts",
    "test": "refuses a second question"
  },
  {
    "id": "booking-consent-baseline-fallback",
    "why": "cto/AdaptaLabs audit row 9. A moderated booking (test/interview) must ALWAYS present consent - the researcher's own wording or the Cortex-owned moderated-default baseline - so a live session whose opportunity carries a null consent_text no longer books with no consent shown and no acceptance record. bookingConsentText is the single rule both the booking gate (bookings.ts) and the participant detail page resolve from; dropping the `|| baseline` fallback returns '' for a null-consent moderated opportunity, which the booking gate reads as 'nothing to accept' and books silently - the exact pre-fix exposure. Proven by mutation before entry: `return stored;` left every suite green except the named test (and the three baseline refusal tests in bookings.consent-acceptance.test.ts).",
    "file": "shared/firsthand/consent-templates.ts",
    "anchor": "  return (stored || MODERATED_CONSENT_TEMPLATE.text).trim();",
    "mutation": "  return stored;",
    "runner": "vitest",
    "spec": "src/firsthand/consent-templates.test.ts",
    "test": "a live session with no wording of its own falls back to the moderated baseline"
  },
  {
    "id": "publish-gate-refuses-slotless-bookable",
    "why": "cto/AdaptaLabs audit row 15 / #118. A published live session (test) or interview must have at least one bookable slot, or the participant home advertises LIVE / \"Book a time\" over nothing anyone can book (the a19/a21 defect). `findPublishProblem` refuses it on `hasBookableSlot === false`, which the create guard supplies as false (a create writes no sessions) and the update guard supplies from a real `end_time > NOW()` count. Flipping the comparison to `=== true` inverts the gate - it would permit a slotless publish and refuse one that has a slot - and leaks nothing, breaks no schema and touches no auth, so no gate reading for authorisation or validity flags it. The tri-state signal is the subtle part: `undefined` must stay permitted, so the naive `!input.hasBookableSlot` a tidy-up would reach for refuses every caller that cannot count. Proven by mutation before entry: `=== true` left every other suite green except the named test.",
    "file": "shared/firsthand/publish-readiness.ts",
    "anchor": "    return input.hasBookableSlot === false",
    "mutation": "    return input.hasBookableSlot === true",
    "runner": "vitest",
    "spec": "src/firsthand/publish-readiness.test.ts",
    "test": "bookable_slot_required fires on a positive report of no bookable slot"
  },
  {
    "id": "publish-gate-counts-only-upcoming-slots",
    "why": "cto/AdaptaLabs audit row 15 / #118, the ROUTE half. The update publish guard supplies `hasBookableSlot` from a count over the participant-ACTIONABLE set (`end_time > NOW()`), matching UPCOMING_SESSIONS_ONLY and the bookings refusal - NOT a bare COUNT(*). A study whose only slot ended yesterday is as unbookable as one with none, so dropping the `AND end_time > NOW()` clause would count that past slot and re-open the a21 defect: a dashboard advertising LIVE / 'Book a time' over nothing anyone can book. A text or mocked assertion cannot see which rows a real NOW() comparison excludes (the shared unit half pins the predicate, not the query), so this is a needsDatabase entry killed by a real-Postgres past-slot test. Proven by mutation before entry: the bare-count query permitted the past-only publish, failing only the named test.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "        'SELECT 1 FROM sessions WHERE opportunity_id = $1 AND end_time > NOW() LIMIT 1',",
    "mutation": "        'SELECT 1 FROM sessions WHERE opportunity_id = $1 LIMIT 1',",
    "runner": "vitest",
    "spec": "src/routes/__tests__/opportunities.publish-slot-gate-postgres.test.ts",
    "test": "counts only upcoming slots: a test whose only slot is in the past cannot publish",
    "needsDatabase": true
  },
  {
    "id": "answered-runtime-set-pins-uploading",
    "why": "cto/AdaptaLabs audit row 10. `answeredRuntimeStates` is the single source three call sites share for 'has this participant answered' - the survey mint gate's 409, the detail page's completion trace, and the home row's Completed word. Dropping `uploading` would let a survey mid-upload read as re-answerable at all three, and re-answering rewrites the stored responses with no history. Pinned as a literal so the set cannot be narrowed silently: a derived expectation could not see the change. Proven by mutation before entry - the narrowed set failed only the named test.",
    "file": "backend/src/firsthand/state-model.ts",
    "anchor": "export const answeredRuntimeStates = [\"completed\", \"uploading\"] as const;",
    "mutation": "export const answeredRuntimeStates = [\"completed\"] as const;",
    "runner": "vitest",
    "spec": "src/firsthand/__tests__/state-model.test.ts",
    "test": "pins the answered set to exactly completed and uploading"
  },
  {
    "id": "completion-trace-reads-the-real-status",
    "why": "cto/AdaptaLabs audit row 10, the wiring half. participantCompletionMap derives the participant-facing `completed` flag from the session's real status through `isAnsweredRuntimeStatus`, not from mere presence of a session. Hardcoding it true would mark an in-progress (resumable) survey as Completed, hiding the Start button on a survey the participant can still finish - the exact 'Completed means visited' class this row exists to end. A text assertion cannot see the derivation; this jest route test drives an in-progress session and demands completed:false. Proven by mutation before entry - `const completed = true` failed only the named test.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "      const completed = isAnsweredRuntimeStatus(row.sessionStatus);",
    "mutation": "      const completed = true;",
    "runner": "jest",
    "spec": "src/routes/__tests__/opportunities.test.ts",
    "test": "does not treat a still-in-progress session as completed"
  },
  {
    "id": "completion-trace-scoped-to-the-participant",
    "why": "cto/AdaptaLabs#119, the scoping half of audit row 10. `WHERE participant_id = $1` on `findParticipantCompletionsForOpportunitiesPostgres` is the IDOR filter on the completion trace: it is what stops caller A being told they completed a study that caller B completed. Nothing sees it removed - the route tests in `opportunities.test.ts` mock this repository, so the predicate is only ever evaluated by a database. The named real-Postgres test seeds A and B across a shared opportunity and makes B's `completed` session the NEWEST row, so a dropped filter does not merely widen the result: `DISTINCT ON (opportunity_id) ... created_at DESC` picks B's row and A's read returns `completed` with B's completed_at. The mutation keeps `$1` referenced (`AND $1::text IS NOT NULL`, always true here) rather than the bare `participant_id = participant_id` tautology on purpose - orphaning `$1` makes Postgres answer `42P18 could not determine data type of parameter $1` before the scope is ever evaluated, which reds the suite for the wrong reason and the harness grades that KILLED. Keeping it referenced makes the query run and fail on the row it gets back. Proven by mutation before entry - the named test kills it on the status not the load; the control test reddens too, as expected, since B's now-unscoped read returns two rows.",
    "file": "backend/src/firsthand/runtime-repository-postgres.ts",
    "anchor": "        WHERE participant_id = $1",
    "mutation": "        WHERE participant_id = participant_id AND $1::text IS NOT NULL",
    "runner": "vitest",
    "spec": "src/firsthand/runtime-participant-completion-scope-postgres.test.ts",
    "test": "scopes each participant's completions to their own sessions",
    "needsDatabase": true
  },
  {
    "id": "screener-gate-refuses-a-non-qualified-participant",
    "why": "The screener's entire value is that the three apply chokepoints refuse anyone without a stored 'qualified' verdict, and this throw in assertScreenerPassed IS that refusal. Turning it into a plain return lets a participant who never took the screener, or who was screened out, book or mint a session anyway. The mocked-pool route tests cannot see it: the verdict lookup only ever runs against a real database. The named real-Postgres test seeds a published opportunity that has a screener and NO verdict for the participant, and asserts the booking answers 403. Proven by mutation before entry: throw->return reds the named test, which answers 201 instead.",
    "file": "backend/src/services/screener.ts",
    "anchor": "    throw new ForbiddenError(SCREENER_NOT_PASSED);",
    "mutation": "    return;",
    "runner": "vitest",
    "spec": "src/routes/__tests__/screener-enforcement-postgres.test.ts",
    "test": "refuses a booking when a screener exists and the participant has no verdict",
    "needsDatabase": true
  },
  {
    "id": "screener-redaction-strips-the-disqualifies-flag",
    "why": "The screener's disqualifies flags are owner-only: a participant who could see which answer screens them out could game the screener. toParticipantScreenerField reduces the screener to its participant-facing shape at the public serialiser, and this is the line that strips the flag. Returning the raw screener instead publishes it to every participant on the browse and detail pages, since those routes select the whole row and this deny-list file publishes new columns by default. The named jest test asserts the serialised participant payload carries no disqualifies anywhere. Proven by mutation before entry: returning the raw screener reds the named test.",
    "file": "backend/src/utils/publicOpportunity.ts",
    "anchor": "    return redactScreenerForParticipant(screener as Screener);",
    "mutation": "    return screener;",
    "runner": "jest",
    "spec": "src/utils/__tests__/publicOpportunity.screener.test.ts",
    "test": "strips the owner-only disqualifies flag from every option"
  },
  {
    "id": "screener-gate-wired-into-the-survey-session-mint",
    "why": "The shared assertScreenerPassed guard is only load-bearing where it is actually called, and the survey-session mint is one of the three chokepoints it must gate. The guard-internal canary is exercised by the booking test and cannot see this call site removed; without an entry here, deleting or no-oping this line lets a screened-out participant mint a native survey/poll session and the suite stays green. The gate fires before the linked-study check, so the named real-Postgres test seeds a published native survey with a screener and NO verdict and asserts 403. Proven by mutation before entry: replacing the call with an inert await reds the named test, which answers 400 (no study linked) instead of 403.",
    "file": "backend/src/routes/opportunities.ts",
    "anchor": "    await assertScreenerPassed(pool, loaded.canonicalOpportunityId ?? id, req.user.id, row.screener);",
    "mutation": "    await Promise.resolve();",
    "runner": "vitest",
    "spec": "src/routes/__tests__/screener-enforcement-postgres.test.ts",
    "test": "refuses to mint a native survey session when the participant has no verdict",
    "needsDatabase": true
  },
  {
    "id": "session-management-slot-counter-excludes-allocated-cells",
    "why": "cto/AdaptaLabs#135: drawnSlots and tableSlotCount used to exclude only a calendar-conflict cell, so a researcher on the Calendar or Table view could see an inflated \"N slots available\" that counted a cell overlapped by a session for another study - one no click could turn into a new session. A lone session cannot reach this branch in a full render (protectedSlotKeys injects its exact time as a slot pruneOverlaps prefers, so the overlapping generated cell is pruned first); only two sessions overlapping each other can, which the mixed-day suite also covers. The named test calls isSlotPickable directly, bypassing that pipeline, so it isolates this line without depending on the fixture.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "  if (isAllocated) return false;",
    "mutation": "  if (false) return false;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "excludes a slot overlapped by another session at a non-exact boundary, and nothing else"
  },
  {
    "id": "session-management-select-all-uses-the-pickable-predicate",
    "why": "cto/AdaptaLabs#135: the per-day Select all (N) in the Table view counted every slot that did not conflict with a calendar event, while the headline counted only pickable ones - the original \"159 slots available against a Select-all sum of 129\" gap, the other way round. Reverting `actionable` to conflicts-only kept all 116 AdminSessionManager tests green before the mixed-day suite existed; the named test asserts headline === sum of Select all counts on a day carrying a past, a session, a full, an odd-length and an allocated cell.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "          const actionable = decorated.filter(d => isSlotPickable(d.slot, events, sessions, confirmedSlots));",
    "mutation": "          const actionable = decorated.filter(d => !d.status.isBusy);",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "Table view: the headline equals the sum of the per-day Select all counts"
  },
  {
    "id": "session-management-slot-counter-excludes-confirmed-only-cells",
    "why": "cto/AdaptaLabs#135: a temporary (new) study has sessions=[] and restores confirmedSlots from sessionStorage, so a confirmed-but-unsaved cell is excluded by this check alone - `isExistingSession` cannot see it. In a saved study the two are derived from each other and a full render masks the removal of either; the named test calls isSlotPickable with only confirmedSlots set.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "  if (isExistingSession || isConfirmed) return false;",
    "mutation": "  if (isExistingSession) return false;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "excludes a slot held only in confirmedSlots, with no session anywhere"
  },
  {
    "id": "session-management-slot-counter-excludes-existing-session-cells",
    "why": "cto/AdaptaLabs#135: the existing-session exclusion. In a saved study confirmedSlots is rebuilt from sessions, so a full render masks deleting this check (confirmedSlots still excludes the cell) - it survived the whole AdminSessionManager suite. The named test calls isSlotPickable with only sessions set, so it fails the moment the check is gone.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "  if (isExistingSession || isConfirmed) return false;",
    "mutation": "  if (isConfirmed) return false;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "excludes a slot that exactly matches a session, with confirmedSlots empty"
  },
  {
    "id": "session-management-chip-blocks-allocated-cells",
    "why": "cto/AdaptaLabs#135: describeSlot is a separate implementation from isSlotPickable, so the chips can drift from the counters. Without isAllocated in isBlocked, a generated cell overlapped by a session drops out of the counters but its chip becomes a clickable free slot. Survived every suite before the mixed-day chip test, which asserts the chips a click could newly select are exactly the counted ones.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "    const isBlocked = isBusy || (!!session && session.remaining <= 0) || isAllocated;",
    "mutation": "    const isBlocked = isBusy || (!!session && session.remaining <= 0);",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "Table view: every counted slot is an enabled chip and every uncounted chip is not offered"
  },
  {
    "id": "session-management-chip-blocks-conflicting-cells",
    "why": "cto/AdaptaLabs#135: without isBusy in describeSlot's isBlocked, a calendar-conflict cell stays out of the counters but its chip becomes a clickable free slot - the chips and the counters disagree. The mixed-day chip test asserts the offered chips are exactly the counted ones.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "    const isBlocked = isBusy || (!!session && session.remaining <= 0) || isAllocated;",
    "mutation": "    const isBlocked = (!!session && session.remaining <= 0) || isAllocated;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "Table view: every counted slot is an enabled chip and every uncounted chip is not offered"
  },
  {
    "id": "session-management-chip-disables-past-cells",
    "why": "cto/AdaptaLabs#135 and #101: forcing describeSlot's isPast false made a past chip clickable while every counter still excluded it, and survived the whole frontend suite. The server refuses a past start, so the researcher would select a slot that can never be created. The mixed-day chip test asserts the offered chips are exactly the counted ones.",
    "file": "frontend/src/components/AdminSessionManager.tsx",
    "anchor": "    const isPast = slotIsPast(slot) && !session && !isConfirmed && !isBusy && !isAllocated;",
    "mutation": "    const isPast = false;",
    "runner": "vitest",
    "project": "frontend",
    "spec": "src/components/__tests__/AdminSessionManager.slot-counter-pickability.test.tsx",
    "test": "Table view: every counted slot is an enabled chip and every uncounted chip is not offered"
  }
]
